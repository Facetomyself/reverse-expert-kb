# 2026-03-21

- Checked `oracle-proxy:/root/grok2api` against upstream `TQZHR/grok2api` without touching the separate Grok registration stack.
- Verified deployed `grok2api` was already current with upstream at `d6a945c`; no upstream update was needed.
- Found substantial local uncommitted deployment customizations in `/root/grok2api`, mainly around Camoufox override support, register proxying, email site password support, and Cloudflare-oriented register/account-settings refresh tolerance.
- Confirmed live service config comes from `/root/grok2api/data/config.toml`, not `config.defaults.toml`.
- Created on-host preservation branch `oracle-proxy/local-custom-20260321` in `/root/grok2api` and committed the local customization set as `2413e6c`, after sanitizing `config.defaults.toml` defaults to `app_key = "admin"` and `api_key = ""` while leaving live `data/config.toml` untouched.
- Updated workspace infra docs for `oracle-proxy` / `grok2api` to record the upstream status, config layering, and local preservation branch.
- `ops-assistant` false-positive follow-up: investigated `ali-cloud: core inspection partial failure -> docker` and confirmed live `docker ps` was healthy; raw `/tmp/ops_docker_inventory.json` showed a transient SSH banner-exchange timeout while host/systemd/drift checks were all OK. Hardened `ops-assistant/checks/docker_inventory.py` with a narrow 3-attempt retry for transient SSH handshake errors (`banner exchange timeout`, `Connection reset by peer`, `kex_exchange_identification`) and re-ran fleet check successfully with no remaining P2 for `ali-cloud`.
