# 2026-06-28 Daily Notes

## 04:00 — Scheduled Healthcheck

All hosts SSH-reachable. Full status summary:

### Tier0/1 (Critical)
- **oracle-open_claw** (local): ✅ Up 45d, disk 74% (12G free on 45G), 5.8GB RAM (4.0GB avail), no Docker
- **oracle-proxy**: ✅ Disk 45% (26G free), 12 Docker containers all up, ExaFree :7860 OK, Caddy inactive
- **oracle-gateway**: ✅ Disk 34% (31G free), Caddy active
- **oracle-newapi-primary**: ⚠️ Disk 9% (89G free), NewAPI container DOWN in compose but localhost:13000 returns 200
- **oracle-newapi-standby**: ⚠️ Same as primary
- **oracle-mail**: ✅ Disk 60% (15G free), outlook-email-plus healthy (2mo uptime), Caddy healthy

### Tier2
- **ali-cloud**: ⚠️ Disk 53% (18G free), **sing-box-gateway FAILED**, **hysteria-egress FAILED**, **dnsmasq inactive**, zcode2api unreachable (HTTP 000)
- **hk-relay**: ✅ Disk 11% (44G free)

### Tier3
- **self-server**: ✅ Disk 6% (48G free), :44001 has 1Panel+FRPS, :44005 has Docker (astrbot, proxy_pool, chatgpt-image-studio, napcat)
- **home/home-macmini/home-nas/company**: Not checked (Tailnet-only hosts)

### Fleet-Wide Observations
1. **Tailscale uninstalled on ALL hosts** — `tailscaled` inactive everywhere, binaries absent on oracle-proxy/hk-relay/newapi-primary. Tailnet connectivity no longer in use.
2. **ali-cloud proxy services down** — sing-box-gateway and hysteria-egress both FAILED. This may affect self-server's explicit proxy transit path (ali-cloud:2080/2081).
3. **NewAPI containers showing DOWN** on both primary and standby, but HTTP health check passes. Likely compose status mismatch.
