# Reverse Expert KB Autosync Run Report

Date: 2026-03-24 19:16 CST
Mode: external-research-driven
Repo: `research/reverse-expert-kb`
Branch focus: malware practical subtree
Chosen seam: Windows service failure-policy semantics after broad service ownership is already plausible

## Scope this run
This run intentionally avoided another dense-branch pass on browser/mobile/protected-runtime or another shallow internal sync.

Recent runs had already performed real external-research-driven maintenance on:
- protected-runtime exception/resume truth
- protocol pending-owner lifetime realism
- iOS Swift async-shape routing

The malware branch still had a thinner but practical continuation available inside the service-persistence area:
- the broad service branch already existed
- timeout-abuse and failure-action ownership already existed as a leaf
- but the leaf still benefited from sharper source-backed stop rules around **failure classification**, **non-crash failure handling**, **recovery-action storage shape**, and **failure-count/action-selection truth**

This run therefore kept the work practical and case-driven by materially improving that existing malware leaf instead of doing another wording-only or index-only repair.

## Branch-balance review
Current branch-balance judgment for this run:
- browser/mobile/protected-runtime/runtime-evidence remain easy to overfeed and had multiple recent source-backed runs already
- protocol/firmware and iOS also received substantive external work in the last few hours
- malware remains thinner overall, and the service-failure branch in particular still had under-preserved practical stop rules
- this made malware service-failure semantics a better target than another dense-branch refinement

Anti-stagnation check:
- this run performed a real explicit multi-source search attempt with `--source exa,tavily,grok`
- it did not spend the run on family-count sync, wording-only cleanup, or index-only maintenance
- it materially improved a practical malware leaf rather than only collecting loose notes

## External research pass
Search method used:
- explicit multi-source `search-layer` invocation via `--source exa,tavily,grok`

Queries used:
1. `Windows service failure actions SC_ACTION_RUN_COMMAND reverse engineering persistence recovery actions clean stop crash semantics`
2. `SERVICE_FAILURE_ACTIONS restart reset period run command malware service persistence analysis`
3. `ChangeServiceConfig2 SERVICE_CONFIG_FAILURE_ACTIONS ServiceMain timeout abuse reverse engineering`

Retained source signal:
- Microsoft Learn `SERVICE_FAILURE_ACTIONS` clarifies that failure policy explicitly carries reset period, action array, and optional run-command path, which is directly useful for reducing restart-owned or run-command-owned persistence
- Microsoft Learn `SERVICE_FAILURE_ACTIONS_FLAG` sharpens a practical stop rule: recovery can also fire for services that report `SERVICE_STOPPED` with non-zero service exit code when non-crash failure handling is enabled
- Microsoft Learn `ChangeServiceConfig2` reinforces that failure actions and failure-action flag are separate service configuration surfaces, which helps keep crash-only and non-crash recovery semantics distinct
- Stephen Cleary’s service-recovery note usefully reinforces the operational distinction between ordinary process exit folklore and SCM-visible service failure truth
- TeknikCS’s recovery-blob writeup usefully preserves the `FailureActions` binary-storage shape as a concrete contract object when action ordering/count matters beyond the ordinary UI summary
- Unit 42’s SCM-abuse reference remains the practical anchor for keeping timeout-abuse launcher cases separate from genuine service-recovery cases

Conservative synthesis boundary:
- the sources were used to sharpen operator routing and proof-object selection, not to overclaim that all malware service cases share identical implementation details
- lower-authority practitioner references were used only as support for workflow-centered stop rules after official Microsoft semantics were already stable enough

## KB changes made
### Updated canonical leaf
- `topics/malware-service-failure-action-and-timeout-abuse-workflow-note.md`

Material improvement:
- added a sharper practical split between crash-only recovery semantics and non-crash recovery semantics
- made `SERVICE_FAILURE_ACTIONS_FLAG` operationally useful instead of leaving it implicit
- added explicit stop rules against confusing process-exit folklore with SCM-visible service failure truth
- added explicit separation of failure-existence truth, failure-count truth, and action-selection truth
- preserved recovery-action storage shape (`FailureActions` blob / action ordering) as a real proof object for odd or blob-edited cases
- added a new practical scenario for `SERVICE_STOPPED`-with-error plus enabled non-crash recovery

### Updated archived source note
- `sources/malware/2026-03-24-service-failure-action-and-timeout-abuse-notes.md`

Material improvement:
- preserved the new routing rules around non-crash recovery, service-visible failure truth, and recovery-action storage shape
- made the retained practical stop rule explicit: keep failure existence, failure count, and selected action separate

### Added archived search artifact
- `sources/malware/2026-03-24-service-failure-actions-search-layer.txt`

## Why this change matters
Before this run, the malware service-failure leaf already knew that:
- timeout-abuse launcher cases exist
- recovery actions can matter
- `ServiceMain`, `svchost`/`ServiceDll`, timeout abuse, and failure-action ownership are distinct broad contract shapes

After this run, the branch more clearly preserves a narrower operator rule:
- recovery is not just “did the process crash?”
- service-visible failure semantics can differ from ordinary process-exit folklore
- non-crash failure handling is a distinct part of the persistence contract
- action ordering/count and reset-window selection can be behavior-bearing proof objects
- therefore the analyst should reduce one exact `failure classification -> selected recovery action -> later consumer/effect` chain instead of writing generic “service failed and got restarted” narration

That improves the KB itself because it strengthens a thinner, practical malware continuation and makes future service-persistence analysis more truthful and reusable.

## Search audit
Requested sources: exa, tavily, grok
Succeeded sources: exa, tavily, grok
Failed sources:
- none during the search-layer run
Endpoints used:
- Exa: `http://158.178.236.241:7860`
- Tavily: `http://proxy.zhangxuemin.work:9874/api`
- Grok: `http://proxy.zhangxuemin.work:8000/v1`

Direct-fetch degradation note:
- direct `web_fetch` extraction for Microsoft Learn and the two practitioner blog posts was usable enough for conservative synthesis
- no source failure changed the search-bearing status of this run

## Files changed this run
- `topics/malware-service-failure-action-and-timeout-abuse-workflow-note.md`
- `sources/malware/2026-03-24-service-failure-action-and-timeout-abuse-notes.md`
- `sources/malware/2026-03-24-service-failure-actions-search-layer.txt`
- `runs/2026-03-24-1916-run-report.md`

## Commit / sync plan
Because the wider workspace may contain unrelated changes, this run should:
- stage only the reverse-KB files listed above
- commit only those KB changes
- run `/root/.openclaw/workspace/scripts/sync-reverse-expert-kb.sh`

## Outcome
Completed an external-research-driven, branch-balance-aware malware maintenance pass that materially improved the service-failure practical leaf instead of drifting back into dense-branch polish or internal-only sync.
