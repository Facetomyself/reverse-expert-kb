# Reverse KB Run Report — 2026-04-08 09:52 Asia/Shanghai

Mode: external-research-driven
Date: 2026-04-08 09:52 Asia/Shanghai / 2026-04-08 01:52 UTC

## Summary
This run deliberately used the external-research slot on a thinner firmware/protocol continuation rather than feeding another broad protocol or protected-runtime page.

Chosen seam:
- **hardware-visible completion / interrupt truth vs ISR/deferred-worker consumer truth when MMIO or completion visibility already exists but the first durable consumer still sits later in the interrupt path**

Result:
- materially extended `topics/isr-and-deferred-worker-consequence-proof-workflow-note.md`
- added supporting source notes and saved the search-layer artifact

The practical value added this run is a sharper separation ladder for hardware-adjacent consequence proof:
- armed
- IRQ arrival / top-half truth
- deferred scheduling/execution truth
- first consumer/reduction truth

That keeps analysts from overreading MMIO write visibility, ISR entry, or DPC/tasklet/workqueue activity as already-good proof of the effect they actually care about.

## Direction / branch-balance review
Recent cadence before this run:
- 06:51: external malware send-boundary realism refinement
- 07:51: external protected-runtime topology choice refinement
- 08:52: external WebSocket framed-consumer refinement

That left the firmware/protocol hardware-adjacent side as a good practical target for this hour.

Branch-choice reasoning:
- anti-stagnation still favored a real external-research pass
- the protocol/firmware branch benefits more from narrow practical handoff notes than from broad survey growth
- the MMIO note already pointed naturally into the ISR/deferred-worker continuation, so this hour could improve the KB itself rather than just collect source notes
- this therefore improved branch balance while staying practical and case-driven

## What changed
### Added
- `sources/protocol/2026-04-08-0952-irq-search-layer.txt`
- `sources/protocol/2026-04-08-isr-deferred-worker-consequence-notes.md`
- `runs/2026-04-08-0952-run-report.md`

### Updated
- `topics/isr-and-deferred-worker-consequence-proof-workflow-note.md`

## New findings
Primary durable split reinforced this run:

```text
armed
  != IRQ arrival / top-half truth
  != deferred scheduling/execution truth
  != first consumer/reduction truth
```

Concrete practical reinforcements preserved in the note:
- Linux interrupt-handling surfaces already separate hard IRQ visibility from softirq/tasklet/workqueue/threaded follow-on work
- Windows documentation already separates ISR and DPC truth
- a visible ISR/DPC/tasklet/workqueue callback is still weaker than one completion/status reduction, reply-family choice, state write, or wakeup edge that actually predicts later behavior
- the workflow should stop at the smallest consequence-bearing reduction, not at generic interrupt plumbing

## Sources consulted
Primary source anchors:
- Linux kernel documentation surfaces for hard IRQ / softirq / tasklet / workqueue / threaded handling
- Microsoft documentation surfaces for ISR / DPC separation

Search-backed support:
- explicit `search-layer` attempt with `--source exa,tavily,grok`
- Exa and Tavily returned usable Linux/Windows interrupt-handling surfaces
- Grok was explicitly invoked but returned 502 errors through the configured proxy path
- embedded/DMA completion discussion surfaces were retained only conservatively as support for handoff shape

## Reflections / synthesis
This was the right shape of firmware/protocol external run.
It did **not** create a broad interrupt-taxonomy page.
It tightened one practical continuation inside the existing ISR/deferred-worker consequence note.

The durable branch value is:
- keeping hardware-visible arrival separate from interrupt-plumbing truth
- keeping ISR/top-half truth separate from deferred-worker truth
- keeping deferred-worker visibility separate from the first consumer/reduction that really matters
- making the hardware-adjacent side of the branch more operator-useful rather than just descriptive

## Candidate topic pages to create or improve
Good later firmware/protocol candidates if branch balance points back here:
- a narrower continuation when deferred scheduling/execution truth is already good enough but the remaining liar is one specific wakeup/reply/state-reduction family
- a parent/subtree sync only if the sharper interrupt-plumbing ladder still feels too leaf-local

## Next-step research directions
Near-term preference:
- rotate again after this run rather than clustering many external slots inside protocol/firmware continuations
- continue preferring bounded practical handoff refinements over broad family restatements
- avoid internal-only wording/index maintenance unless it blocks a practical source-backed addition

## Concrete scenario notes or actionable tactics added this run
- ask explicitly whether the run only proved armed state, only IRQ/top-half truth, only deferred scheduling/execution, or the first actual consumer/reduction
- do not stop at ISR/DPC/tasklet/workqueue visibility by default
- prefer the first completion/status reduction, state write, reply selector, or wakeup edge that predicts later behavior

## Search audit
Search sources requested:
- `exa`
- `tavily`
- `grok`

Search sources succeeded:
- `exa`
- `tavily`

Search sources failed:
- `grok`

Exa endpoint:
- via `python3 /root/.openclaw/workspace/skills/search-layer/scripts/search.py --source exa,tavily,grok --mode deep --intent exploratory --num 5 --queries ...`

Tavily endpoint:
- via `python3 /root/.openclaw/workspace/skills/search-layer/scripts/search.py --source exa,tavily,grok --mode deep --intent exploratory --num 5 --queries ...`

Grok endpoint:
- via configured completions proxy surfaced in runtime errors: `http://proxy.zhangxuemin.work:8000/v1/chat/completions`

Searches attempted this run:
1. `Linux interrupt top half threaded irq tasklet workqueue bottom half docs`
2. `Windows ISR DPC interrupt service routine deferred procedure call official docs`
3. `embedded dma completion interrupt bottom half deferred worker consequence`

Degraded-source-set note:
- this run still counts as a real external-research pass because all three sources were explicitly requested and Grok was actually invoked
- Grok returned repeated 502 errors through the proxy path
- Exa, Tavily, and the retained Linux/Windows documentation surfaces were sufficient for a bounded extension of the existing ISR/deferred-worker note

## Commit / archival sync
If diffs remain after report write:
- commit only KB paths from `/root/.openclaw/workspace`
- then run `/root/.openclaw/workspace/scripts/sync-reverse-expert-kb.sh`
- if archival sync fails, preserve the local commit and record the failure conservatively rather than treating it as KB-content failure

## Best-effort notes
- `.learnings/ERRORS.md` remains non-blocking / best-effort only
