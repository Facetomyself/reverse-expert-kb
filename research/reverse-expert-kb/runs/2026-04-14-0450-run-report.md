# Reverse KB Run Report — 2026-04-14 04:50 Asia/Shanghai

Mode: external-research-driven
Date: 2026-04-14 04:50 Asia/Shanghai / 2026-04-13 20:50 UTC

## Summary
This run deliberately used the external-research slot on a thinner deobfuscation seam instead of drifting back into browser/mobile maintenance or doing another internal-only synchronization pass.

Chosen seam:
- **once one normalized next-carrier already exists, copied-code / question-opaque branch inflation can still hide which visible branch family actually owns the trustworthy successor split**

Result:
- added a new bounded workflow note: `topics/copied-code-branch-inflation-to-trustworthy-successor-split-workflow-note.md`
- added fresh supporting source notes and saved the explicit tri-source search artifact
- synchronized the immediate opaque-predicate parent note, the protected-runtime subtree guide, the protected-runtime parent pages, and the top-level index

The practical value added this run is a sharper deobfuscation ladder:
- normalized next-carrier truth
- copied-arm / branch-inflation visibility truth
- carrier-dependency truth
- trustworthy successor-split truth
- dispatcher-contract truth
- only then safe CFG repair or downstream state-edge work

That keeps analysts from overreading copied-code width, one patched wrapper branch, one recovered next value, or one state-carrier family as if any of those already proved the real split.

## Direction / branch-balance review
Recent external cadence before this run had already rotated across:
- malware MSI custom-action execution truth
- iOS / Apple-platform XPC reply-error-invalidation-reconnect truth
- native Qt event-filter vs signal-slot consumer truth
- protected-runtime lower-surface vs richer user-space uncertainty reduction
- protocol retired-owner vs callback-firing compare truth

This run returned to the protected-runtime branch, but on a still-open thinner deobfuscation candidate rather than another topology-comparison pass or another browser/mobile convenience slot.

Branch-choice reasoning:
- anti-stagnation still required a real explicit `exa,tavily,grok` attempt in the current window
- the top-level index still carried a live thin deobfuscation candidate around copied-code branch inflation versus trustworthy successor-split proof
- recent external work had already rotated sanely across malware, iOS, native, protected-runtime, and protocol, so using this slot on a still-underfed deobfuscation seam did not create branch clustering risk
- the resulting delta is a concrete workflow continuation plus canonical synchronization, not another broad flattening taxonomy page

Current balance read after this run:
- browser/mobile remain strong and easy to overfeed, and were not chosen again
- recent external work continues to span several branches rather than collapsing back into browser/mobile or protocol convenience work
- the protected-runtime deobfuscation branch is healthier because broad next-carrier recovery no longer has to carry copied-code split realism only by implication
- a later deobfuscation slot should only return soon if repeated real cases show that successor-split truth is now good enough but dispatcher-contract preservation still lies during repair

## What changed
### Added
- `sources/protected-runtime/2026-04-14-0450-copied-code-successor-split-search-layer.txt`
- `sources/protected-runtime/2026-04-14-copied-code-branch-successor-split-notes.md`
- `topics/copied-code-branch-inflation-to-trustworthy-successor-split-workflow-note.md`
- `runs/2026-04-14-0450-run-report.md`

### Updated
- `topics/protected-runtime-practical-subtree-guide.md`
- `topics/opaque-predicate-and-computed-next-state-recovery-workflow-note.md`
- `topics/anti-tamper-and-protected-runtime-analysis.md`
- `topics/obfuscation-deobfuscation-and-packed-binaries.md`
- `index.md`

## Scope this run
Bounded protected-runtime / deobfuscation continuation only:
- no broad control-flow-flattening overview page
- no generic Tigress / OLLVM taxonomy expansion
- no browser/mobile maintenance detour
- no wording-only index repair pass without a new practical note

The run stayed focused on one operator question:
- when one normalized next-carrier already exists, which visible branch family actually owns the real successor split, and which copied / inflated arms are only widening the picture?

## New findings
Primary durable split reinforced this run:

```text
normalized next-carrier recovered
  != visible copied-code / question-opaque branch family understood
  != branch actually depends on the carrier or its dependency family
  != trustworthy successor split recovered
  != safe CFG repair
```

A second compact compare-heavy split reinforced this run:

```text
raw branch arms differ
  != normalized carrier outputs differ
  != backbone-mapped destinations differ
  != later state edge differs
```

Concrete practical reinforcements preserved in the KB:
- Tigress docs keep dispatch form, next-variable obfuscation, and conditional-branch encoding as separate knobs; AddOpaque `question` predicates explicitly create original-vs-copy branch families and can obfuscate the copied arm
- Binary Ninja’s MLIL/data-flow workflow is useful for constant-wrapper cleanup, but its own limitations around loops, writable segments, and unmodeled data remind us that constant-branch cleanup is weaker than proving a still-conditional split
- d0minik’s Binary Ninja unflattening workflow preserves the practical rule that state-carrier recovery and split ownership are separate steps; retained control-flow should actually depend on the state-variable dependency family
- MODeflattener’s Miasm/SSA workflow reduces conditional relevant blocks into `true_next` / `false_next` recovery plus later backbone mapping, which is exactly the right proof object once copied-code width is already distracting the analyst
- angr symbolic-execution docs support using symbolic constraints as a narrow A-to-B split extractor rather than as a demand for whole-function recovery first

## Sources consulted
Primary retained source anchors:
- Tigress documentation — `Flatten`
- Tigress documentation — `AddOpaque`
- Binary Ninja blog — `Automated Opaque Predicate Removal`
- d0minik — `Binary Ninja Shenanigans: Control Flow Unflattening`
- mrT4ntr4 — `MODeflattener - Miasm's OLLVM Deflattener`
- angr documentation — `Symbolic Execution`

Search-backed support:
- explicit `search-layer` attempt with `--source exa,tavily,grok`
- Exa and Tavily returned usable Tigress, Binary Ninja, angr, and Miasm-oriented surfaces
- Grok was explicitly invoked but failed with repeated 502 errors through the configured proxy path

## Reflections / synthesis
This was the right shape of deobfuscation external run.
It did **not** create another broad control-flow-flattening or Tigress overview page.
It promoted one still-open thin candidate into a concrete workflow continuation and synchronized that continuation into the immediate opaque-predicate parent note, the protected-runtime subtree guide, the protected-runtime parent pages, and the top-level index.

The durable branch value is:
- keeping next-carrier recovery separate from copied-arm visibility
- keeping copied-arm visibility separate from carrier-dependency truth
- keeping carrier-dependency truth separate from the trustworthy successor split
- keeping successor-split truth separate from dispatcher-contract preservation
- making the protected-runtime deobfuscation branch more useful for real repair decisions instead of vague “the split is noisy” narration

## Candidate topic pages to create or improve
Good later candidates if branch balance points back here:
- a narrower deobfuscation follow-up only if repeated real cases show that one trustworthy successor split is already good enough but dispatcher-contract preservation still lies during CFG repair
- a later parent-page sync only if the new copied-code / successor-split seam still feels too leaf-local after more protected-runtime case work lands
- no immediate need for another broad flattening or opaque-predicate taxonomy pass

## Next-step research directions
Near-term preference:
- rotate away again after this run rather than clustering several protected-runtime / deobfuscation slots back-to-back
- keep favoring thinner practical continuations on underfed branches over dense browser/mobile convenience work
- if deobfuscation is selected again soon, bias toward a still-thin contract-preservation or post-split downstream continuation rather than another copied-code-only pass unless repeated real cases justify it

## Concrete scenario notes or actionable tactics added this run
- freeze the last stable next-carrier before arguing about visible branch width
- compare copied / noisy arms at the level of normalized carrier outputs and mapped destinations, not raw pseudocode size
- do not trust a branch family as the real split unless it materially depends on the carrier or its dependency family
- treat constant-wrapper patching as noise reduction first, not automatic split proof
- before CFG repair, keep successor truth and dispatcher-contract truth as separate proof objects

## Search audit
Search sources requested:
- `exa`
- `tavily`
- `grok`

Search sources succeeded:
- `exa`
- `tavily`

Search sources failed:
- `grok`

Exa endpoint:
- via `python3 /root/.openclaw/workspace/skills/search-layer/scripts/search.py --source exa,tavily,grok --queries "Tigress FlattenObfuscateNext FlattenConditionalKinds compute flag AddOpaque documentation" "Binary Ninja control flow flattening unflattening opaque predicates branch fix blog" "angr control flow deobfuscation flatten symbolic execution state variable documentation" "Miasm MODeflattener OLLVM unflatten symbolic execution github" --mode deep --intent exploratory --num 5 --domain-boost tigress.wtf,binary.ninja,docs.angr.io,angr.io,github.com`

Tavily endpoint:
- via `python3 /root/.openclaw/workspace/skills/search-layer/scripts/search.py --source exa,tavily,grok --queries "Tigress FlattenObfuscateNext FlattenConditionalKinds compute flag AddOpaque documentation" "Binary Ninja control flow flattening unflattening opaque predicates branch fix blog" "angr control flow deobfuscation flatten symbolic execution state variable documentation" "Miasm MODeflattener OLLVM unflatten symbolic execution github" --mode deep --intent exploratory --num 5 --domain-boost tigress.wtf,binary.ninja,docs.angr.io,angr.io,github.com`

Grok endpoint:
- via configured completions proxy surfaced in runtime errors: `http://proxy.zhangxuemin.work:8000/v1/chat/completions`

Searches attempted this run:
1. `Tigress FlattenObfuscateNext FlattenConditionalKinds compute flag AddOpaque documentation`
2. `Binary Ninja control flow flattening unflattening opaque predicates branch fix blog`
3. `angr control flow deobfuscation flatten symbolic execution state variable documentation`
4. `Miasm MODeflattener OLLVM unflatten symbolic execution github`

Degraded-source-set note:
- this run still counts as a real external-research pass because all three sources were explicitly requested and Grok was actually invoked
- Grok returned repeated 502 errors through the proxy path
- Exa, Tavily, and the retained documentation/write-up surfaces were sufficient for a conservative bounded extension of the protected-runtime deobfuscation branch

## Commit / archival sync
KB changes from this run were committed in `/root/.openclaw/workspace`, and archival sync was run afterward via `/root/.openclaw/workspace/scripts/sync-reverse-expert-kb.sh`.
If sync had failed, the local KB commit would still have been kept as the primary success condition for this run.

## Best-effort notes
- `.learnings/ERRORS.md` logging remained non-blocking / best-effort only for this run
- an early grep probe used shell backquotes unsafely and hit `/bin/sh` parsing; it was immediately rerun with simpler quoting and did not affect the retained research evidence or KB delta
