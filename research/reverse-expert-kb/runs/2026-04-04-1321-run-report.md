# Reverse expert KB autosync run report — 2026-04-04 13:21 Asia/Shanghai

Mode: external-research-driven
Date: 2026-04-04 13:21 Asia/Shanghai / 2026-04-04 05:21 UTC

## Scope this run
This run deliberately rotated back into a content-bearing protected-runtime / deobfuscation continuation after the previous maintenance slot.

Chosen seam: **TLS callback -> first payload-bearing consumer**.

Why this seam:
- the broad packed/startup page already preserved startup-owned pre-entry truth, but the KB still lacked a thinner continuation for the specific Windows/native case where the remaining ambiguity is already clearly TLS-callback-shaped
- this seam is practical and operator-useful: it prevents stopping at TLS callback presence/replay when the real question is where payload-bearing code actually begins
- it is thin enough to complement the existing packed-stub note instead of duplicating it

Added:
- `topics/tls-callback-to-first-payload-consumer-workflow-note.md`
- `sources/protected-runtime/2026-04-04-tls-callback-first-payload-consumer-notes.md`
- `sources/protected-runtime/2026-04-04-1321-tls-callback-search-layer.txt`
- `runs/2026-04-04-1321-run-report.md`

Updated:
- `topics/protected-runtime-practical-subtree-guide.md`
- `index.md`

## Direction review / branch-balance review
Recent content-bearing runs now cover five thinner practical seams across different branches:
- malware: AppInit DLL consumer-proof
- iOS: keychain item retrieval -> request/signing owner
- protocol: Windows RPC binding auth-info / context-lineage
- native: timer queue / threadpool timer first-consumer
- protected runtime: TLS callback -> first payload-bearing consumer

This is good anti-stagnation behavior and avoids overfeeding browser/mobile-protected-runtime challenge-loop work while still allowing a targeted protected-runtime continuation when the seam is genuinely thin and practical.

## New findings (conservative)
Retained practical ladder:

```text
TLS directory / callback array exists
  != callback replay truth for this run
  != this callback owns the startup work that matters
  != startup-owned callback body has handed off to payload-bearing code
  != first payload-bearing consumer truth
```

Key retained operator consequences:
- PE/TLS structure visibility is startup-surface truth, not payload proof
- one callback replay is still weaker than proving one payload-bearing consumer downstream of that callback
- a callback body can still be dominated by startup normalization / CRT or constructor work rather than ordinary payload behavior

## Sources consulted
Primary references:
- Microsoft Learn — PE Format
  - https://learn.microsoft.com/en-us/windows/win32/debug/pe-format
- Microsoft Learn — `/TLS`
  - https://learn.microsoft.com/en-us/cpp/build/reference/tls?view=msvc-170
- MSDN Magazine archive — Inside Windows: An In-Depth Look into the Win32 Portable Executable File Format, Part 2
  - https://learn.microsoft.com/en-us/archive/msdn-magazine/2002/march/inside-windows-an-in-depth-look-into-the-win32-portable-executable-file-format-part-2

Notes:
- the archived MSDN article was used conservatively as practical PE/TLS startup-reading context, not as the sole basis for modern loader claims

## Concrete operator tactics added
- freeze one callback pointer/slot rather than widening immediately to “all TLS callbacks”
- separate callback presence from callback replay in the run that matters
- explicitly test whether the callback body is still only startup proof rather than payload proof
- stop at one callback -> one handoff -> one first payload-bearing consumer rather than forcing the full bootstrap story

## Search audit
Search sources requested: `exa`, `tavily`, `grok`
Search sources succeeded: `exa`, `tavily`
Search sources failed: `grok` (invoked; returned empty set)

Exa endpoint: via `python3 /root/.openclaw/workspace/skills/search-layer/scripts/search.py --source exa,tavily,grok`
Tavily endpoint: via `python3 /root/.openclaw/workspace/skills/search-layer/scripts/search.py --source exa,tavily,grok`
Grok endpoint: via `python3 /root/.openclaw/workspace/skills/search-layer/scripts/search.py --source exa,tavily,grok`

## Commit / archival sync
Committed locally as:
- `d30c72b` — `kb(protected-runtime): add TLS callback payload-consumer workflow note`

Archival sync attempt:
- ran `/root/.openclaw/workspace/scripts/sync-reverse-expert-kb.sh`
- helper created `kb-sync-temp` from `git subtree split --prefix=research/reverse-expert-kb`
- push to the standalone GitHub repo was rejected as **non-fast-forward**

Result:
- local KB progress was kept intact
- archival sync remained blocked by remote-history divergence rather than KB-content failure

## Best-effort notes
- `.learnings/ERRORS.md` logging remained optional/non-blocking
