# Reverse KB Run Report — 2026-04-08 15:56 Asia/Shanghai

Mode: external-research-driven
Date: 2026-04-08 15:56 Asia/Shanghai / 2026-04-08 07:56 UTC

## Summary
This run deliberately used the external-research slot on the protocol branch, but kept it bounded by tightening the existing parser/state workflow note rather than creating a broad TLS or gRPC framing page.

Chosen seam:
- **decrypted/plaintext transport visibility / framed-message visibility vs first local parser/dispatch consumer truth when protocol material is already readable but behavior ownership is still ambiguous**

Result:
- materially extended `topics/protocol-parser-to-state-edge-localization-workflow-note.md`
- added supporting source notes and saved the search-layer artifact

The practical value added this run is a sharper protocol ladder:
- decrypted/plaintext transport visibility
- framed message visibility
- parse/decode capability
- first local parser/dispatch consumer

That keeps analysts from overreading TLS-decrypted plaintext, HTTP/2/gRPC frame extraction, or successful protobuf/gRPC decode as already-good proof of the first local state/reply/gate owner that actually matters.

## Direction / branch-balance review
Recent cadence before this run:
- 12:56: external iOS AsyncStream consumer-truth refinement
- 13:56: external native Linux watcher record-shaping refinement
- 14:56: external browser CDP interception/body-ownership refinement

That made the protocol branch the right rotation target for this hour, and the framed-plaintext-to-consumer seam was a thinner practical continuation already worth preserving.

Branch-choice reasoning:
- anti-stagnation still favored a real external-research pass
- the protocol branch is established but still benefits from thinner stop-rule refinements rather than broad new syntheses
- the existing parser/state note already had the correct operator shape for this material
- this improved the KB itself instead of just collecting gRPC/TLS notes

## What changed
### Added
- `sources/protocol/2026-04-08-1556-protocol-plaintext-search-layer.txt`
- `sources/protocol/2026-04-08-plaintext-frame-visibility-vs-first-consumer-notes.md`
- `runs/2026-04-08-1556-run-report.md`

### Updated
- `topics/protocol-parser-to-state-edge-localization-workflow-note.md`

## New findings
Primary durable split reinforced this run:

```text
decrypted/plaintext transport visibility
  != framed message visibility
  != parse/decode capability
  != first local parser/dispatch consumer
```

Concrete practical reinforcements preserved in the note:
- decrypted TLS/plaintext HTTP/2 visibility is real transport readability, but weaker than local consumer ownership
- gRPC length-prefixed message extraction is real framed-message truth, but weaker than application consumer truth
- successful protobuf/gRPC parse/decode is real decodability, but weaker than the first consequence-bearing local consumer
- the workflow should step from plaintext/frame visibility into the first local reduction that predicts reply/state behavior

## Sources consulted
Primary source anchors:
- gRPC HTTP/2 protocol documentation
- Protocol Buffers parser / parseFrom documentation surfaces
- Wireshark TLS / HTTP2 documentation surfaces

Search-backed support:
- explicit `search-layer` attempt with `--source exa,tavily,grok`
- Exa and Tavily returned usable gRPC framing, protobuf parser, and Wireshark/TLS surfaces
- Grok was explicitly invoked but returned 502 errors through the configured proxy path
- ancillary discussion surfaces were retained conservatively and not treated as the main authority

## Reflections / synthesis
This was the right shape of protocol external run.
It did **not** create a generic TLS/plaintext or gRPC page.
It tightened one practical continuation inside the existing parser/state workflow note.

The durable branch value is:
- keeping plaintext transport visibility separate from framed-message visibility
- keeping framed-message visibility separate from parse/decode capability
- keeping parse/decode capability separate from the first local parser/dispatch consumer
- making the protocol branch more useful for real consequence localization after transport readability is already solved

## Candidate topic pages to create or improve
Good later protocol candidates if branch balance points back here:
- a narrower continuation when first parser/dispatch consumer truth is already good enough but the remaining liar is later async queue ownership or reply-emission handoff
- a subtree-guide sync only if this framed-plaintext stop rule still feels too leaf-local

## Next-step research directions
Near-term preference:
- rotate away again after this run rather than clustering several protocol external slots in a row
- keep preferring thin practical continuations over broad framing/tooling summaries
- avoid internal-only cleanup unless it blocks a source-backed practical extension

## Concrete scenario notes or actionable tactics added this run
- ask explicitly whether the current proof is only plaintext visibility, only framed-message extraction, only parse/decode capability, or the first local consequence-bearing consumer
- do not stop at Wireshark-readable plaintext, extracted gRPC message bytes, or one successful protobuf decode by default
- step to the first local reduction after framing/decompression/parse when the real question is reply/state ownership

## Search audit
Search sources requested:
- `exa`
- `tavily`
- `grok`

Search sources succeeded:
- `exa`
- `tavily`

Search sources failed:
- `grok`

Exa endpoint:
- via `python3 /root/.openclaw/workspace/skills/search-layer/scripts/search.py --source exa,tavily,grok --mode deep --intent exploratory --num 5 --queries ...`

Tavily endpoint:
- via `python3 /root/.openclaw/workspace/skills/search-layer/scripts/search.py --source exa,tavily,grok --mode deep --intent exploratory --num 5 --queries ...`

Grok endpoint:
- via configured completions proxy surfaced in runtime errors: `http://proxy.zhangxuemin.work:8000/v1/chat/completions`

Searches attempted this run:
1. `gRPC HTTP/2 frame data message compression length-prefixed docs`
2. `Wireshark TLS decryption HTTP2 plaintext frames docs`
3. `protobuf parseFrom framed message stream first consumer docs`

Degraded-source-set note:
- this run still counts as a real external-research pass because all three sources were explicitly requested and Grok was actually invoked
- Grok returned repeated 502 errors through the proxy path
- Exa, Tavily, and the retained official protocol surfaces were sufficient for a bounded extension of the existing protocol parser/state note

## Commit / archival sync
If diffs remain after report write:
- commit only KB paths from `/root/.openclaw/workspace`
- then run `/root/.openclaw/workspace/scripts/sync-reverse-expert-kb.sh`
- if archival sync fails, preserve the local commit and record the failure conservatively rather than treating it as KB-content failure

## Best-effort notes
- reverse-kb-autosync skill path lookup after the sync did not return a stable direct hit from the workspace search, but the workflow contract remained clear from prior runs and the existing run pattern, so execution continued without needing confirmation
- `.learnings/ERRORS.md` logging remained non-blocking and was not needed for this run
