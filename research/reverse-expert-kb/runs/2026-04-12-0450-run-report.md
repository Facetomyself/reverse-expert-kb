# Reverse KB Run Report — 2026-04-12 04:50 Asia/Shanghai

Mode: external-research-driven
Date: 2026-04-12 04:50 Asia/Shanghai / 2026-04-11 20:50 UTC

## Summary
This run deliberately used the external-research slot on a thinner protected-runtime seam, but avoided broad anti-instrumentation or mobile-challenge expansion.

Chosen seam:
- **once one lower-surface boundary already exists, proving whether it actually reduced uncertainty more than one richer user-space posture would have**

Result:
- added a new bounded workflow note: `topics/protected-runtime-lower-surface-vs-user-space-uncertainty-reduction-workflow-note.md`
- added fresh supporting source notes and saved the explicit tri-source search artifact
- synchronized the protected-runtime observation-topology parent note, the protected-runtime subtree guide, the protected-runtime parent page, and the top-level index

The practical value added this run is a sharper compare ladder:
- same trigger compared
- visibility improved
- question answered
- distortion reduced
- semantic carry-forward preserved
- smaller next target recovered

That keeps analysts from overreading earlier Gadget posture, richer Stalker / DBI visibility, or one lower-surface event hit as if any of those alone already proved the right working model.

## Direction / branch-balance review
Recent external cadence before this run had already rotated across:
- protocol parser/plaintext vs first-consumer refinement
- malware MSI custom-action execution truth
- iOS XPC reply/error/invalidation/reconnect truth
- native Qt event-filter vs signal-slot consumer truth

This run therefore returned to the protected-runtime branch, but on a bounded compare-heavy seam instead of another browser/mobile convenience pass or another internal-only sync.

Branch-choice reasoning:
- anti-stagnation still required a real explicit `exa,tavily,grok` attempt in this run
- the protected-runtime branch already had topology-selection and lower-surface notes, but the compare-heavy continuation between them was still only a candidate in the top-level index
- the resulting delta is a concrete workflow continuation, not another broad tooling-comparison page
- this improves branch balance without feeding browser anti-bot or mobile challenge-loop density again

Current balance read after this run:
- browser/mobile remain strong and easy to overfeed
- recent runs rotated sanely across protocol, malware, iOS, native, and now protected-runtime
- the protected-runtime observation branch is healthier because topology selection no longer has to carry the whole “lower surface vs richer user-space” decision burden by implication alone
- the thinner protocol timeout/cancel-cleanup continuation remains the most honest top-level candidate if the next external slot should avoid protected-runtime again

## What changed
### Added
- `sources/protected-runtime/2026-04-12-0450-lower-surface-vs-user-space-search-layer.txt`
- `sources/protected-runtime/2026-04-12-lower-surface-vs-user-space-uncertainty-reduction-notes.md`
- `topics/protected-runtime-lower-surface-vs-user-space-uncertainty-reduction-workflow-note.md`
- `runs/2026-04-12-0450-run-report.md`

### Updated
- `topics/protected-runtime-observation-topology-selection-workflow-note.md`
- `topics/protected-runtime-practical-subtree-guide.md`
- `topics/anti-tamper-and-protected-runtime-analysis.md`
- `index.md`

## Scope this run
Bounded protected-runtime compare-heavy continuation only:
- no broad anti-instrumentation taxonomy expansion
- no generic Frida-vs-DBI-vs-eBPF tooling page
- no browser/mobile detour
- no internal-only wording/index repair pass

The run stayed focused on one operator question:
- when one lower-surface boundary already exists and one richer user-space posture is still available, which one actually reduces the decisive uncertainty more honestly, and does the lower surface preserve enough semantic meaning to replace or only pair with the richer slice?

## New findings
Primary durable split reinforced this run:

```text
same trigger compared
  != lower surface reduced uncertainty more
  != lower surface preserved enough semantic meaning
  != richer user-space alternative is unnecessary
  != smaller next target recovered
```

A second compact split reinforced this run:

```text
visibility improved
  != question answered
  != distortion reduced
  != semantic carry-forward preserved
  != next target recovered
```

Concrete practical reinforcements preserved in the KB:
- Frida Gadget can improve posture and timing by loading earlier, but earlier in-process reach is still not automatic truth improvement
- Frida Gadget configuration can also change capability assumptions; on jailed iOS, `code_signing=required` disables the Interceptor API
- Frida Stalker and DBI-style tooling can dramatically improve visibility, but still carry cost/distortion questions and are not automatically the best answer to a boundary-truth question
- Linux uprobe and seccomp documentation give narrower boundary-truth objects, but those narrower objects can also lose semantic carry-forward if the analyst forgets to compare what next target they actually yield
- “pair both surfaces” is a first-class outcome: keep the lower surface for boundary truth and keep the richer user-space slice for owner/consumer semantics when needed

## Sources consulted
Primary retained source anchors:
- Frida documentation — `Gadget`
- Frida documentation — `Stalker`
- DynamoRIO documentation — `Code Manipulation API`
- DynamoRIO documentation — `Profiling DynamoRIO and Clients`
- Linux kernel documentation — `Uprobe-based Event Tracing`
- Linux kernel documentation — `Seccomp BPF (SECure COMPuting with filters)`

Search-backed support:
- explicit `search-layer` attempt with `--source exa,tavily,grok`
- Exa and Tavily returned usable official Frida, DynamoRIO, and Linux-kernel documentation surfaces
- Grok was explicitly invoked but failed with repeated 502 errors through the configured proxy path

## Reflections / synthesis
This was the right shape of protected-runtime external run.
It did **not** create another broad anti-instrumentation page.
It also did **not** stop at lightly tweaking the parent observation-topology note again.

Instead it promoted a still-open top-level candidate into a concrete workflow continuation and synchronized that continuation into the parent note, the subtree guide, the protected-runtime parent page, and the top-level index.

The durable branch value is:
- keeping improved visibility separate from actual uncertainty reduction
- keeping lower-surface truth separate from semantic carry-forward
- keeping “lower surface wins” separate from the equally real outcome “pair both surfaces”
- making the branch more honest about when a quieter lower boundary has actually earned the right to become the main working model

## Candidate topic pages to create or improve
Good later candidates if branch balance points elsewhere next:
- a narrower protocol timeout/cancel-cleanup continuation when current-owner realism is already mostly solved but cleanup/liveness still lies
- a later protected-runtime continuation only if repeated real cases show that lower-surface boundary truth is already good enough and the main remaining liar is the first upward semantic handoff into one reducer / consumer / owner path
- a subtree-guide or parent-page sync only if the new compare-heavy note still feels too leaf-local after more protected-runtime case work lands

## Next-step research directions
Near-term preference:
- rotate away again after this run rather than clustering protected-runtime work back-to-back
- keep favoring thinner practical continuations on underfed branches over dense browser/mobile convenience work
- if protected-runtime is selected again soon, bias toward a different thin seam or an upward handoff continuation rather than more observation-topology wording

## Concrete scenario notes or actionable tactics added this run
- compare the **same trigger** before deciding that one topology is better
- ask explicitly whether the new move improved only visibility, actually answered the question, actually reduced distortion, actually preserved semantic carry-forward, or already yielded one smaller next target
- allow **keep / pair / reject** as the three honest outcomes for lower-surface comparison work
- do not assume earlier Gadget posture means the same user-space capabilities still exist
- do not assume Stalker / DBI richness beats a lower-surface boundary when the live question is boundary truth rather than route detail
- do not assume one lower-surface event hit is enough if it yields no credible upward handoff

## Search audit
Search sources requested:
- `exa`
- `tavily`
- `grok`

Search sources succeeded:
- `exa`
- `tavily`

Search sources failed:
- `grok`

Exa endpoint:
- via `python3 /root/.openclaw/workspace/skills/search-layer/scripts/search.py --source exa,tavily,grok --queries ... --mode deep --intent exploratory --num 5 --domain-boost frida.re,dynamorio.org,docs.kernel.org,kernel.org`

Tavily endpoint:
- via `python3 /root/.openclaw/workspace/skills/search-layer/scripts/search.py --source exa,tavily,grok --queries ... --mode deep --intent exploratory --num 5 --domain-boost frida.re,dynamorio.org,docs.kernel.org,kernel.org`

Grok endpoint:
- via configured completions proxy surfaced in runtime errors: `http://proxy.zhangxuemin.work:8000/v1/chat/completions`

Searches attempted this run:
1. `Frida Gadget Interceptor Stalker early instrumentation user space instrumentation official docs`
2. `DynamoRIO instrumentation code cache overhead client interface official docs`
3. `Linux uprobe tracing userspace function tracing kernel documentation`
4. `Linux seccomp filter documentation official`

Degraded-source-set note:
- this run still counts as a real external-research pass because all three sources were explicitly requested and Grok was actually invoked
- Grok returned repeated 502 errors through the proxy path
- Exa, Tavily, and the retained official documentation surfaces were sufficient for a conservative bounded extension of the protected-runtime observation branch

## Commit / archival sync
KB changes from this run were committed in `/root/.openclaw/workspace`, and archival sync was run afterward via `/root/.openclaw/workspace/scripts/sync-reverse-expert-kb.sh`.
If sync had failed, the local KB commit would still have been kept as the primary success condition for this run.

## Best-effort notes
- `.learnings/ERRORS.md` logging remained non-blocking / best-effort only for this run
- an initial multi-line `search.py` invocation with redirect was rejected by OpenClaw exec preflight, so the search was rerun as a direct `python3` invocation and the artifact was written separately; this did not block the KB run or change the retained search evidence
