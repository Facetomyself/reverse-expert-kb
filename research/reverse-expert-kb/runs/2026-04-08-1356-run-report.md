# Reverse KB Run Report — 2026-04-08 13:56 Asia/Shanghai

Mode: external-research-driven
Date: 2026-04-08 13:56 Asia/Shanghai / 2026-04-08 05:56 UTC

## Summary
This run deliberately used the external-research slot on the native Linux watcher branch, tightening an existing workflow note rather than creating a broad filesystem-notification taxonomy page.

Chosen seam:
- **watcher registration / returned-record truth vs first event-owned consumer truth when inotify/fanotify already returns real records but the later consumer is still ambiguous**

Result:
- materially extended `topics/native-inotify-fanotify-first-event-consumer-workflow-note.md`
- added supporting source notes and saved the search-layer artifact

The practical value added this run is a sharper Linux watcher ladder:
- watch registration
- returned event record
- coalesced/paired/overflow-shaped or permission-shaped watcher surface
- first event-owned consumer

That keeps analysts from overreading watch registration, one returned record, or fanotify metadata arrival as already-good proof of the behavior-bearing consumer they actually care about.

## Direction / branch-balance review
Recent cadence before this run:
- 10:56: external macOS persistence launch-truth refinement
- 11:56: external watch-scope causal-selection refinement
- 12:56: external iOS AsyncStream consumer-truth refinement

That left the native branch as a good practical target for this hour, and the Linux watcher seam was a thinner bounded continuation already worth preserving.

Branch-choice reasoning:
- anti-stagnation still favored a real external-research pass
- the existing native inotify/fanotify note already had the right place for a practical source-backed refinement
- this improved branch balance while staying concrete and case-driven rather than widening into generic watcher coverage

## What changed
### Added
- `sources/native/2026-04-08-1356-inotify-search-layer.txt`
- `sources/native/2026-04-08-inotify-fanotify-record-shaping-notes.md`
- `runs/2026-04-08-1356-run-report.md`

### Updated
- `topics/native-inotify-fanotify-first-event-consumer-workflow-note.md`

## New findings
Primary durable split reinforced this run:

```text
watch registration
  != returned event record
  != coalesced/paired/overflow-shaped or permission-shaped watcher surface
  != first event-owned consumer
```

Concrete practical reinforcements preserved in the note:
- returned inotify records may already be shaped by coalescing, rename-cookie pairing, or overflow semantics
- fanotify metadata arrival is weaker than the allow/deny decision owner in permission-event cases
- returned watcher records are often reduction boundaries, not yet the first truthful owner of the later effect
- the workflow should stop only when one event-owned consumer predicts later behavior strongly enough

## Sources consulted
Primary source anchors:
- man7 / Linux inotify documentation surfaces
- man7 / Linux fanotify documentation surfaces

Search-backed support:
- explicit `search-layer` attempt with `--source exa,tavily,grok`
- Exa and Tavily returned usable man7/Linux watcher surfaces
- Grok was explicitly invoked but returned 502 errors through the configured proxy path
- kernel-list discussion surfaces were retained only conservatively as framing support

## Reflections / synthesis
This was the right shape of native external run.
It did **not** create a broad filesystem-notification page.
It tightened one practical continuation inside the existing Linux watcher workflow note.

The durable branch value is:
- keeping watch registration separate from returned-record truth
- keeping returned-record truth separate from record-shaping semantics
- keeping watcher-surface truth separate from the first event-owned consumer
- making the native Linux watcher branch more useful for real consequence localization

## Candidate topic pages to create or improve
Good later native candidates if branch balance points back here:
- a narrower continuation when the first event-owned consumer is already good enough but the remaining liar is queue reuse or event-owner reincarnation across bursts
- a parent/subtree sync only if the sharpened watcher ladder still feels too leaf-local

## Next-step research directions
Near-term preference:
- rotate again after this run rather than clustering several external slots inside native watcher continuations
- continue preferring thin practical continuations over broad subsystem summaries
- avoid internal-only wording/index maintenance unless it blocks a practical source-backed addition

## Concrete scenario notes or actionable tactics added this run
- ask explicitly whether the current proof is only registration, only a returned record, only a shaped watcher surface, or the first event-owned consumer
- do not stop at one returned event record by default
- in fanotify permission cases, freeze the allow/deny response owner rather than metadata arrival alone

## Search audit
Search sources requested:
- `exa`
- `tavily`
- `grok`

Search sources succeeded:
- `exa`
- `tavily`

Search sources failed:
- `grok`

Exa endpoint:
- via `python3 /root/.openclaw/workspace/skills/search-layer/scripts/search.py --source exa,tavily,grok --mode deep --intent exploratory --num 5 --queries ...`

Tavily endpoint:
- via `python3 /root/.openclaw/workspace/skills/search-layer/scripts/search.py --source exa,tavily,grok --mode deep --intent exploratory --num 5 --queries ...`

Grok endpoint:
- via configured completions proxy surfaced in runtime errors: `http://proxy.zhangxuemin.work:8000/v1/chat/completions`

Searches attempted this run:
1. `inotify coalesced events rename cookie IN_Q_OVERFLOW man7`
2. `fanotify permission event response decision consumer man7`
3. `inotify read event record coalescing returned record not full history docs`

Degraded-source-set note:
- this run still counts as a real external-research pass because all three sources were explicitly requested and Grok was actually invoked
- Grok returned repeated 502 errors through the proxy path
- Exa, Tavily, and the retained man7/Linux watcher surfaces were sufficient for a bounded extension of the existing native watcher note

## Commit / archival sync
If diffs remain after report write:
- commit only KB paths from `/root/.openclaw/workspace`
- then run `/root/.openclaw/workspace/scripts/sync-reverse-expert-kb.sh`
- if archival sync fails, preserve the local commit and record the failure conservatively rather than treating it as KB-content failure

## Best-effort notes
- `.learnings/ERRORS.md` remains non-blocking / best-effort only
