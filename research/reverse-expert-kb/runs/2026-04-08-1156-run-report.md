# Reverse KB Run Report — 2026-04-08 11:56 Asia/Shanghai

Mode: external-research-driven
Date: 2026-04-08 11:56 Asia/Shanghai / 2026-04-08 03:56 UTC

## Summary
This run deliberately used the external-research slot on the runtime-evidence branch, tightening an existing compare-run workflow note rather than creating a broad debugger-tooling page.

Chosen seam:
- **watchpoint/query scope truth vs actual causal object selection when a real hit is visible but the watched scope may still be broader than the consequence-bearing object**

Result:
- materially extended `topics/compare-run-design-and-divergence-isolation-workflow-note.md`
- added supporting source notes and saved the search-layer artifact

The practical value added this run is a sharper runtime-evidence ladder:
- watchable
- real hit
- causally selected object
- consequence-bearing reduction

That keeps analysts from overreading a real watchpoint/data-breakpoint hit as already-good causal proof when the selected object/range is still too broad.

## Direction / branch-balance review
Recent cadence before this run:
- 08:52: external WebSocket framed-consumer refinement
- 09:52: external IRQ/deferred consequence refinement
- 10:56: external macOS persistence launch-truth refinement

That left runtime-evidence as a good practical target for this hour.

Branch-choice reasoning:
- anti-stagnation still favored a real external-research pass
- the runtime-evidence branch already had the right existing compare-run note for a bounded refinement
- this improved branch balance while staying practical and case-driven rather than widening into generic debugger coverage

## What changed
### Added
- `sources/runtime-evidence/2026-04-08-1156-watch-scope-search-layer.txt`
- `sources/runtime-evidence/2026-04-08-watchpoint-scope-vs-causal-object-notes.md`
- `runs/2026-04-08-1156-run-report.md`

### Updated
- `topics/compare-run-design-and-divergence-isolation-workflow-note.md`

## New findings
Primary durable split reinforced this run:

```text
watchable
  != real hit
  != causally selected object
  != consequence-bearing reduction
```

Concrete practical reinforcements preserved in the note:
- debugger-supported watchpoints/data breakpoints help answer a bounded access question, but do not choose the right semantic object for you
- a real hit can still belong to a wrapper/range/object scope that is truthful but not explanatory for the consequence you care about
- watched-object/query scope is part of correctness, not just tooling efficiency
- the workflow should stop only when one narrower field/slot/reducer output predicts later behavior strongly enough

## Sources consulted
Primary source anchors:
- GDB / ROCGDB watchpoint documentation surfaces
- WinDbg data-breakpoint documentation surfaces

Search-backed support:
- explicit `search-layer` attempt with `--source exa,tavily,grok`
- Exa and Tavily returned usable debugger/watchpoint surfaces
- Grok was explicitly invoked but returned 502 errors through the configured proxy path
- rr / replay-query workflow material was retained conservatively as support for operator framing

## Reflections / synthesis
This was the right shape of runtime-evidence external run.
It did **not** create a broad debugger-tooling page.
It tightened one practical compare-run continuation inside the existing workflow note.

The durable branch value is:
- keeping watchability separate from real-hit truth
- keeping real-hit truth separate from correct causal-object selection
- keeping object-scope correctness visible as part of the workflow rather than as mere tooling efficiency
- making the runtime-evidence branch more operator-useful for causal narrowing

## Candidate topic pages to create or improve
Good later runtime-evidence candidates if branch balance points back here:
- a narrower continuation when the causal object is already chosen but the remaining liar is incarnation/reuse vs same-object persistence across the compare pair
- a parent/subtree sync only if the sharpened watch-scope ladder still feels too leaf-local

## Next-step research directions
Near-term preference:
- rotate again after this run rather than clustering several external slots inside runtime-evidence
- continue preferring thin practical continuations over broad tooling summaries
- avoid internal-only wording/index maintenance unless it blocks a practical source-backed addition

## Concrete scenario notes or actionable tactics added this run
- ask explicitly whether the current result is only watchability, only a real hit, correct causal-object selection, or a consequence-bearing reduction
- do not stop at the first real watchpoint/data-breakpoint hit by default
- shrink watched scope before narrating causality when the watched object is still aggregate or wrapper-shaped

## Search audit
Search sources requested:
- `exa`
- `tavily`
- `grok`

Search sources succeeded:
- `exa`
- `tavily`

Search sources failed:
- `grok`

Exa endpoint:
- via `python3 /root/.openclaw/workspace/skills/search-layer/scripts/search.py --source exa,tavily,grok --mode deep --intent exploratory --num 5 --queries ...`

Tavily endpoint:
- via `python3 /root/.openclaw/workspace/skills/search-layer/scripts/search.py --source exa,tavily,grok --mode deep --intent exploratory --num 5 --queries ...`

Grok endpoint:
- via configured completions proxy surfaced in runtime errors: `http://proxy.zhangxuemin.work:8000/v1/chat/completions`

Searches attempted this run:
1. `rr watchpoints hardware watchpoint limitations false positives object scope docs`
2. `GDB watchpoint masked watchpoints scope data breakpoint docs`
3. `WinDbg data breakpoint watchpoint object selection false positives docs`

Degraded-source-set note:
- this run still counts as a real external-research pass because all three sources were explicitly requested and Grok was actually invoked
- Grok returned repeated 502 errors through the proxy path
- Exa, Tavily, and the retained debugger/watchpoint surfaces were sufficient for a bounded extension of the existing compare-run note

## Commit / archival sync
If diffs remain after report write:
- commit only KB paths from `/root/.openclaw/workspace`
- then run `/root/.openclaw/workspace/scripts/sync-reverse-expert-kb.sh`
- if archival sync fails, preserve the local commit and record the failure conservatively rather than treating it as KB-content failure

## Best-effort notes
- `.learnings/ERRORS.md` remains non-blocking / best-effort only
