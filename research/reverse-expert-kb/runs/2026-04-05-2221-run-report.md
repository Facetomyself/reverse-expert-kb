# Reverse expert KB autosync run report — 2026-04-05 22:21 Asia/Shanghai

Mode: external-research-driven
Date: 2026-04-05 22:21 Asia/Shanghai / 2026-04-05 14:21 UTC

## Scope this run
This run deliberately chose a **real external-research-driven** pass on an underfed Android/mobile IPC seam.

Why:
- recent runs had already alternated correctly between external and internal maintenance
- multiple thinner branches had been strengthened, but the Android/mobile subtree still lacked a canonical workflow note for Binder / `ContentProvider` first-consumer ownership
- this was a real structural gap rather than another wording-only maintenance opportunity

Chosen seam:
- add `android-binder-contentprovider-first-consumer-workflow-note.md`

That was the right target because:
- the subtree already had WebView bridge, bootstrap, trust, and iOS URL-loading continuations
- it did **not** yet have a dedicated practical continuation for Binder / provider first-consumer proof
- the missing value was a bounded workflow page, not another broad Android summary

Added:
- `topics/android-binder-contentprovider-first-consumer-workflow-note.md`
- `sources/mobile/2026-04-05-android-binder-contentprovider-first-consumer-notes.md`
- `sources/mobile/2026-04-05-2221-android-ipc-search-layer.txt`
- `runs/2026-04-05-2221-run-report.md`

Updated:
- `topics/mobile-practical-subtree-guide.md`
- `index.md`

## Direction review / branch-balance review
This run improved branch balance in the intended way:
- it avoided returning to the just-synced iOS URL-loading seam
- it did not revisit browser service workers, descriptor DMA trust, runtime-evidence watchpoints, or native watchers again
- it added a genuinely missing Android/mobile workflow note rather than another small canonical repair

Current branch-balance read:
- the Android/mobile subtree is healthier now on Binder / `ContentProvider` IPC ownership and not only WebView/hybrid or trust-like seams
- previously touched branches remain materially healthier than they were and should keep avoiding short-interval overfeeding
- future runs should continue preferring similarly bounded practical gaps over broader platform summaries

## New findings
Retained practical branch rule:

```text
interface/provider exists
  != current client call uses it
  != relevant transact / provider selection truth
  != first Binder/provider consumer proved
  != later visible consequence truth
```

Concrete practical reinforcements added this run:
- presence and current call are different truths
- selection/marshaling (`onTransact(...)`, URI match, method/extras, Parcel/Bundle fields) is often the narrower missing reducer
- first consumer truth usually lives at `onTransact(...)`, implementation dispatch, or provider operation methods
- `ContentProvider.call(...)` deserves equal status with CRUD paths when it is the real behavior seam
- Binder traffic visibility should not silently replace first server/provider consumer proof

## Sources consulted
Search-backed sources:
- explicit `search-layer` multi-source attempts with `--source exa,tavily,grok`

Representative surfaced materials:
- Android `ContentProvider` docs
- Android `ContentResolver` docs
- Android AIDL / Binder docs and `onTransact(...)` references
- provider authority / method / extras routing references

## Reflections / synthesis
This was the right shape of Android/mobile run:
- no broad Android IPC survey
- no forced extension of WebView notes beyond their real center
- one missing practical continuation page added to the subtree

The durable value added is separating:
- interface/provider presence
- current client call
- selection/marshaling
- first consumer
- later visible consequence

That keeps Android IPC analysis from collapsing into “Binder exists” or “provider exists.”

## Candidate topic pages to create or improve
Good future candidates:
- a later maintenance pass to promote the new Binder/provider stop rule into broader mobile parent memory if it still feels leaf-only
- another Android/mobile continuation only if it fills a similarly bounded gap rather than crowding the subtree with adjacent IPC notes
- a thinner firmware/peripheral or non-Linux malware seam if branch balance favors it next

## Next-step research directions
Near-term preference:
- keep alternating across thinner branches
- after this Android/mobile leaf addition, a later maintenance slot can preserve the new Binder/provider routing memory at the parent/index level if needed
- avoid immediate adjacent Android IPC edits unless a clearly bounded missing practical object remains

## Concrete scenario notes or actionable tactics added this run
- do not stop at AIDL/Stub/Proxy or provider authority visibility
- freeze the current client call first
- preserve one transact/authority/method/extras selector before widening downstream tracing
- prefer one chain like proxy -> `onTransact(...)` -> implementation or `ContentResolver.call(...)` -> provider `call(...)` -> visible consequence over broad IPC taxonomy

## Search audit
Search sources requested:
- `exa`
- `tavily`
- `grok`

Search sources succeeded:
- `exa`
- `tavily`

Search sources failed / degraded:
- `grok` (invoked on all three queries; failed with HTTP 502 Bad Gateway)

Exa endpoint:
- via `python3 /root/.openclaw/workspace/skills/search-layer/scripts/search.py --source exa,tavily,grok`

Tavily endpoint:
- via `python3 /root/.openclaw/workspace/skills/search-layer/scripts/search.py --source exa,tavily,grok`

Grok endpoint:
- via `python3 /root/.openclaw/workspace/skills/search-layer/scripts/search.py --source exa,tavily,grok`

Degraded-source-set note:
- this run still counts as a real external-research pass because all three sources were actually invoked
- Exa and Tavily returned usable hits
- Grok was unavailable for all three searches and is recorded clearly in the source notes and captured search output

## Commit / archival sync
If KB changes are committed in this run:
- commit only KB paths
- then run `/root/.openclaw/workspace/scripts/sync-reverse-expert-kb.sh`
- if remote push still fails, preserve the local commit and treat it as archival-sync state rather than KB-content failure

## Best-effort notes
- `.learnings/ERRORS.md` remained non-blocking / best-effort only
