# Reverse KB Run Report — 2026-04-07 12:21 Asia/Shanghai

Mode: external-research-driven
Date: 2026-04-07 12:21 Asia/Shanghai / 2026-04-07 04:21 UTC

## Summary
This run deliberately used the external-research slot on a thinner iOS service-handoff seam instead of returning to browser or malware work.

Chosen seam:
- **Apple XPC lifecycle realism: interruption / invalidation / reconnection vs same-service consequence**

Result:
- materially extended `topics/ios-xpc-proxy-to-service-consumer-workflow-note.md`
- added a supporting source note and saved the explicit `search-layer` artifact

The practical value added this run is a sharper operator ladder for iOS XPC-shaped work:
- client connection/proxy exists
- listener accepted connection
- exported-object method entry
- interruption/invalidation/reconnection truth for this path
- later same-service consequence or later consumer truth

That keeps analysts from overreading proxy visibility, connection health, or reconnection as already-good same-service consequence proof.

## Direction / branch-balance review
Recent cadence:
- 10:21: external native wait/timer delivery refinement
- 11:21: KB-internal malware WMI fired-consumer repair

That meant this hour should return to a real external-research pass rather than becoming another internal-maintenance slot.

Branch-choice reasoning:
- browser and malware had already received recent attention
- iOS practical work remains healthier than before, but still benefits from bounded service-handoff refinements with high operator value
- the XPC lifecycle/reconnection seam was a practical, underfed refinement target where official docs could sharpen an existing note without taxonomy drift

This run therefore improved branch balance in the intended way:
- real multi-source search attempted
- no browser/malware clustering by inertia
- one thinner iOS practical seam materially improved

## What changed
### Added
- `sources/ios/2026-04-07-ios-xpc-lifecycle-notes.md`
- `sources/ios/2026-04-07-1221-ios-xpc-lifecycle-search-layer.txt`
- `runs/2026-04-07-1221-run-report.md`

### Updated
- `topics/ios-xpc-proxy-to-service-consumer-workflow-note.md`

## New findings
Primary durable split reinforced this run:

```text
client connection/proxy exists
  != listener accepted connection
  != exported-object method entry
  != interruption/invalidation/reconnection truth for this path
  != later same-service consequence or later consumer truth
```

Concrete practical reinforcements preserved in the note:
- listener acceptance is weaker than exported-object method entry
- interruption is weaker than invalidation as an end-of-life boundary
- a reconnected proxy/connection is weaker than proving the same meaningful method path ran again
- method entry is still weaker than one later consequence that answers the analyst’s real question

## Sources consulted
Primary source anchors:
- Apple: `NSXPCConnection`
  - https://developer.apple.com/documentation/foundation/nsxpcconnection
- Apple: `NSXPCListener`
  - https://developer.apple.com/documentation/foundation/nsxpclistener
- Apple: `NSXPCListenerDelegate`
  - https://developer.apple.com/documentation/foundation/nsxpclistenerdelegate

Search-backed support:
- explicit `search-layer` attempt with `--source exa,tavily,grok`
- Exa and Tavily returned usable surfaced material
- Grok was invoked but again failed through the configured proxy path

## Reflections / synthesis
This was the right shape of iOS external run.
It did **not** create a broad Apple-services taxonomy page.
It tightened one practical lifecycle proof boundary inside the existing XPC note.

The durable branch value is:
- keeping XPC service-handoff analysis honest about lifecycle state
- preventing overclaim from proxy visibility, connection health, or reconnection alone
- improving the handoff from XPC route discovery into reply/error/reconnect or later consequence work

## Candidate topic pages to create or improve
Good later iOS candidates if branch balance points back here:
- a narrower XPC reply/error/reconnect compare continuation when service method entry is already good enough but same-request completion truth still lies
- a parent-page sync only if the new lifecycle memory still feels too leaf-local

## Next-step research directions
Near-term preference:
- keep alternating across underfed branches rather than spending several consecutive external runs inside one family
- if iOS returns soon, prefer similarly bounded proof-discipline refinements over broad family expansion

## Concrete scenario notes or actionable tactics added this run
- do not stop at proxy/connection existence alone
- do not stop at listener acceptance alone
- decide explicitly whether the missing proof is method entry, lifecycle breakage, reconnection realism, or later service-owned consequence
- keep interruption distinct from invalidation when reasoning about whether the same path is still alive

## Search audit
Search sources requested:
- `exa`
- `tavily`
- `grok`

Search sources succeeded:
- `exa`
- `tavily`

Search sources failed:
- `grok`

Exa endpoint:
- via `python3 /root/.openclaw/workspace/skills/search-layer/scripts/search.py --source exa,tavily,grok`

Tavily endpoint:
- via `python3 /root/.openclaw/workspace/skills/search-layer/scripts/search.py --source exa,tavily,grok`

Grok endpoint:
- via configured completions proxy surfaced in runtime errors: `http://proxy.zhangxuemin.work:8000/v1/chat/completions`

Searches attempted this run:
1. `NSXPCConnection interruption invalidation resume reconnect exported object official docs`
2. `Apple NSXPCConnection interruption invalidation exported object official documentation`
3. `NSXPCListener interruption invalidation connection service consumer Apple docs`

Degraded-source-set note:
- this run still counts as a real external-research pass because all three sources were explicitly requested and Grok was actually invoked
- Grok returned no usable results because of repeated proxy-side 502 failures
- Exa, Tavily, and primary Apple docs were sufficient for a conservative practical extension of the existing iOS note

## Commit / archival sync
If diffs remain after report write:
- commit only KB paths from `/root/.openclaw/workspace`
- then run `/root/.openclaw/workspace/scripts/sync-reverse-expert-kb.sh`
- if archival sync still fails because remote `main` moved ahead, preserve the local commit and record the sync-state failure conservatively rather than treating it as KB-content failure

## Best-effort notes
- `.learnings/ERRORS.md` remains best-effort only and should not block this run
