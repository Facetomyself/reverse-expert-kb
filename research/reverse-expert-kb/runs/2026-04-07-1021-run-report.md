# Reverse KB Run Report — 2026-04-07 10:21 Asia/Shanghai

Mode: external-research-driven
Date: 2026-04-07 10:21 Asia/Shanghai / 2026-04-07 02:21 UTC

## Summary
This run deliberately used the external-research slot on a thinner native async seam instead of returning to browser or malware work.

Chosen seam:
- **waitable timer / registered-wait / threadpool-wait delivery realism**

Result:
- materially extended:
  - `topics/native-wait-object-and-threadpool-wait-first-consumer-workflow-note.md`
  - `topics/native-timer-queue-and-threadpool-timer-first-consumer-workflow-note.md`
- added a supporting source note and saved the explicit `search-layer` artifact

The practical value added this run is a sharper operator ladder for native wait/timer cases:
- armed / registered
- object signaled or timer fired
- APC / registered-wait / threadpool-wait callback delivered
- later consequence-bearing consumer

That keeps analysts from overreading registration, association, or raw signal visibility as already-good callback or consumer truth.

## Direction / branch-balance review
Recent cadence:
- 08:21: external protocol RPC registration/reachability refinement
- 09:21: KB-internal browser evidence-calibration repair

That meant this hour should return to a real external-research pass rather than becoming another internal-maintenance slot.

Branch-choice reasoning:
- browser remains easy to overfeed and had just received a canonical repair
- malware had already received several recent runs
- native practical work had not been touched recently and still benefits from thinner async-delivery refinements with high operator value
- waitable timer / registered-wait realism was a bounded seam where official docs could sharpen existing native leaves without taxonomy drift

This run therefore improved branch balance in the intended way:
- real multi-source search attempted
- no browser/malware clustering by inertia
- one thinner native practical seam materially improved

## What changed
### Added
- `sources/native-and-desktop/2026-04-07-native-waitable-timer-and-registered-wait-notes.md`
- `sources/native-and-desktop/2026-04-07-1021-native-waitable-timer-search-layer.txt`
- `runs/2026-04-07-1021-run-report.md`

### Updated
- `topics/native-wait-object-and-threadpool-wait-first-consumer-workflow-note.md`
- `topics/native-timer-queue-and-threadpool-timer-first-consumer-workflow-note.md`

## New findings
Primary durable split reinforced this run:

```text
armed / registered
  != object signaled or timer fired
  != APC / registered-wait / threadpool-wait callback delivered
  != later consequence-bearing consumer
```

Concrete practical reinforcements preserved in the notes:
- wait registration / threadpool-wait association is weaker than actual callback delivery truth
- waitable-timer armed truth is weaker than actual timer-fire truth
- timer-fire/object-signal truth is weaker than APC or callback delivery truth
- callback delivery is still weaker than one later consumer/effect that answers the analyst’s real question

## Sources consulted
Primary source anchors:
- Microsoft Learn: `Waitable Timer Objects`
  - https://learn.microsoft.com/en-us/windows/win32/sync/waitable-timer-objects
- Microsoft Learn: `Using Waitable Timer Objects`
  - https://learn.microsoft.com/en-us/windows/win32/sync/using-waitable-timer-objects
- Microsoft Learn: `RegisterWaitForSingleObject`
  - https://learn.microsoft.com/en-us/windows/win32/api/winbase/nf-winbase-registerwaitforsingleobject
- Microsoft Learn: `SetThreadpoolWait`
  - https://learn.microsoft.com/en-us/windows/win32/api/threadpoolapiset/nf-threadpoolapiset-setthreadpoolwait

Search-backed support:
- explicit `search-layer` attempt with `--source exa,tavily,grok`
- Exa and Tavily returned usable surfaced material
- Grok was invoked but again failed through the configured proxy path

## Reflections / synthesis
This was the right shape of native external run.
It did **not** create a broad Windows synchronization taxonomy page.
It tightened one practical proof boundary shared across the native wait/timer leaves.

The durable branch value is:
- keeping native async-delivery analysis honest about registration vs signal vs delivery vs consumer truth
- preventing overclaim from object association or raw signal visibility alone
- improving the handoff from wait/timer setup analysis into callback/consumer proof

## Candidate topic pages to create or improve
Good later native candidates if branch balance points back here:
- a narrower APC-vs-registered-wait compare continuation when queueing/delivery realism still lies after wait/timer truth is already good enough
- a parent-page sync only if the new wait/timer-delivery memory still feels too leaf-local

## Next-step research directions
Near-term preference:
- keep alternating across underfed branches rather than spending several consecutive external runs inside one family
- if native returns soon, prefer similarly bounded proof-discipline refinements over broad family expansion

## Concrete scenario notes or actionable tactics added this run
- do not stop at wait/threadpool-wait registration alone
- do not stop at timer armed/object signaled truth alone
- decide explicitly whether the missing proof is signal/firing, callback delivery, or later consumer
- keep APC-style completion distinct from ordinary object-signal truth when alertable-wait semantics matter

## Search audit
Search sources requested:
- `exa`
- `tavily`
- `grok`

Search sources succeeded:
- `exa`
- `tavily`

Search sources failed:
- `grok`

Exa endpoint:
- via `python3 /root/.openclaw/workspace/skills/search-layer/scripts/search.py --source exa,tavily,grok`

Tavily endpoint:
- via `python3 /root/.openclaw/workspace/skills/search-layer/scripts/search.py --source exa,tavily,grok`

Grok endpoint:
- via configured completions proxy surfaced in runtime errors: `http://proxy.zhangxuemin.work:8000/v1/chat/completions`

Searches attempted this run:
1. `CreateWaitableTimer SetWaitableTimer RegisterWaitForSingleObject SetThreadpoolWait official docs callback race cancellation Windows`
2. `Windows waitable timer register wait threadpool wait official docs callback delivery cancellation`
3. `SetWaitableTimer APC completion routine waitable timer vs wait object official docs Windows`

Degraded-source-set note:
- this run still counts as a real external-research pass because all three sources were explicitly requested and Grok was actually invoked
- Grok returned no usable results because of repeated proxy-side 502 failures
- Exa, Tavily, and primary Microsoft docs were sufficient for a conservative practical extension of the existing native notes

## Commit / archival sync
If diffs remain after report write:
- commit only KB paths from `/root/.openclaw/workspace`
- then run `/root/.openclaw/workspace/scripts/sync-reverse-expert-kb.sh`
- if archival sync still fails because remote `main` moved ahead, preserve the local commit and record the sync-state failure conservatively rather than treating it as KB-content failure

## Best-effort notes
- `.learnings/ERRORS.md` remains best-effort only and should not block this run
