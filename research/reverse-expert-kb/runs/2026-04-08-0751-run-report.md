# Reverse KB Run Report — 2026-04-08 07:51 Asia/Shanghai

Mode: external-research-driven
Date: 2026-04-08 07:51 Asia/Shanghai / 2026-04-07 23:51 UTC

## Summary
This run deliberately used the external-research slot on the protected-runtime branch, but avoided easy dense-branch expansion by tightening an existing observation-topology note instead of creating another broad anti-instrumentation taxonomy page.

Chosen seam:
- **truth-vs-visibility choice when earlier or richer user-space instrumentation looks attractive, but a lower-surface boundary may still answer the actual question more honestly**

Result:
- materially extended `topics/protected-runtime-observation-topology-selection-workflow-note.md`
- added supporting source notes and saved the search-layer artifact

The practical value added this run is a sharper comparison ladder for observation-topology choice:
- visibility improved
- distortion reduced
- lower-surface truth gained
- useful semantic consequence recovered

That keeps analysts from overreading earlier Frida/Gadget posture or richer DBI visibility as automatic proof that the better topology has been chosen.

## Direction / branch-balance review
Recent cadence before this run:
- 04:51: external iOS XPC lifecycle realism refinement
- 05:51: external deobfuscation encoded-next recovery refinement
- 06:51: external malware send-boundary realism refinement

That already spread recent external work across several practical branches.
This hour returned to the protected-runtime branch, but on a bounded cross-cutting seam that improves operator judgment rather than feeding another dense browser/mobile challenge leaf.

Branch-choice reasoning:
- anti-stagnation still favored a real external-research pass
- protected-runtime remains important, but the run should avoid drifting back into browser/mobile convenience micro-branches
- the observation-topology note already had the right place to preserve a sharper truth-vs-visibility comparison rule
- this therefore improved branch balance while still touching a strategically important strong branch in a disciplined way

## What changed
### Added
- `sources/protected-runtime/2026-04-08-0751-topology-search-layer.txt`
- `sources/protected-runtime/2026-04-08-topology-truth-vs-visibility-notes.md`
- `runs/2026-04-08-0751-run-report.md`

### Updated
- `topics/protected-runtime-observation-topology-selection-workflow-note.md`

## New findings
Primary durable split reinforced this run:

```text
visibility improved
  != distortion reduced
  != lower-surface truth gained
  != useful semantic consequence recovered
```

Concrete practical reinforcements preserved in the note:
- earlier Frida/Gadget posture can improve reach and timing without automatically improving truth
- rich DBI visibility can be the right move for control-structure questions, but still be the wrong move when anti-instrumentation distortion dominates and the real question lives at a lower boundary
- syscall-adjacent / transport-adjacent / lower-surface boundaries can sometimes answer the operator’s real question more directly than richer in-process instrumentation
- topology choice should be judged by boundary truth and reduced distortion, not by tooling comfort alone

## Sources consulted
Primary source anchors:
- Frida documentation surfaces for Gadget / early instrumentation posture
- eBPF / uprobe tracing documentation surfaces for lower-surface tracing
- DynamoRIO / Intel Pin documentation surfaces for DBI visibility and instrumentation posture

Search-backed support:
- explicit `search-layer` attempt with `--source exa,tavily,grok`
- Exa and Tavily returned usable Frida / tracing / DBI documentation surfaces
- Grok was explicitly invoked but returned 502 errors through the configured proxy path

## Reflections / synthesis
This was the right shape of protected-runtime external run.
It did **not** create another broad anti-instrumentation page.
It tightened one practical topology-selection seam inside the existing workflow note.

The durable branch value is:
- keeping visibility gains separate from actual distortion reduction
- keeping richer user-space tooling comfort separate from lower-surface truth gain
- making the branch more explicit about when a lower boundary is the cheaper truthful move
- improving operator judgment instead of only tool inventory

## Candidate topic pages to create or improve
Good later protected-runtime candidates if branch balance points back here:
- a narrower compare-heavy continuation when one lower-surface boundary already exists but the remaining question is whether it preserved enough semantic meaning to replace richer user-space tracing
- a parent/subtree sync only if the sharpened truth-vs-visibility rule still feels too leaf-local

## Next-step research directions
Near-term preference:
- rotate away again after this run rather than deepening protected-runtime continuously
- keep preferring bounded practical continuations over broad family restatements
- avoid internal-only wording/index maintenance unless it blocks a practical source-backed addition

## Concrete scenario notes or actionable tactics added this run
- ask explicitly whether the new move improved only visibility, actually reduced distortion, actually exposed a truer lower boundary, or already recovered a smaller next target
- do not treat earlier Frida/Gadget or richer DBI visibility as automatic success
- when anti-instrumentation churn dominates, consider a cheaper lower-surface boundary before widening in-process tooling further

## Search audit
Search sources requested:
- `exa`
- `tavily`
- `grok`

Search sources succeeded:
- `exa`
- `tavily`

Search sources failed:
- `grok`

Exa endpoint:
- via `python3 /root/.openclaw/workspace/skills/search-layer/scripts/search.py --source exa,tavily,grok --mode deep --intent exploratory --num 5 --queries ...`

Tavily endpoint:
- via `python3 /root/.openclaw/workspace/skills/search-layer/scripts/search.py --source exa,tavily,grok --mode deep --intent exploratory --num 5 --queries ...`

Grok endpoint:
- via configured completions proxy surfaced in runtime errors: `http://proxy.zhangxuemin.work:8000/v1/chat/completions`

Searches attempted this run:
1. `Frida gadget early instrumentation vs attach detection official docs`
2. `eBPF uprobes Android tracing lower overhead syscall boundary official docs`
3. `dynamic binary instrumentation overhead anti instrumentation Intel PIN DynamoRIO official docs`

Degraded-source-set note:
- this run still counts as a real external-research pass because all three sources were explicitly requested and Grok was actually invoked
- Grok returned repeated 502 errors through the proxy path
- Exa, Tavily, and the retained documentation surfaces were sufficient for a bounded extension of the existing topology-selection note

## Commit / archival sync
If diffs remain after report write:
- commit only KB paths from `/root/.openclaw/workspace`
- then run `/root/.openclaw/workspace/scripts/sync-reverse-expert-kb.sh`
- if archival sync fails, preserve the local commit and record the failure conservatively rather than treating it as KB-content failure

## Best-effort notes
- `.learnings/ERRORS.md` remains non-blocking / best-effort only
