# Reverse expert KB autosync run report — 2026-04-05 14:21 Asia/Shanghai

Mode: external-research-driven
Date: 2026-04-05 14:21 Asia/Shanghai / 2026-04-05 06:21 UTC

## Scope this run
This run deliberately chose a **real external-research-driven** pass on a thinner firmware/protocol seam.

Why:
- recent runs had already alternated correctly between external and internal maintenance
- malware/Linux persistence, protocol request-lifetime realism, protected-runtime next-state recovery, runtime-evidence object identity, and native watcher delivery had all received recent targeted work
- the descriptor ownership / completion-visibility seam remained practical and still under-anchored to DMA trust-transfer realism

Chosen seam:
- strengthen `descriptor-ownership-transfer-and-completion-visibility-workflow-note.md`

That was the right target because:
- the page already existed canonically
- the missing value was not another sibling page
- the practical operator risk is still common: treating descriptor bytes or completion bytes as trustworthy before ownership transfer, barrier, or sync semantics actually justify that claim

Updated:
- `topics/descriptor-ownership-transfer-and-completion-visibility-workflow-note.md`
- `sources/protocol-and-network-recovery/2026-04-05-descriptor-ownership-and-dma-trust-notes.md`
- `sources/protocol-and-network-recovery/2026-04-05-1421-descriptor-dma-search-layer.txt`
- `runs/2026-04-05-1421-run-report.md`

## Direction review / branch-balance review
This run improved branch balance in the intended way:
- it avoided returning immediately to the just-synced native watcher branch
- it did not revisit malware/Linux persistence or protected-runtime again
- it strengthened a thinner firmware/protocol continuation through one existing workflow page rather than adding another nearby leaf

Current branch-balance read:
- malware/Linux, protocol request-lifetime, protected-runtime, runtime-evidence, and native watcher delivery are all materially healthier than before
- the descriptor ownership / DMA-trust seam now has stronger practical memory
- future runs should continue preferring narrow mechanism-bearing continuations over broad surveys or duplicate pages

## New findings
Retained practical branch rule:

```text
descriptor or completion bytes visible
  != peer may already trust them
  != ownership transfer is complete
  != durable completion consequence proved
```

Concrete practical reinforcements added this run:
- Linux DMA API material makes CPU/device ownership transfer explicit on streaming / non-coherent paths
- `dma_sync_*for_device()` / `dma_sync_*for_cpu()`-style boundaries are first-class proof objects when trust is not automatic
- memory barriers and observation semantics can separate “publish written” from “peer may now truthfully observe it”
- completion trust can still lag behind DMA-finished or interrupt-visible status
- DMAengine docs reinforce that descriptor ownership/lifetime changes after submit and completion are not conceptual decoration; they constrain what memory/metadata is still safe or meaningful to touch
- virtio material remains useful as a reminder that role labels like avail/used are weaker than a full ownership-transfer and reclaim story

## Sources consulted
Search-backed sources:
- explicit `search-layer` multi-source attempts with `--source exa,tavily,grok`

Representative surfaced materials:
- Linux DMA API HOWTO / core DMA API docs
- Linux DMA Engine API guide
- Linux memory-barriers documentation
- OASIS virtio spec and explanatory virtqueue material
- Red Hat virtqueue explanation

## Reflections / synthesis
This was the right kind of firmware/protocol run:
- no duplicate descriptor/ring sibling page
- no broad DMA survey
- one existing workflow note made more operationally trustworthy

The durable value added is separating:
- descriptor/completion bytes visible
- ownership transfer complete
- peer trust justified
- durable reclaim/consequence proved

The most useful practical additions are the trust-side reminders:
- CPU->device handoff is sometimes the missing proof object
- barrier/observation split matters
- DMA-finished or interrupt-visible is weaker than completion trust
- reclaim/reuse remains the strongest consequence-side proof

## Candidate topic pages to create or improve
Good future candidates:
- a later maintenance pass to promote the new DMA-trust stop rule into protocol/firmware parent/index memory if it still feels leaf-only
- a thinner firmware/peripheral seam around effect-bearing ownership/callback proof if one concrete gap remains
- a non-Linux malware continuation if branch balance swings back there on the next external slot

## Next-step research directions
Near-term preference:
- keep alternating across thinner branches
- after this descriptor-ownership leaf strengthening, a later maintenance slot can promote the new DMA-trust reminder into parent/index routing if needed
- avoid immediate nearby descriptor/ring edits unless a clear missing practical object remains

## Concrete scenario notes or actionable tactics added this run
- do not stop at descriptor or completion bytes being visible in backing memory
- freeze whether CPU->device or device->CPU ownership transfer is actually complete
- preserve the split between publish written, peer observation, and trust justified
- treat DMA-finished / interrupt-visible status as weaker than completion trust when barrier or sync semantics still matter
- prefer reclaim/reuse/wakeup continuity as consequence-side proof once trust is established

## Search audit
Search sources requested:
- `exa`
- `tavily`
- `grok`

Search sources succeeded:
- `exa`
- `tavily`

Search sources failed / degraded:
- `grok` (invoked on all three queries; failed with HTTP 502 Bad Gateway)

Exa endpoint:
- via `python3 /root/.openclaw/workspace/skills/search-layer/scripts/search.py --source exa,tavily,grok`

Tavily endpoint:
- via `python3 /root/.openclaw/workspace/skills/search-layer/scripts/search.py --source exa,tavily,grok`

Grok endpoint:
- via `python3 /root/.openclaw/workspace/skills/search-layer/scripts/search.py --source exa,tavily,grok`

Degraded-source-set note:
- this run still counts as a real external-research pass because all three sources were actually invoked
- Exa and Tavily returned usable hits
- Grok was unavailable for all three searches and is recorded clearly in the source notes and captured search output

## Commit / archival sync
If KB changes are committed in this run:
- commit only KB paths
- then run `/root/.openclaw/workspace/scripts/sync-reverse-expert-kb.sh`
- if remote push still fails, preserve the local commit and treat it as archival-sync state rather than KB-content failure

## Best-effort notes
- `.learnings/ERRORS.md` remained best-effort only
