# Reverse expert KB autosync run report — 2026-04-05 10:21 Asia/Shanghai

Mode: external-research-driven
Date: 2026-04-05 10:21 Asia/Shanghai / 2026-04-05 02:21 UTC

## Scope this run
This run deliberately chose a **real external-research-driven** pass on the thinner runtime-evidence branch.

Why:
- recent runs had already alternated well between external and internal maintenance
- recent external pressure had gone to malware/Linux persistence, protocol pending-request lifetime, and protected-runtime next-state recovery
- runtime-evidence still has fewer practical leaves and high operator value
- the watchpoint/query object-identity stop rule was already present in branch memory, but still benefited from fresher external anchoring

Chosen seam:
- strengthen `first-bad-write-and-decisive-reducer-localization-workflow-note.md`

That was the right target because:
- the canonical page already existed
- the improvement needed was not another sibling page
- the practical value was to reinforce one existing stop rule with debugger/watchpoint-backed reminders

Updated:
- `topics/first-bad-write-and-decisive-reducer-localization-workflow-note.md`
- `sources/runtime-evidence/2026-04-05-watchpoint-object-identity-and-address-drift-notes.md`
- `sources/runtime-evidence/2026-04-05-1021-watchpoint-identity-search-layer.txt`
- `runs/2026-04-05-1021-run-report.md`

## Direction review / branch-balance review
This run improved branch balance in the intended way:
- it avoided returning to malware/Linux, protocol, or protected-runtime immediately
- it gave the smaller runtime-evidence branch a source-backed practical reinforcement
- it kept the work case-driven and narrow instead of adding another abstract runtime page

Current branch-balance read:
- malware/Linux, protocol, and protected-runtime all received meaningful recent attention and should avoid immediate overfeeding
- runtime-evidence remains smaller and benefits from precise practical stop rules
- native/iOS remain substantial branches already, but future external work should still keep looking for thinner seams rather than dense-branch polishing

## New findings
Retained practical branch rule:

```text
same address
  != same object
  != same consequence-bearing incarnation
```

Concrete practical reinforcements added this run:
- GDB/watchpoint material reinforces that ordinary watchpoints are usually expression/location-oriented first, not semantic-object-lifetime-oriented first
- dynamic-memory / compound-object discussions are especially useful because they make rebinding/reallocation drift explicit
- rr issue material is useful mainly as a tooling-boundary reminder: reverse-watchpoint behavior limits should not be silently promoted into semantic conclusions
- low-level hardware-watchpoint precision still does not remove the need for stronger object-identity anchors when storage is reused or rebound

## Sources consulted
Search-backed sources:
- explicit `search-layer` multi-source attempts with `--source exa,tavily,grok`

Representative surfaced materials:
- GDB `Set Watchpoints` documentation
- GDB/Sourceware and Stack Overflow discussion around fixed-address watchpoints and dynamic memory
- rr issue/discussion material on reverse-watchpoint behavior
- Memfault hardware watchpoint explainer

## Reflections / synthesis
This was the right kind of runtime-evidence run:
- no broad runtime theory page
- no duplicate watchpoint sibling page
- one existing workflow note made more operationally grounded

The durable value added is a sharper practical reminder that:
- a watchpoint hit is often a **location fact** first
- the analyst still has to prove object identity / incarnation truth before narrating that hit as the first bad write for the real consequence-bearing object

## Candidate topic pages to create or improve
Good future candidates:
- a native or firmware seam if one thinner ownership/callback/effect bridge is still underfed
- a runtime-evidence maintenance pass later to promote this object-identity reminder into parent/index branch memory if needed
- a non-Linux malware seam if branch balance swings that way on the next external slot

## Next-step research directions
Near-term preference:
- keep alternating across thinner branches
- after this runtime-evidence leaf strengthening, a later maintenance slot can promote the new reminder into runtime parent/index memory if it still feels leaf-only
- avoid immediate nearby edits unless a real routing gap remains

## Concrete scenario notes or actionable tactics added this run
- when using watchpoint- or memory-query-heavy workflows, ask whether the current address is still the same semantic object across the whole backward-search window
- if not, freeze a stronger identity anchor first: owner pointer, container slot, generation counter, allocation/lifetime event, compare-aligned rebinding point, or similar
- treat reverse-watchpoint/tool limitations as tooling limits first, not as automatic semantic disproof
- prefer the first write to the current meaningful incarnation over the earliest write to any historically related storage when copy/move/rebind churn is present

## Search audit
Search sources requested:
- `exa`
- `tavily`
- `grok`

Search sources succeeded:
- `exa`
- `tavily`

Search sources failed / degraded:
- `grok` (invoked on both queries; failed with HTTP 502 Bad Gateway)

Exa endpoint:
- via `python3 /root/.openclaw/workspace/skills/search-layer/scripts/search.py --source exa,tavily,grok`

Tavily endpoint:
- via `python3 /root/.openclaw/workspace/skills/search-layer/scripts/search.py --source exa,tavily,grok`

Grok endpoint:
- via `python3 /root/.openclaw/workspace/skills/search-layer/scripts/search.py --source exa,tavily,grok`

Degraded-source-set note:
- this run still counts as a real external-research pass because all three sources were actually invoked
- Exa and Tavily returned usable hits
- Grok was unavailable for both searches and is recorded clearly in the source notes and captured search output

## Commit / archival sync
If KB changes are committed in this run:
- commit only KB paths
- then run `/root/.openclaw/workspace/scripts/sync-reverse-expert-kb.sh`
- if remote push still fails, preserve the local commit and treat it as archival-sync state rather than KB-content failure

## Best-effort notes
- `.learnings/ERRORS.md` remained best-effort only
