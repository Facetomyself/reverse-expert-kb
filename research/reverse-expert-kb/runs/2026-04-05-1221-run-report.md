# Reverse expert KB autosync run report — 2026-04-05 12:21 Asia/Shanghai

Mode: external-research-driven
Date: 2026-04-05 12:21 Asia/Shanghai / 2026-04-05 04:21 UTC

## Scope this run
This run deliberately chose a **real external-research-driven** pass on a thinner Linux-native seam.

Why:
- recent runs had already alternated correctly between external and internal maintenance
- malware/Linux persistence, protocol, protected-runtime, and runtime-evidence had all received recent targeted work
- the Linux native filesystem-watch branch remained practical and relatively underfed
- the existing inotify/fanotify note still benefited from sharper delivery-side realism rather than broad watcher taxonomy growth

Chosen seam:
- strengthen `native-inotify-fanotify-first-event-consumer-workflow-note.md`

That was the right target because:
- the page already existed canonically
- the real missing value was source-backed delivery semantics, not another sibling page
- the practical operator problem is easy to overread: “watcher exists” or “event seen” is still weaker than first consumer truth

Updated:
- `topics/native-inotify-fanotify-first-event-consumer-workflow-note.md`
- `sources/native/2026-04-05-native-inotify-fanotify-delivery-and-overflow-notes.md`
- `sources/native/2026-04-05-1221-native-watchfs-search-layer.txt`
- `runs/2026-04-05-1221-run-report.md`

## Direction review / branch-balance review
This run improved branch balance in the intended way:
- it avoided immediately revisiting just-synced runtime-evidence language
- it did not return to malware/Linux persistence, protocol, or protected-runtime again
- it strengthened a thinner native/Linux watcher continuation through one existing workflow page rather than adding another adjacent leaf

Current branch-balance read:
- malware/Linux persistence, protocol, protected-runtime, and runtime-evidence are all materially healthier than before
- the native watcher seam benefited from a concrete source-backed reinforcement this hour
- future runs should continue preferring narrow mechanism-bearing continuations over dense-branch polishing or abstract summaries

## New findings
Retained practical branch rule:

```text
watch registration
  != relevant event occurred
  != returned event record fully explains history
  != first event-owned consumer proved
```

Concrete practical reinforcements added this run:
- inotify returned records may be coalesced, so one record is weaker than one full underlying operation history
- rename handling should preserve `cookie`-paired `IN_MOVED_FROM` / `IN_MOVED_TO` logic rather than stopping at one move-related record
- `IN_Q_OVERFLOW` is behavior-bearing and should not be flattened into harmless logging noise
- for fanotify permission classes, metadata arrival is weaker than the first allow/deny response path that actually decides access
- fanotify object identity may be delivered via FD or FID-style report forms, so the first useful consumer may sit at identity-resolution or permission-response logic above raw event receipt

## Sources consulted
Search-backed sources:
- explicit `search-layer` multi-source attempts with `--source exa,tavily,grok`

Normative/local sources:
- local `man 7 inotify`
- local `man 7 fanotify`

Representative surfaced materials:
- `inotify(7)` and `fanotify(7)` / `fanotify_init(2)` / `fanotify_mark(2)` references
- watcher/adapter issues around rename association and coalescing-related quirks
- Red Hat support material illustrating blocking permission-response behavior in fanotify

## Reflections / synthesis
This was the right kind of native/Linux watcher run:
- no duplicate watcher sibling page
- no broad event-framework survey
- one existing workflow page made more operationally realistic

The durable value added is separating:
- watch registration
- returned record receipt
- event-history interpretation
- first consequence-bearing consumer truth

The most useful practical additions are the delivery-shaping reminders:
- coalescing
- rename cookies
- overflow
- permission-response boundary

## Candidate topic pages to create or improve
Good future candidates:
- a firmware/peripheral or native ownership/effect seam if it yields one similarly bounded workflow object
- a maintenance pass later to promote the new watcher-delivery stop rule into native parent/index memory if needed
- a non-Linux malware seam if branch balance swings back there on the next external slot

## Next-step research directions
Near-term preference:
- keep alternating across thinner branches
- after this native leaf strengthening, a later maintenance slot can promote the new watcher-delivery reminder into parent/index routing if it still feels leaf-only
- avoid immediate adjacent watcher edits unless a clear missing practical object remains

## Concrete scenario notes or actionable tactics added this run
- do not stop at one inotify record when coalescing could already have compressed visible history
- preserve rename-cookie pairing when move semantics matter
- treat `IN_Q_OVERFLOW` as potentially behavior-bearing, especially when the consumer has explicit rebuild/degrade logic
- in fanotify permission flows, prove the first allow/deny response path rather than stopping at metadata arrival
- keep object identity / report-format resolution separate from raw event receipt in fanotify FID-heavy cases

## Search audit
Search sources requested:
- `exa`
- `tavily`
- `grok`

Search sources succeeded:
- `exa`
- `tavily`

Search sources failed / degraded:
- `grok` (invoked on both queries; failed with HTTP 502 Bad Gateway)

Exa endpoint:
- via `python3 /root/.openclaw/workspace/skills/search-layer/scripts/search.py --source exa,tavily,grok`

Tavily endpoint:
- via `python3 /root/.openclaw/workspace/skills/search-layer/scripts/search.py --source exa,tavily,grok`

Grok endpoint:
- via `python3 /root/.openclaw/workspace/skills/search-layer/scripts/search.py --source exa,tavily,grok`

Degraded-source-set note:
- this run still counts as a real external-research pass because all three sources were actually invoked
- Exa and Tavily returned usable hits
- Grok was unavailable for both searches and is recorded clearly in the source notes and captured search output

## Commit / archival sync
If KB changes are committed in this run:
- commit only KB paths
- then run `/root/.openclaw/workspace/scripts/sync-reverse-expert-kb.sh`
- if remote push still fails, preserve the local commit and treat it as archival-sync state rather than KB-content failure

## Best-effort notes
- `.learnings/ERRORS.md` remained best-effort only
