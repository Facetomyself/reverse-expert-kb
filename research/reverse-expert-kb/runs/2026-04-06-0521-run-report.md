# Reverse KB Run Report — 2026-04-06 05:21 Asia/Shanghai

Mode: KB-internal-maintenance

## Summary
This run did **not** force another external-research pass because the previous hour already satisfied the rolling 6-hour requirement with a real `search-layer --source exa,tavily,grok` attempt.

Instead, this run spent the hour on a concrete canonical-integrity repair in the malware subtree: the subtree guide routed readers to a **Linux XDG autostart desktop-entry consumer-proof** continuation, but the actual canonical leaf page was missing.

This was a real operator-facing gap, not just wording polish:
- navigation advertised a thinner persistence continuation that did not exist
- branch memory for XDG desktop-session persistence stop rules was therefore implied but not preserved in a dedicated note
- future runs risked repeating the same drift unless the leaf, guide, and index were repaired together

## Direction / branch-balance review
The branch-balance question this hour was not “what external topic can be searched next?”
It was “what practical branch-integrity issue most deserves repair now that an external-research run already landed last hour?”

Reasoning:
- anti-stagnation was already satisfied by the 04:21 run
- another immediate external run would have been weaker than repairing a genuine canonical mismatch
- the malware subtree had just gained stronger Linux/macOS persistence continuity, which made the missing XDG leaf stand out more sharply

So the right choice was **KB-internal maintenance with practical operator value**, not another thinly-justified external search.

## What changed
### Added
- `topics/malware-xdg-autostart-desktop-entry-consumer-proof-workflow-note.md`

### Updated
- `topics/malware-practical-subtree-guide.md`
- `index.md`

### Synced
- mirrored the same repairs into the reverse workspace copy under `/root/.openclaw/workspace-reverse/research/reverse-expert-kb/`

## Practical synthesis added
The new XDG autostart leaf preserves the smaller ladder:

```text
autostart entry exists
  != effective entry after same-name override/suppression
  != eligible desktop-session entry in the run that matters
  != launchable command path
  != actual session-start execution
  != later malware-owned effect
```

Key practical stop rules now preserved canonically:
- visible `.desktop` entry != effective same-name winner
- effective entry != eligible in the actual session
- `Exec=` text != actual launchability or session-start launch
- `TryExec` present != `TryExec` satisfied
- manual execution of the target != natural desktop-session autostart proof

## Why this run matters
This was not cosmetic cleanup.
It repaired a real branch-routing defect in a practical malware persistence seam and made the branch internally honest again:
- subtree guide now points to a real leaf
- index now lists and summarizes the XDG leaf explicitly
- future operators can land on a dedicated XDG persistence note instead of inferring missing branch memory from neighboring pages

## Search audit
No external search was needed or performed in this run.

Requested sources:
- none

Succeeded:
- none

Failed:
- none

Reason:
- the previous 04:21 run already satisfied the rolling 6-hour explicit multi-source-search requirement
- this hour’s highest-value work was canonical synchronization / branch-balance repair, not another immediate research pass

## Commit / archival sync
This run should:
- commit KB changes from the actual Git repo root (`/root/.openclaw/workspace`)
- run `/root/.openclaw/workspace/scripts/sync-reverse-expert-kb.sh` after commit

Best-effort only:
- `.learnings/ERRORS.md` logging if needed
