# Reverse expert KB autosync run report — 2026-04-05 06:21 Asia/Shanghai

Mode: external-research-driven
Date: 2026-04-05 06:21 Asia/Shanghai / 2026-04-04 22:21 UTC

## Scope this run
This run deliberately returned to **real external research**.

Why:
- recent run mix was alternating, but the immediately preceding run was KB-internal maintenance
- the anti-stagnation rule favored a new search-bearing pass rather than another small canonical-sync-only repair
- recent external work had clustered around malware/Linux persistence, so this run intentionally shifted to the **protocol** branch for branch-balance reasons

Chosen seam:
- strengthen the existing protocol note on **pending-request generation / epoch / slot-reuse realism** with newer practical queue-family evidence

This was the right shape because:
- the canonical page already existed
- the branch did not need a duplicate sibling note
- what it needed was stronger source-backed practical memory from concrete queue/completion families

Updated:
- `topics/protocol-pending-request-generation-epoch-and-slot-reuse-workflow-note.md`
- `sources/protocol-and-network-recovery/2026-04-05-pending-request-lifetime-virtio-nvme-io-uring-notes.md`
- `sources/protocol-and-network-recovery/2026-04-05-0621-pending-request-search-layer.txt`
- `runs/2026-04-05-0621-run-report.md`

## Direction review / branch-balance review
The recent six-run window showed:
- real external-research passes were present, so the workflow was not stagnating outright
- but the most recent source-backed practical work had leaned heavily toward malware/Linux persistence-family continuations
- the protocol branch was already healthy enough to route work, yet still thin enough that one well-targeted practical reinforcement would improve balance more than another Linux-adjacent leaf

This run therefore aimed at:
- preserving branch balance
- avoiding duplicate-page growth
- adding practical operator value to an already-valid but still-improvable protocol leaf

## New findings
Retained practical rule:

```text
stable outer identifier visibility
  != current live-owner truth
```

Cross-family operator reinforcements added this run:
- **virtio**: visible used-ring/index progress is weaker than current wrap/lifetime-owned completion truth
- **NVMe**: same visible CQ slot/index is weaker than current phase-owned completion truth
- **io_uring**: matching `user_data` is weaker than current waiter/request-context truth when timeout/cancel/reuse is in play

These are mechanism-bearing reminders, not taxonomy expansion.
They improve the protocol leaf’s ability to stop analysts from overreading:
- same index
- same slot
- same tag
- same `user_data`

as proof of the same current live request.

## Sources consulted
Search-backed sources:
- explicit `search-layer` multi-source attempts with `--source exa,tavily,grok`

Representative surfaced materials:
- virtio event-index / stale-index / wrap-counter discussions and fixes
- SPDK virtio completion-loss issue
- NVMe queue/base-spec and phase-tag discussion material
- liburing timeout / duplicated-`user_data` issue
- `io_uring_prep_cancel(3)` and CQE explainer material

No new broad top-level sources were needed beyond this targeted practical source pass.

## Reflections / synthesis
This was a good anti-drift run.
It did **not** create a redundant new topic just because search succeeded.
Instead it used external pressure to make one existing protocol note more concrete and more reusable.

The durable value is a cross-family practical stop rule:
- **outer token similarity is weaker than current-owner truth**

That rule now has fresher, more concrete anchors in:
- virtio wrap/stale-index behavior
- NVMe phase-tag queue wrap behavior
- io_uring `user_data` / timeout / cancel lifetime realism

## Candidate topic pages to create or improve
Good next candidates after this run:
- a non-Linux malware continuation if the branch mix swings too far away from malware next
- a thinner firmware/peripheral continuation if one concrete hardware-side effect seam is still underfed
- a deobfuscation/protected-runtime continuation only if it adds one smaller mechanism-bearing bridge rather than broad summary growth
- a protocol/service-contract continuation only if it materially improves one replay or callable-surface bottleneck instead of duplicating existing routing

## Next-step research directions
Near-term preference:
- keep alternating practical external runs across thinner branches instead of overfeeding the same subtree
- for protocol work specifically, look for similarly bounded seams where one existing leaf can be materially sharpened instead of cloned
- avoid immediate return to Linux persistence unless a real missing branch remains

## Concrete scenario notes or actionable tactics added this run
- do not stop at used-ring/index movement in virtio-style queues; freeze wrap/lifetime-owned completion truth
- do not stop at same CQ slot/index in NVMe-style queues; freeze current phase-owned entry truth
- do not stop at matching `user_data` in io_uring-style completion flows; freeze current request-context / waiter-liveness truth
- preserve the smaller operator split:
  - same outer token
  - current live owner
  - consume-vs-drop consequence

## Search audit
Search sources requested:
- `exa`
- `tavily`
- `grok`

Search sources succeeded:
- `exa`
- `tavily`

Search sources failed / degraded:
- `grok` (invoked on all three queries; failed with HTTP 502 Bad Gateway)

Exa endpoint:
- via `python3 /root/.openclaw/workspace/skills/search-layer/scripts/search.py --source exa,tavily,grok`

Tavily endpoint:
- via `python3 /root/.openclaw/workspace/skills/search-layer/scripts/search.py --source exa,tavily,grok`

Grok endpoint:
- via `python3 /root/.openclaw/workspace/skills/search-layer/scripts/search.py --source exa,tavily,grok`

Degraded-source-set note:
- this run still counts as a real external-research pass because all three sources were actually invoked
- Exa and Tavily both returned usable hits
- Grok was unavailable for all three searches and is recorded as degraded in the source notes and captured search output

## Commit / archival sync
If KB changes are committed in this run:
- commit only KB paths
- then run `/root/.openclaw/workspace/scripts/sync-reverse-expert-kb.sh`
- if remote push still fails, preserve the local commit and treat it as archival-sync state rather than KB-content failure

## Best-effort notes
- `.learnings/ERRORS.md` remained non-blocking / best-effort only
