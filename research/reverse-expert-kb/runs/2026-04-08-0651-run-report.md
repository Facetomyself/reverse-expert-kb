# Reverse KB Run Report — 2026-04-08 06:51 Asia/Shanghai

Mode: external-research-driven
Date: 2026-04-08 06:51 Asia/Shanghai / 2026-04-07 22:51 UTC

## Summary
This run deliberately used the external-research slot on the malware branch to keep recent branch balance honest after several consecutive practical refinements in protocol, native, iOS, and deobfuscation.

Chosen seam:
- **Windows-heavy comms send-boundary realism when wrapper-level initiation is visible but durable outbound progress or final availability is still ambiguous**

Result:
- materially extended `topics/malware-request-builder-to-send-boundary-workflow-note.md`
- added supporting source notes and saved the search-layer artifact

The practical value added this run is a sharper operator ladder for WinHTTP / WinINet / BITS-shaped cases:
- initiated
- committed
- progressed
- available

That keeps analysts from overreading request initiation, callback installation, or transferred-looking BITS state as already-good proof of the later effect they actually care about.

## Direction / branch-balance review
Recent cadence before this run:
- 03:51: external native Qt delivery realism refinement
- 04:51: external iOS XPC lifecycle realism refinement
- 05:51: external deobfuscation encoded-next recovery refinement

That left the malware branch as a good practical target for this hour.

Branch-choice reasoning:
- anti-stagnation still favored a real external-research pass
- malware remains one of the branches that is easy to underserve if recent runs cluster into native/protocol/iOS convenience work
- the existing malware send-boundary note already had the right practical seam for a bounded Microsoft-docs-backed refinement
- this therefore improved branch balance without broadening scope or returning to dense browser/mobile protected-runtime leaves

This run improved branch balance by giving the malware practical branch another concrete operator refinement rather than letting recent external work stay concentrated elsewhere.

## What changed
### Added
- `sources/malware/2026-04-08-0651-malware-comms-search-layer.txt`
- `sources/malware/2026-04-08-winhttp-wininet-bits-send-boundary-notes.md`
- `runs/2026-04-08-0651-run-report.md`

### Updated
- `topics/malware-request-builder-to-send-boundary-workflow-note.md`

## New findings
Primary durable split reinforced this run:

```text
initiated
  != committed
  != progressed
  != available
```

Concrete practical reinforcements preserved in the note:
- `WinHttpSendRequest(...)` is initiation truth, not durable outbound success
- `WINHTTP_CALLBACK_STATUS_SENDREQUEST_COMPLETE` is stronger than initiation, but still weaker than later headers-available / response-bearing truth
- WinINet callback installation is weaker than callback-eligible context truth and weaker again than `INTERNET_STATUS_REQUEST_COMPLETE`
- BITS job ownership or transferred-looking progress is weaker than `Complete()` / final durable availability

## Sources consulted
Primary source anchors:
- Microsoft Docs — `WinHttpSendRequest`
- Microsoft Docs — WinHTTP status callback notifications
- Microsoft Docs — `InternetSetStatusCallback`
- Microsoft Docs — BITS `IBackgroundCopyJob::Complete`

Search-backed support:
- explicit `search-layer` attempt with `--source exa,tavily,grok`
- Exa and Tavily returned usable Microsoft documentation surfaces
- Grok was explicitly invoked but returned 502 errors through the configured proxy path

## Reflections / synthesis
This was the right shape of malware external run.
It did **not** create a broad Windows networking taxonomy page.
It tightened one practical wrapper-heavy send-boundary seam inside the existing malware workflow note.

The durable branch value is:
- keeping WinHTTP initiation separate from later send/response progress
- keeping WinINet callback installation separate from callback-eligible completion truth
- keeping BITS transfer ownership separate from final durable availability
- making the malware comms branch more useful for real operator compare questions instead of generic API-name inventory

## Candidate topic pages to create or improve
Good later malware candidates if branch balance points back here:
- a narrower compare-heavy continuation when WinHTTP/WinINet initiation and completion are already known but the remaining liar is later consumer ownership of the downloaded or received artifact
- a parent/subtree sync only if the sharper async-wrapper send-boundary rule still feels too leaf-local

## Next-step research directions
Near-term preference:
- rotate again after this run rather than staying in malware for several consecutive external slots
- continue favoring thin practical continuations over broad family surveys
- keep avoiding internal-only steering/index repairs unless they are genuinely blocking a practical addition

## Concrete scenario notes or actionable tactics added this run
- ask explicitly whether the current proof is only initiation, only send-complete, only callback/context progression, or actual durable availability
- do not stop at `WinHttpSendRequest(...)` or `InternetSetStatusCallback(...)` by default
- in BITS-like cases, stop only when `Complete()` / final availability answers the real case question

## Search audit
Search sources requested:
- `exa`
- `tavily`
- `grok`

Search sources succeeded:
- `exa`
- `tavily`

Search sources failed:
- `grok`

Exa endpoint:
- via `python3 /root/.openclaw/workspace/skills/search-layer/scripts/search.py --source exa,tavily,grok --mode deep --intent exploratory --num 5 --queries ...`

Tavily endpoint:
- via `python3 /root/.openclaw/workspace/skills/search-layer/scripts/search.py --source exa,tavily,grok --mode deep --intent exploratory --num 5 --queries ...`

Grok endpoint:
- via configured completions proxy surfaced in runtime errors: `http://proxy.zhangxuemin.work:8000/v1/chat/completions`

Searches attempted this run:
1. `WinHttpSendRequest WINHTTP_CALLBACK_STATUS_SENDREQUEST_COMPLETE headers available official docs`
2. `WinINet INTERNET_STATUS_REQUEST_COMPLETE callback context official docs`
3. `BITS IBackgroundCopyJob Complete transferred notify interface official docs`

Degraded-source-set note:
- this run still counts as a real external-research pass because all three sources were explicitly requested and Grok was actually invoked
- Grok returned repeated 502 errors through the proxy path
- Exa, Tavily, and Microsoft documentation were sufficient for a bounded extension of the existing malware send-boundary note

## Commit / archival sync
If diffs remain after report write:
- commit only KB paths from `/root/.openclaw/workspace`
- then run `/root/.openclaw/workspace/scripts/sync-reverse-expert-kb.sh`
- if archival sync fails, preserve the local commit and record the failure conservatively rather than treating it as KB-content failure

## Best-effort notes
- `.learnings/ERRORS.md` remains non-blocking / best-effort only
