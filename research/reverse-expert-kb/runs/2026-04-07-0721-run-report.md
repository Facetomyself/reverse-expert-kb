# Reverse KB Run Report — 2026-04-07 07:21 Asia/Shanghai

Mode: KB-internal-maintenance
Date: 2026-04-07 07:21 Asia/Shanghai / 2026-04-06 23:21 UTC

## Summary
This run did **not** force another external-research pass because the previous 06:21 run already satisfied the rolling requirement with a real `search-layer --source exa,tavily,grok` attempt.

Instead, this hour spent itself on a bounded canonical-integrity repair in the browser subtree:
- the browser runtime subtree guide still advertised `cdp-side-effect-and-automation-signal-analysis.md`
- but the canonical leaf did not exist

That was a real operator-facing navigation gap, not a cosmetic wording issue.
The practical branch memory already implied a seam between:
- analyst-induced CDP/DevTools side effects
- target-observable automation signals
- later classification/consequence

But without the leaf, that seam remained implicit and easy to flatten into vague “CDP is detectable” folklore.

## Direction / branch-balance review
Recent cadence:
- 06:21: external-research-driven iOS PAC callback triage refinement

Because the previous hour already used the external slot, this hour could legitimately spend itself on a bounded canonical repair.

Branch-choice reasoning:
- browser is easy to overfeed, so it was not the right branch for another external expansion by default
- however, browser also had the only currently visible broken guide reference across the subtree guides
- fixing that broken reference had direct operator value and improved canonical honesty more than another small index-only cleanup would

So this hour was correctly spent as **KB-internal-maintenance with practical operator value**.

## What changed
### Added
- `topics/cdp-side-effect-and-automation-signal-analysis.md`
- `runs/2026-04-07-0721-run-report.md`

### Updated
- `index.md`

## New findings
The new browser leaf preserves the smaller ladder:

```text
instrumentation attached
  != analyst-visible side effect introduced
  != target-observed automation signal proved
  != automation-classification decision proved
  != later challenge / token / state consequence proved
```

Key practical stop rules now preserved canonically:
- **attached != observable != classified != consequence**
- DevTools-open state is not automatically the same proof object as target-observed debugger semantics
- CDP domain enablement is not automatically the same proof object as a meaningful automation signal the page/challenge consumes
- analyst-induced breakage/timing distortion should be kept separate from genuine target-side automation detection

## Why this run matters
This was not just wording repair.
It converted a broken guide-advertised browser seam into a real practical continuation page and improved the top-level index so the branch memory is now easier to navigate honestly.

That improves the KB in three ways:
- browser subtree routing is no longer broken at this seam
- a recurring practical anti-bot/debugger-analysis question now has a canonical landing page
- future external browser runs can build on a real workflow note instead of re-explaining the seam ad hoc

## Candidate topic pages to create or improve
No equally urgent broken-ref browser subtree gap is visible after this repair.
Future browser work should therefore be justified by bounded practical value, not branch inertia.

## Next-step research directions
Near-term preference:
- after this browser repair, future external slots should continue biasing toward thinner underfed branches rather than easy browser growth by default
- if browser work returns later, it should be because a concrete practical continuation needs research, not because browser is simply dense and convenient

## Concrete scenario notes or actionable tactics added this run
- do not flatten DevTools/CDP attachment, target-observable automation signal, and later challenge consequence into one claim
- freeze one instrumentation delta and one consequence question first
- prefer one compare-friendly instrumentation delta over broad anti-bot folklore
- keep analyst-induced browser breakage separate from genuine target-consumed automation signals

## Search audit
No external search was needed or performed in this run.

Requested sources:
- none

Succeeded:
- none

Failed:
- none

Reason:
- the previous 06:21 run already satisfied the rolling explicit multi-source-search requirement
- this hour’s highest-value work was a bounded canonical repair for a broken browser subtree leaf reference, not another immediate search pass

## Commit / archival sync
If diffs remain after report write:
- commit only KB paths from `/root/.openclaw/workspace`
- then run `/root/.openclaw/workspace/scripts/sync-reverse-expert-kb.sh`
- if archival sync fails again because remote `main` moved ahead, preserve the local commit and record the failure conservatively rather than treating it as KB-content failure

## Best-effort notes
- `.learnings/ERRORS.md` remains non-blocking / best-effort only
