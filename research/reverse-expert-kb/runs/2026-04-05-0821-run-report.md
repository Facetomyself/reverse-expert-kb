# Reverse expert KB autosync run report — 2026-04-05 08:21 Asia/Shanghai

Mode: external-research-driven
Date: 2026-04-05 08:21 Asia/Shanghai / 2026-04-05 00:21 UTC

## Scope this run
This run deliberately chose a **real external-research-driven** pass on a thinner deobfuscation/protected-runtime seam.

Why:
- recent runs had already alternated properly between external and internal maintenance
- malware/Linux persistence and protocol pending-request lifetime had both received recent source-backed attention
- this hour benefited more from strengthening an existing but still-thin deobfuscation bridge than from adding another adjacent malware or protocol leaf

Chosen seam:
- strengthen `opaque-predicate-and-computed-next-state-recovery-workflow-note.md`

That was the right target because:
- the page already existed and was structurally useful
- what it still needed was fresher practical anchoring from transform docs and operator tooling patterns
- creating a sibling page would have duplicated scope instead of improving the existing bridge

Updated:
- `topics/opaque-predicate-and-computed-next-state-recovery-workflow-note.md`
- `sources/protected-runtime/2026-04-05-opaque-next-state-recovery-notes.md`
- `sources/protected-runtime/2026-04-05-0821-opaque-next-state-search-layer.txt`
- `runs/2026-04-05-0821-run-report.md`

## Direction review / branch-balance review
This run improved branch balance well:
- it avoided returning immediately to malware/Linux persistence
- it avoided another protocol maintenance pass directly after the protocol parent/index sync
- it strengthened the deobfuscation/protected-runtime practical branch through one existing workflow page rather than dense-branch polish

Current branch-balance read:
- malware/Linux is much healthier than before and should continue to avoid short-interval overfeeding
- protocol has recently received both external pressure and canonical sync
- deobfuscation/protected-runtime still benefits from thin, practical operator bridges like this one
- future runs should continue preferring small mechanism-bearing additions over broad summary expansion

## New findings
Retained practical branch rule:

```text
dispatcher recognized
  != next-state carrier recovered
  != trustworthy successor relation
  != full deobfuscation required
```

Concrete practical reinforcements added this run:
- Tigress docs make dispatch shape and next-variable obfuscation explicit separate knobs
- encoded-branch / obfuscated-next variants should still be treated as next-state recovery problems, not automatically as a reason to widen into total deobfuscation
- Binary Ninja-oriented workflows repeatedly support recovering one state mapping or one successor relation first, then patching or prettifying later
- opaque-predicate cleanup is useful mainly as noise reduction when the real missing object is still one trustworthy successor relation
- Miasm / OLLVM deflattening references reinforce symbolic execution or local emulation as narrow successor extractors rather than all-or-nothing whole-function solvers

## Sources consulted
Search-backed sources:
- explicit `search-layer` multi-source attempts with `--source exa,tavily,grok`

Representative surfaced materials:
- Tigress flatten / opaque / encode-branches documentation
- Binary Ninja opaque predicate removal
- Binary Ninja control-flow unflattening and branch-obfuscation workflow writeups
- MODeflattener / OLLVM unflattening references

## Reflections / synthesis
This was the right shape of external run:
- no duplicate sibling page
- no abstract survey writing
- one existing workflow page made more practical and more operator-grounded

The durable value added is a sharper reminder that the next useful unit in these cases is often:
- one normalized next-state carrier
- one trustworthy successor relation

not a full victory over every opaque predicate or every CFG artifact.

## Candidate topic pages to create or improve
Good future candidates:
- a thin non-Linux malware continuation if that branch becomes the best next external target
- a firmware/peripheral practical continuation if a concrete effect-bearing hardware seam is still underfed
- a native baseline continuation only if it adds one smaller mechanism-bearing ownership proof rather than general loader description
- another deobfuscation continuation only if it fills a distinct bridge and not a near-duplicate of the page improved here

## Next-step research directions
Near-term preference:
- keep alternating across thinner branches
- after this run, a later maintenance slot can promote the new next-state-recovery memory into parent/index routing if needed
- avoid immediate further edits to this same page unless a clear missing practical object remains

## Concrete scenario notes or actionable tactics added this run
- when Tigress-like transforms expose dispatch shape plus next-obfuscation separately, do not confuse dispatcher discovery with next-state recovery completion
- use opaque-predicate cleanup primarily to lower noise around the successor question
- if one state mapping or one successor edge is already enough to unlock CFG repair or downstream proof, stop there before demanding a perfect decompilation
- treat symbolic execution / emulation as a narrow extractor for one relation, not a requirement to solve the whole flattened function in one pass

## Search audit
Search sources requested:
- `exa`
- `tavily`
- `grok`

Search sources succeeded:
- `exa`
- `tavily`

Search sources failed / degraded:
- `grok` (invoked on all three queries; failed with HTTP 502 Bad Gateway)

Exa endpoint:
- via `python3 /root/.openclaw/workspace/skills/search-layer/scripts/search.py --source exa,tavily,grok`

Tavily endpoint:
- via `python3 /root/.openclaw/workspace/skills/search-layer/scripts/search.py --source exa,tavily,grok`

Grok endpoint:
- via `python3 /root/.openclaw/workspace/skills/search-layer/scripts/search.py --source exa,tavily,grok`

Degraded-source-set note:
- this run still counts as a real external-research pass because all three sources were actually invoked
- Exa and Tavily returned usable hits
- Grok was unavailable for all three searches and is recorded clearly in the source notes and captured search output

## Commit / archival sync
If KB changes are committed in this run:
- commit only KB paths
- then run `/root/.openclaw/workspace/scripts/sync-reverse-expert-kb.sh`
- if remote push still fails, preserve the local commit and treat it as archival-sync state rather than KB-content failure

## Best-effort notes
- `.learnings/ERRORS.md` remained best-effort only
