# Reverse KB Run Report — 2026-04-07 14:21 Asia/Shanghai

Mode: external-research-driven
Date: 2026-04-07 14:21 Asia/Shanghai / 2026-04-07 06:21 UTC

## Summary
This run deliberately used the external-research slot on a thinner protocol async seam instead of returning to browser or malware work.

Chosen seam:
- **pending-request current-owner realism under timeout/cancel/epoch/slot reuse**

Result:
- materially extended `topics/protocol-pending-request-generation-epoch-and-slot-reuse-workflow-note.md`
- added a supporting source note and saved the explicit `search-layer` artifact

The practical value added this run is a sharper operator ladder for async protocol systems:
- visible correlation token matches
- request is still the current live owner
- timeout/cancel/retire/epoch shift left ownership unchanged
- late completion still belongs to the same meaningful path
- later consequence truth

That keeps analysts from overreading superficial token matches as already-good current-owner proof.

## Direction / branch-balance review
Recent cadence:
- 12:21: external iOS XPC lifecycle refinement
- 13:21: KB-internal mobile/protected-runtime lower-surface repair

That meant this hour should return to a real external-research pass rather than becoming another internal-maintenance slot.

Branch-choice reasoning:
- browser and malware had already received substantial recent attention
- protocol remains healthy but still benefits from thinner proof-discipline refinements with high operator value
- pending-request current-owner realism was a practical, underfed refinement target where docs could sharpen an existing note without taxonomy drift

This run therefore improved branch balance in the intended way:
- real multi-source search attempted
- no browser/malware clustering by inertia
- one thinner protocol practical seam materially improved

## What changed
### Added
- `sources/protocol/2026-04-07-pending-request-current-owner-notes.md`
- `sources/protocol/2026-04-07-1421-pending-request-reuse-search-layer.txt`
- `runs/2026-04-07-1421-run-report.md`

### Updated
- `topics/protocol-pending-request-generation-epoch-and-slot-reuse-workflow-note.md`

## New findings
Primary durable split reinforced this run:

```text
visible correlation token matches
  != request is still the current live owner
  != timeout/cancel/retire/epoch shift left ownership unchanged
  != late completion still belongs to the same meaningful path
  != later consequence truth
```

Concrete practical reinforcements preserved in the note:
- visible IDs/tags/user_data are weaker than current-owner proof
- timeout/cancel cleanup is weaker than proving a later completion still belongs to the same live request
- epoch/generation/slot reuse realism matters even when outer correlation looks good
- completion visibility is still weaker than one later consequence that answers the analyst’s real question

## Sources consulted
Primary source anchors:
- gRPC C++ async docs
  - https://grpc.io/docs/languages/cpp/async/
- `io_uring(7)` man page
  - https://man7.org/linux/man-pages/man7/io_uring.7.html

Additional attempted anchor:
- kernel docs fetch attempted at `https://docs.kernel.org/io_uring/io_uring.html`
- current fetch path returned 404; non-blocking for this run

Search-backed support:
- explicit `search-layer` attempt with `--source exa,tavily,grok`
- Exa and Tavily returned usable surfaced material
- Grok was invoked but again failed through the configured proxy path

## Reflections / synthesis
This was the right shape of protocol external run.
It did **not** create a broad async-correlation taxonomy page.
It tightened one practical ownership/liveness proof boundary inside the existing pending-request note.

The durable branch value is:
- keeping async request/reply analysis honest about current-owner realism
- preventing overclaim from superficial token matches or late completion visibility alone
- improving the handoff from correlation work into timeout/cancel/reuse or later consequence work

## Candidate topic pages to create or improve
Good later protocol candidates if branch balance points back here:
- a narrower timeout/cancel cleanup continuation when current-owner realism is already known to be the main liar
- a parent-page sync only if the new late-completion/current-owner memory still feels too leaf-local

## Next-step research directions
Near-term preference:
- keep alternating across underfed branches rather than spending several consecutive external runs inside one family
- if protocol returns soon, prefer similarly bounded proof-discipline refinements over broad family expansion

## Concrete scenario notes or actionable tactics added this run
- do not stop at matching IDs/tags/user_data alone
- decide explicitly whether the missing proof is current-owner liveness, timeout/cancel cleanup, epoch/slot reuse, or later consequence truth
- keep late completion visibility separate from same-path ownership
- treat superficial correlation as a clue, not a solved ownership claim

## Search audit
Search sources requested:
- `exa`
- `tavily`
- `grok`

Search sources succeeded:
- `exa`
- `tavily`

Search sources failed:
- `grok`

Exa endpoint:
- via `python3 /root/.openclaw/workspace/skills/search-layer/scripts/search.py --source exa,tavily,grok`

Tavily endpoint:
- via `python3 /root/.openclaw/workspace/skills/search-layer/scripts/search.py --source exa,tavily,grok`

Grok endpoint:
- via configured completions proxy surfaced in runtime errors: `http://proxy.zhangxuemin.work:8000/v1/chat/completions`

Searches attempted this run:
1. `gRPC completion queue tag Next official docs outstanding call completion ownership`
2. `io_uring user_data completion stale reused request ownership official docs`
3. `late reply timeout cleanup outstanding request ownership async completion official docs`

Degraded-source-set note:
- this run still counts as a real external-research pass because all three sources were explicitly requested and Grok was actually invoked
- Grok returned no usable results because of repeated proxy-side 502 failures
- Exa, Tavily, and primary gRPC/io_uring docs were sufficient for a conservative practical extension of the existing protocol note

## Commit / archival sync
If diffs remain after report write:
- commit only KB paths from `/root/.openclaw/workspace`
- then run `/root/.openclaw/workspace/scripts/sync-reverse-expert-kb.sh`
- if archival sync still fails because remote `main` moved ahead, preserve the local commit and record the sync-state failure conservatively rather than treating it as KB-content failure

## Best-effort notes
- `.learnings/ERRORS.md` remains best-effort only and should not block this run
