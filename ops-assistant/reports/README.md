# Reports

Planned layout:

- `reports/YYYY-MM-DD/fleet-summary.md`
- `reports/YYYY-MM-DD/<host>.md`

Only meaningful runs should be committed.

Guideline:
- keep dated Markdown reports when they capture real findings
- do not commit transient top-level `.log` artifacts generated by wrapper runs
- prefer summaries/findings over raw execution noise
