# Cloudflare DNS audit — 2026-07-14

Zone: `zhangxuemin.work`

## Live snapshot result

- Live record count: 51
- Type counts: A 41, AAAA 2, MX 4, TXT 4
- Comparison against `infra/cloudflare-dns/baseline-records.json`: **no semantic DNS diff**
  - added: 0
  - removed: 0

## Finding requiring operator review

The live zone still contains:

- `ctf-gpt-cn.zhangxuemin.work` A `154.86.30.10` (DNS-only, HK edge)

The committed baseline and older reconciliation docs treat this as an active CN/HK edge for the `oracle-newapi-standby` / legacy `oracle-reverse-dev` CTF GPT Plus route. However, the current host documentation for `oracle-newapi-standby` says the 2026-07-07 cleanup removed the origin app/listener for that route and that remaining DNS/HK-relay config should be treated as stale edge configuration until deliberately rebuilt or removed.

This is not an unexpected Cloudflare API drift from the committed baseline, but it is now a **documented service-state mismatch**: DNS still publishes an edge name whose origin service is documented as gone.

## Recommended next step

Do not mutate Cloudflare automatically from this cron. Operator should choose one coordinated path:

1. If CTF GPT Plus is intentionally retired: remove or disable the HK relay route and then delete `ctf-gpt-cn.zhangxuemin.work` from Cloudflare; afterwards refresh `baseline-records.json` and the DNS reconciliation docs.
2. If the service should remain available: rebuild/verify the origin on `oracle-newapi-standby` and update the host docs to mark `ctf-gpt-cn.zhangxuemin.work` active again.

No Cloudflare records were changed by this audit.
