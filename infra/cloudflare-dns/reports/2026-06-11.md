# Cloudflare DNS zone audit — 2026-06-11

Zone: `zhangxuemin.work`

## Live snapshot result

- Live record count: 44
- Type counts: A 34, AAAA 2, MX 4, TXT 4
- Compared with committed `infra/cloudflare-dns/baseline-records.json`: **no semantic diff**
  - Added vs baseline: 0
  - Removed vs baseline: 0

The live Cloudflare zone still matches the committed baseline snapshot exported by the maintenance script.

## Documentation reconciliation finding

There is no live-vs-baseline DNS drift, but the New API / former registry transition remains unresolved between current host documentation, inventory, and live DNS.

Current host docs and inventory describe:

- `infra/hosts/oracle-newapi-primary/NETWORK.md`
  - active New API entrypoint: `ai.zhangxuemin.work`
  - planned DNS: `newapi.zhangxuemin.work`
  - former registry names are retired: `hub.zhangxuemin.work`, `ghcr.zhangxuemin.work`, `k8s.zhangxuemin.work`, `mcr.zhangxuemin.work`
  - if those DNS records still exist, treat them as stale until separately reconciled
- `infra/hosts/oracle-newapi-standby/NETWORK.md`
  - planned DNS: `newapi-standby.zhangxuemin.work`
  - former CTF GPT Plus app access was retired
  - remaining HK/Cloudflare edge objects for that app should be treated as stale until separately cleaned
- `infra/hosts/hk-relay/NETWORK.md`
  - still documents `ctf-gpt-cn.zhangxuemin.work` as an active HK edge endpoint, so this remains internally inconsistent with the standby host doc
- `infra/inventory.yaml`
  - lists `newapi.zhangxuemin.work` and `newapi-standby.zhangxuemin.work` as host domains, but live DNS does not currently contain either record
  - lists `reverse-cn.zhangxuemin.work` under `oracle-newapi-standby`, while live DNS intentionally points that name to `hk-relay` (`154.86.30.10`) as the SSH edge

The live zone still contains the former registry records:

| Record | Live target | Current host-doc interpretation |
|---|---:|---|
| `hub.zhangxuemin.work` | `140.245.33.114` | former registry mapping; host now documented as New API primary |
| `ghcr.zhangxuemin.work` | `140.245.33.114` | former registry mapping |
| `k8s.zhangxuemin.work` | `140.245.33.114` | former registry mapping |
| `mcr.zhangxuemin.work` | `140.245.33.114` | former registry mapping |

The live zone does **not** currently contain the new planned New API names:

| Planned record | Current live state |
|---|---|
| `newapi.zhangxuemin.work` | absent |
| `newapi-standby.zhangxuemin.work` | absent |

Related edge records:

| Record | Live target | Current doc interpretation |
|---|---:|---|
| `reverse-cn.zhangxuemin.work` | `154.86.30.10` | HK SSH edge to standby host; inventory currently lists it under the standby host rather than the DNS-facing HK relay |
| `ctf-gpt-cn.zhangxuemin.work` | `154.86.30.10` | standby host doc says retired/stale; HK relay doc still says active |

## Classification

- Live zone vs committed baseline: **matches baseline**.
- Live zone vs current host/inventory docs: **needs operator review**.
- Risk level: **moderate documentation/live-state ambiguity**, not an emergency DNS drift.

This report intentionally does not refresh the baseline because the baseline already matches live DNS and refreshing it would not resolve the documentation conflict.

## Recommended next step

Ask the operator to choose the intended New API cutover state:

1. If New API should receive canonical DNS now:
   - add `newapi.zhangxuemin.work -> 140.245.33.114`
   - add `newapi-standby.zhangxuemin.work -> 140.245.61.236`
   - decide whether `hub`, `ghcr`, `k8s`, and `mcr` remain as compatibility aliases or should be removed/repurposed
2. If the former registry names are intentionally retained during a compatibility window:
   - update `infra/hosts/oracle-newapi-primary/NETWORK.md`, `infra/inventory.yaml`, and `infra/dns-reconciliation.md` to say so explicitly
3. Reconcile `ctf-gpt-cn.zhangxuemin.work` between the standby host doc and the HK relay doc before any DNS deletion.
4. Normalize the inventory representation for `reverse-cn.zhangxuemin.work` so it is clear that DNS points to `hk-relay` while the TCP edge forwards to `oracle-newapi-standby`.

No Cloudflare record mutations were applied in this cron run.
