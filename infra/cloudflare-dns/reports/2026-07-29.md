# Cloudflare DNS zone audit — 2026-07-29

Zone: `zhangxuemin.work`

## Live snapshot result

The live Cloudflare DNS snapshot was exported with `skills/cloudflare-dns-maintenance/scripts/cloudflare_zone_snapshot.py` and compared with `infra/cloudflare-dns/baseline-records.json`.

- live record count: 53
- type counts: A 43, AAAA 2, MX 4, TXT 4
- semantic diff vs committed baseline: none
- added records vs baseline: 0
- removed records vs baseline: 0

## Operator-review item

Although the live zone matches the committed baseline, one live record still appears risky relative to the host documentation:

| Record | Live DNS | Why it needs review |
|---|---|---|
| `ctf-gpt-cn.zhangxuemin.work` A | `154.86.30.10` (`hk-relay`) | `infra/hosts/oracle-newapi-standby/NETWORK.md` says the origin app/listener for the CTF GPT Plus route was removed on 2026-07-07 and any remaining DNS/HK relay config should be treated as stale edge configuration until deliberately rebuilt or removed. Other docs and the committed baseline still carry the DNS record as present. |

No Cloudflare mutations were applied.

## Recommended next step

Make an explicit operator decision for `ctf-gpt-cn.zhangxuemin.work`:

1. If CTF GPT Plus is retired: remove or disable the HK relay route, then delete `ctf-gpt-cn.zhangxuemin.work` from Cloudflare; afterwards refresh `baseline-records.json`, `baseline-summary.md`, and the DNS reconciliation docs.
2. If the service should remain available: rebuild/verify the origin on `oracle-newapi-standby`, then update the standby and HK relay docs to mark the route active again.

Until that decision is made, keep treating this as a review item rather than an automatic DNS cleanup target.
