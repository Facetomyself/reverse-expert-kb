# Cloudflare DNS audit — 2026-07-21

Zone: `zhangxuemin.work`

## Live snapshot result

- Live record count: 51
- Type counts: A 41, AAAA 2, MX 4, TXT 4
- Comparison against `infra/cloudflare-dns/baseline-records.json`: **no semantic DNS diff**
  - added: 0
  - removed: 0

The live snapshot was exported with `skills/cloudflare-dns-maintenance/scripts/cloudflare_zone_snapshot.py`; no Cloudflare records were mutated.

## Finding requiring operator review

The live zone still contains:

- `ctf-gpt-cn.zhangxuemin.work` A `154.86.30.10` (DNS-only, HK edge)

This still matches the committed baseline, so it is not unexpected Cloudflare API drift. It remains a service-state reconciliation issue:

- `infra/hosts/hk-relay/NETWORK.md` documents `ctf-gpt-cn.zhangxuemin.work` as a CN/HK TLS edge to `http://140.245.61.236:8000/ctf-gpt-plus`.
- `infra/hosts/oracle-newapi-standby/NETWORK.md` documents that the 2026-07-07 cleanup removed the origin app/listener and says remaining DNS/HK-relay config should be treated as stale edge configuration until deliberately rebuilt or removed.

## Recommended next step

Do not mutate Cloudflare automatically from this recurring audit. Operator should choose one coordinated path:

1. If CTF GPT Plus is intentionally retired: remove or disable the HK relay route, then delete `ctf-gpt-cn.zhangxuemin.work` from Cloudflare; afterwards refresh `baseline-records.json`, `baseline-summary.md`, and the DNS reconciliation docs.
2. If the service should remain available: rebuild/verify the origin on `oracle-newapi-standby`, then update the standby and HK relay docs to mark `ctf-gpt-cn.zhangxuemin.work` active again.

No Cloudflare records were changed by this audit.
