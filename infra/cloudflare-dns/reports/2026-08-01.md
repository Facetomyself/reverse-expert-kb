# Cloudflare DNS Zone Audit - 2026-08-01

Zone: `zhangxuemin.work`

## Live Snapshot Result

The live Cloudflare DNS zone was exported with `skills/cloudflare-dns-maintenance/scripts/cloudflare_zone_snapshot.py` and compared against `infra/cloudflare-dns/baseline-records.json`.

- live record count: 53
- type counts: A 43, AAAA 2, MX 4, TXT 4
- semantic diff vs committed baseline: none
- added records: 0
- removed records: 0

No Cloudflare record mutation was made.

## Documentation Review Finding

The live zone and committed baseline still contain:

- `ctf-gpt-cn.zhangxuemin.work A 154.86.30.10`

The current reconciliation document classifies it as matching reality and describes it as the HK edge for `oracle-reverse-dev` CTF GPT Plus. However, `infra/hosts/oracle-newapi-standby/NETWORK.md` now says the origin app/listener for `https://ctf-gpt-cn.zhangxuemin.work/ctf-gpt-plus` was removed during the 2026-07-07 cleanup, and any remaining DNS/HK-relay configuration should be treated as stale edge configuration until deliberately rebuilt or removed.

The HK relay host documentation still lists the edge route and origin target as configured, so this is not a baseline-vs-live drift. It is an operator-review item where the live DNS/HK edge may be intentionally retained for compatibility, but the origin-host document says the service is gone.

## Recommended Next Step

Review `ctf-gpt-cn.zhangxuemin.work` as a coordinated DNS/HK-relay/origin cleanup decision:

- If CTF GPT Plus should remain retired, remove or disable the HK edge route and then remove the DNS record from Cloudflare in an explicit operator-approved change.
- If the service should return, rebuild/verify the origin listener on `oracle-newapi-standby` and update `infra/hosts/oracle-newapi-standby/NETWORK.md` to mark the route active again.

Until that decision is made, keep the live DNS unchanged and do not refresh the baseline.
