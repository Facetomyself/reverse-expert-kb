# Cloudflare DNS zone audit — 2026-06-09

Zone: `zhangxuemin.work`

## Live snapshot result

- Live record count: 42
- Type counts: A 32, AAAA 2, MX 4, TXT 4
- Compared with committed `infra/cloudflare-dns/baseline-records.json`: **no semantic diff**
  - Added vs baseline: 0
  - Removed vs baseline: 0

The live Cloudflare zone still matches the committed baseline snapshot.

## Documentation reconciliation finding

Although there is no live-vs-baseline DNS drift, the current host/inventory documentation has moved ahead of the committed DNS reconciliation docs for the New API host rename/deployment.

Current `infra/inventory.yaml` and the new host network docs describe:

- `oracle-newapi-primary` / `140.245.33.114`
  - planned DNS: `newapi.zhangxuemin.work`
  - former registry names retired: `hub.zhangxuemin.work`, `ghcr.zhangxuemin.work`, `k8s.zhangxuemin.work`, `mcr.zhangxuemin.work`
- `oracle-newapi-standby` / `140.245.61.236`
  - planned DNS: `newapi-standby.zhangxuemin.work`
  - former CTF GPT Plus app access retired

The live zone currently still contains the former registry records:

| Record | Live target | Current doc interpretation |
|---|---:|---|
| `hub.zhangxuemin.work` | `140.245.33.114` | retired former registry mapping; host is now documented as New API primary |
| `ghcr.zhangxuemin.work` | `140.245.33.114` | retired former registry mapping |
| `k8s.zhangxuemin.work` | `140.245.33.114` | retired former registry mapping |
| `mcr.zhangxuemin.work` | `140.245.33.114` | retired former registry mapping |

The live zone does **not** currently contain the new planned New API names:

| Expected/planned record | Current live state |
|---|---|
| `newapi.zhangxuemin.work` | absent |
| `newapi-standby.zhangxuemin.work` | absent |

Related edge note:

- `ctf-gpt-cn.zhangxuemin.work` still exists in the live zone and points at `154.86.30.10` (`hk-relay`).
- `infra/hosts/oracle-newapi-standby/NETWORK.md` says the former CTF GPT Plus origin/edge was retired and that remaining HK/Cloudflare edge objects should be treated as stale routing artifacts until separately cleaned.
- `infra/hosts/hk-relay/NETWORK.md` still documents the `ctf-gpt-cn` HK edge as an active endpoint, so this requires operator reconciliation before changing DNS.

## Classification

- Live zone vs committed baseline: **matches baseline**.
- Live zone vs current host/inventory docs: **needs operator review**.
- Risk level: **moderate documentation/live-state ambiguity**, not an emergency drift.

This report intentionally does not refresh the baseline because doing so would only preserve the already-known live state and would not resolve the documentation conflict.

## Recommended next step

Ask the operator to choose the intended New API DNS cutover state:

1. If New API is ready for DNS:
   - add `newapi.zhangxuemin.work -> 140.245.33.114`
   - add `newapi-standby.zhangxuemin.work -> 140.245.61.236`
   - then decide whether to remove or repurpose the old registry names (`hub`, `ghcr`, `k8s`, `mcr`) and the stale `ctf-gpt-cn` edge.
2. If the registry names must remain temporarily for compatibility:
   - update `infra/hosts/oracle-newapi-primary/NETWORK.md`, `infra/inventory.yaml`, and `infra/dns-reconciliation.md` to say the old names are intentionally retained during a compatibility window.
3. Separately reconcile `ctf-gpt-cn.zhangxuemin.work` between the standby host doc and the HK relay doc before any DNS deletion.

No Cloudflare record mutations were applied in this cron run.
