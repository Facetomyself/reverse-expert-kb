# Cloudflare DNS zone audit — 2026-07-09

Zone: `zhangxuemin.work`

## Live snapshot result

- Live record count: 51
- Type counts: A 41, AAAA 2, MX 4, TXT 4
- Compared with current working `infra/cloudflare-dns/baseline-records.json`: **no semantic diff**
  - Added vs working baseline: 0
  - Removed vs working baseline: 0
- Compared with committed `HEAD:infra/cloudflare-dns/baseline-records.json`: **2 live A records are not in HEAD**:
  - `gpt-session.zhangxuemin.work -> 158.178.236.241`
  - `gpt-session-cn.zhangxuemin.work -> 154.86.30.10`

The working-tree baseline and directly related docs already reflect the current live Cloudflare export for the GPT Session Converter source/HK entrypoints. No Cloudflare record mutations were applied.

## Documentation reconciliation finding

The `gpt-session` additions appear intentional/current rather than suspicious drift:

- `infra/hosts/oracle-proxy/NETWORK.md` documents `gpt-session.zhangxuemin.work` as the source/static site on `oracle-proxy`.
- `infra/hosts/hk-relay/NETWORK.md` documents `gpt-session-cn.zhangxuemin.work` as the CN/HK edge proxying to the source hostname.
- `infra/hosts/oracle-proxy/projects/gpt-session-converter.md` documents the runtime path, static-only deployment shape, DNS records, and verification.
- `infra/inventory.yaml` includes the source and CN/HK edge names under the relevant hosts.

A separate operator-review item remains for `ctf-gpt-cn.zhangxuemin.work`:

| Record | Live target | Current documentation state |
|---|---:|---|
| `ctf-gpt-cn.zhangxuemin.work` | `154.86.30.10` | `dns-reconciliation` / `hk-relay` docs still describe it as active, while `oracle-newapi-standby/NETWORK.md` now says the origin app/listener was removed on 2026-07-07 and the remaining DNS/HK edge config should be treated as stale until deliberately rebuilt or removed. |

This is not a live-vs-baseline DNS surprise, but it is a cross-document/runtime ambiguity that should be resolved before any deletion.

## Classification

- Live zone vs current working baseline: **matches**.
- Live zone vs committed baseline: **baseline/docs need commit/refresh** for the two GPT Session Converter A records.
- `gpt-session` / `gpt-session-cn`: **intentional/current documentation delta**.
- `ctf-gpt-cn`: **needs operator review** before any DNS or HK relay cleanup.

## Recommended next step

1. Commit the refreshed baseline and directly related docs for the intentional `gpt-session` / `gpt-session-cn` live records.
2. Separately decide whether `ctf-gpt-cn.zhangxuemin.work` should be rebuilt as an active HK edge or removed from DNS/HK relay/docs in a coordinated cleanup.

No Cloudflare record mutations were applied in this cron run.
