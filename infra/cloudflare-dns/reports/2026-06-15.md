# Cloudflare DNS zone audit — 2026-06-15

Zone: `zhangxuemin.work`

## Live snapshot result

- Live record count: 44
- Type counts: A 34, AAAA 2, MX 4, TXT 4
- Compared with current `infra/cloudflare-dns/baseline-records.json`: **no semantic diff**
  - Added vs baseline: 0
  - Removed vs baseline: 0
- Compared with the last committed `HEAD:infra/cloudflare-dns/baseline-records.json`: **2 live A records are not in HEAD**:
  - `ai.zhangxuemin.work -> 140.245.33.114`
  - `ai-cn.zhangxuemin.work -> 154.86.30.10`

The working-tree baseline already matches the live Cloudflare export exactly, so no Cloudflare mutation is needed.

## Documentation reconciliation finding

There is still a documentation/live-state ambiguity around the New API / former registry transition:

- Current DNS reconciliation and baseline treat `ai.zhangxuemin.work` and `ai-cn.zhangxuemin.work` as active, intentional New API entrypoints.
- The live zone still retains the former registry A records on `140.245.33.114`:
  - `hub.zhangxuemin.work`
  - `ghcr.zhangxuemin.work`
  - `k8s.zhangxuemin.work`
  - `mcr.zhangxuemin.work`
- `infra/hosts/oracle-newapi-primary/NETWORK.md` describes those former registry mappings as retired/stale if they still exist in DNS.
- The live zone does not currently contain the planned `newapi.zhangxuemin.work` or `newapi-standby.zhangxuemin.work` names that appear in inventory/host planning docs.
- `ctf-gpt-cn.zhangxuemin.work` also remains a cross-doc ambiguity: live DNS and `hk-relay` docs still describe it as an HK edge record, while the standby/New API host docs describe the old CTF app path as retired.

## Classification

- Live zone vs current working baseline: **matches**.
- Live zone vs last committed baseline: **baseline/docs need commit/refresh** for the two New API A records.
- Live zone vs host/inventory docs: **needs operator review** before any deletion or canonical-name cutover.
- Risk level: **moderate documentation/live-state ambiguity**, not emergency DNS drift.

## Recommended next step

1. Commit the already-refreshed baseline and directly related DNS reconciliation docs for the intentional `ai` / `ai-cn` live records.
2. Ask the operator to choose the intended state for the former registry names:
   - keep `hub`, `ghcr`, `k8s`, and `mcr` as compatibility aliases and document that explicitly; or
   - remove/repurpose them in a separately approved Cloudflare change.
3. Decide whether to create the planned `newapi` / `newapi-standby` DNS names, or remove them from planning docs until they are actually intended.
4. Reconcile `ctf-gpt-cn.zhangxuemin.work` between the standby/New API docs and `hk-relay` docs before any live DNS cleanup.

No Cloudflare record mutations were applied in this cron run.
