# oracle-newapi-standby / CHANGELOG

- 2026-07-07 21:30 GMT+8: Follow-up cleanup removed Firefox browser snap residue from the now-empty host. Ran `snap remove --purge firefox`, removed any remaining Firefox snap directories, and reset stale systemd failed units. Verification: `snap list` no longer includes Firefox, Firefox launcher paths are absent, `systemctl --failed` reports `0 loaded units listed`, root disk remains `96G total / 12G used / 85G free` (`13%` used).

- 2026-07-07 17:06 GMT+8: User clarified that New API standby is not used and requested `oracle-newapi-standby` be cleaned to a no-project baseline. Cleanup performed: removed the `new-api` container, removed Docker images (`calciumion/new-api:latest`, `hello-world`), pruned Docker networks/volumes/build cache, removed `/opt/new-api` and `/opt/new-api-docs`, stopped/disabled Caddy and replaced `/etc/caddy/Caddyfile` with an intentional-empty note, and cleaned `/home/ubuntu` to login/shell basics only (`.ssh`, shell rc/profile files). Verification after cleanup: Docker has 0 containers/images/volumes/build cache, public project listeners are gone (`80`, `443`, `127.0.0.1:13000`, `127.0.0.1:8888` absent), remaining public listener is SSH plus system/local controls, root disk `96G total / 12G used / 84G free` (`13%` used), memory `11Gi` total with `10Gi` available.

- 2026-07-06: Nightly read-only check confirmed `oracle-newapi-standby` healthy in standby New API role. Snapshot at `2026-07-06 03:00 GMT+8`: uptime 63d, load idle (`0.03 0.02 0.00`), root disk `96G total / 25G used / 72G free` (`26%` used), memory `11Gi` total with `10Gi` available, swap `48Ki / 2Gi`. `new-api` container up 3 weeks (healthy), loopback `127.0.0.1:13000` listening. Caddy on `80`. Legacy python listener on `127.0.0.1:8888` still present. **Kernel restart required** (pending updates). Tailscale status not checked (persistent issue since 2026-06-25). `snap-firefox-8519.mount` not-found/failed persists (harmless). No meaningful delta from last check.

- 2026-07-05: Nightly read-only check confirmed `oracle-newapi-standby` healthy in standby New API role. Snapshot at `2026-07-05 03:01 GMT+8`: uptime 62d, load `0.00 0.00 0.00`, root disk `96G total / 25G used / 72G free` (`26%` used), memory `11Gi` total with `10Gi` available, swap `48Ki / 2Gi`. `new-api` container up 3 weeks (healthy), loopback `127.0.0.1:13000` listening. Caddy on `80`. Legacy python listener on `127.0.0.1:8888` still present. **Kernel restart required** (pending updates). Tailscale status not checked (persistent issue since 2026-06-25). `snap-firefox-8519.mount` not-found/failed persists (harmless). No meaningful delta from last check.

- 2026-07-03: Nightly read-only check confirmed `oracle-newapi-standby` healthy in standby New API role. Snapshot at `2026-07-03 03:02 GMT+8`: uptime 60d, load `0.00 0.00 0.00`, root disk `96G total / 25G used / 72G free` (`26%` used), memory `11Gi` total with `10Gi` available, swap `48Ki / 2Gi`. `new-api` container up 3 weeks (healthy), loopback `127.0.0.1:13000` listening. Caddy on `80`. Legacy python listener on `127.0.0.1:8888` still present. **Kernel restart required** (pending updates). Tailscale status not checked (persistent issue since 2026-06-25). `snap-firefox-8519.mount` not-found/failed persists (harmless).

- 2026-06-19 16:03 GMT+8: Synced channel `codexcli` (type=1, `https://codexcli.club/v1`, models: agnes/agnes-2.0-flash/gpt-5.5/gpt-5.4/gpt-image-2/gpt-5.4-mini/gpt-4o/gpt-4o-mini) from primary for HA consistency. Upstream reported `INSUFFICIENT_BALANCE` — key valid but zero balance.
- 2026-06-12 03:00 GMT+8: Oracle-scoped nightly read-only check found the host reachable and resource-comfortable in its standby New API role (`96G total / 24G used / 73G free`, `25%` used; `11Gi` RAM with ~`10Gi` available; `new-api` healthy on loopback `127.0.0.1:13000`). Meaningful runtime delta: an enabled `gpt-pp.service` is now running from `/home/ubuntu/gpt-pp` as user `ubuntu`, bound only to `127.0.0.1:8888`, and has been active since 2026-06-10 06:47 UTC. No remediation was performed.

- 2026-06-08 22:16 GMT+8: Deployed a custom static New API documentation site under `/opt/new-api-docs`, served at `/docs/` via Caddy while preserving the New API console on `/`. The site adapts the quick-start/token/model-group/CLI structure from an external reference into this private relay deployment, covering primary/standby endpoints, semantic model families, OpenAI-compatible usage, Anthropic `/v1/messages` usage, and FAQ. Verified `/docs` redirect, `/docs/` HTML, `/docs/style.css`, and root console all return expected responses.
- 2026-06-08 21:46 GMT+8: Corrected channel protocol types after user review: `claude` now uses New API channel type `14` / Anthropic because Kiro-Go supports native `/v1/messages`; `gpt`, `deepseek`, `minimax`, `glm`, and `qwen` remain type `1` / OpenAI-compatible. Verified `gpt-5.5` via `/v1/chat/completions`, `claude-sonnet-4.6` via both `/v1/chat/completions` conversion and native `/v1/messages`, all OK on this host.
- 2026-06-08 21:33 GMT+8: Applied user correction to New API routing: renamed channels to semantic family names `gpt`, `claude`, `deepseek`, `minimax`, `glm`, and `qwen`; removed `codex-auto-review` because it is internal automatic-review use only; removed aggregate `auto` / `auto-thinking`; normalized GPT aliases to lower-case canonical names only; kept Kiro-Go `gpt-4/gpt-4o` out of New API; purged stale excluded model rows; verified real chat completions for one representative model per family and verified `auto` now returns expected `model_not_found`.
- 2026-06-08 21:25 GMT+8: Repartitioned New API upstream routing by source/model family: CPA/CLIProxy now serves only OpenAI-family models via `oracle-proxy-cpa-openai`; Kiro-Go is split into `oracle-proxy-kiro-go-anthropic` for Claude/Anthropic models and `oracle-proxy-kiro-go-other` for non-Anthropic/non-OpenAI models. Rebuilt `abilities` from each channel model list, excluded Kiro-Go `gpt-4/gpt-4o` to avoid OpenAI-route ambiguity, and verified `gpt-5.5`, `claude-sonnet-4.6`, `auto`, and `deepseek-3.2` real chat completions returned `OK`.
- 2026-06-08 20:35 GMT+8: Integrated existing `oracle-proxy` CPA/CLIProxy and Kiro-Go services into the standby New API site by direct SQLite provisioning instead of manual UI setup. Added channels `oracle-proxy-cliproxy-cpa` (`http://proxy.zhangxuemin.work:8317`, representative model `gpt-5.5`) and `oracle-proxy-kiro-go` (`https://kiro.zhangxuemin.work`, representative model `auto`), populated `abilities`/`models`, created an operator smoke-test token stored only at `/opt/new-api/newapi-access-token.txt`, enabled `SelfUseModeEnabled=true`, and verified real `/v1/chat/completions` calls through both upstreams returned `OK`. Pre-change DB backup stored under `/opt/new-api/backups/one-api.before-channel-provision-*.db`.
- 2026-06-08: After New API deployment, external check to `http://140.245.61.236/` initially returned `No route to host` while local Caddy was healthy. Root cause was host iptables allowing only SSH before a final `REJECT --reject-with icmp-host-prohibited`. Added and persisted a narrow new TCP/80 allow rule with `netfilter-persistent`; external HTTP check then returned `HTTP/1.1 200 OK` with `X-NewAPI-Role: standby`.
- 2026-06-08 19:17 GMT+8: User re-positioned the former `oracle-reverse-dev` host as `oracle-newapi-standby`, the standby New API / AI API relay host. System hostname changed to `oracle-newapi-standby`; local SSH alias `oracle-newapi-standby` should be preferred while `oracle-reverse-dev` remains as a compatibility alias. Deployed New API under `/opt/new-api` using image `calciumion/new-api:latest`, compose project `newapi`, container `new-api`, SQLite data under `/opt/new-api/data`, app listener `127.0.0.1:13000`, and Caddy HTTP front door on `:80` with role header `X-NewAPI-Role: standby`. Verification: container healthy, `curl -I http://127.0.0.1:13000/` and Caddy `curl -I http://127.0.0.1/` returned `HTTP/1.1 200 OK` and `X-New-Api-Version: v1.0.0-rc.10`.
- 2026-06-08: User-requested cleanup retired the no-longer-needed deployed projects on this host. Removed Docker compose workloads `abaiautoplus` (`abaiautoplus-app-1`) and `gpt-full-regist-and-payment-flow` (`fullflow-app-web`, `fullflow-payment-browser`), removed volumes `gpt-full-regist-and-payment-flow_accfile-data` and `gpt-full-regist-and-payment-flow_runtime-data`, removed local project images, removed `/home/ubuntu/aBaiAutoplus`, `/home/ubuntu/GPT-FULL-REGIST-AND-PAYMENT-FLOW`, `/opt/source-archives/GPT-FULL-REGIST-AND-PAYMENT-FLOW`, and transient Firefox profile/upload dirs, and stopped/disabled snap CUPS services. Verification after cleanup showed no Docker containers, no matching project images/volumes, project directories gone, and former public app/CUPS listeners `6080/8000/8765/8889/631` absent; root disk was `96G total / 22G used / 75G free` (`23%` used).
- 2026-06-02: Oracle-scoped nightly read-only check found the host reachable and resource-comfortable, but with meaningful runtime drift from the previous source-only/no-services stance. Observed Docker containers: `fullflow-app-web` and `fullflow-payment-browser` from compose project `gpt-full-regist-and-payment-flow` under `/home/ubuntu/GPT-FULL-REGIST-AND-PAYMENT-FLOW`, plus `abaiautoplus-app-1` under `/home/ubuntu/aBaiAutoplus`. Public listeners included TCP `6080`, `8000`, `8765`, `8889`, and recurring CUPS `631`, in addition to SSH `22`. No remediation was performed.
- 2026-06-01: Ran project compatibility checks for the source-only GPT flow checkout on `oracle-reverse-dev`: Python syntax compilation passed, root `.venv` `pip check` had no broken requirements after enabling pip, protocol tests passed (`9 passed`), selected ruyipage compatibility tests passed (`43 passed`), and a live Firefox/ruyipage headless smoke succeeded using snap Firefox with profiles under `/home/ubuntu/snap/firefox/common/ruyipage-profiles`. Updated non-secret env stubs with `RUYI_PROFILE_ROOT` because snap Firefox remote debugging did not reliably start with ruyipage temp profiles under `/tmp`/generic dot-directories.
- 2026-06-01: Added browser-side dependencies for the source-only GPT flow checkout: installed Firefox snap `151.0.2` at `/snap/bin/firefox`, Xvfb, x11vnc, dbus-x11, CJK/emoji fonts, and X11/Mesa support libraries. Created non-secret env stubs and set `RUYI_FIREFOX_PATH=/snap/bin/firefox`. Noted that the repo's documented fingerprint Firefox archive is x86_64-only and therefore not suitable for this aarch64 Oracle host unless an ARM-compatible browser bundle is supplied.
- 2026-06-01: Prepared Python dependencies for the source-only `GPT-FULL-REGIST-AND-PAYMENT-FLOW` checkout using `uv 0.11.17`. Created root `.venv` and component venvs for `protocol/gpt_trial_protocol` and `ruyipage`; installed editable protocol and ruyipage packages plus async/test dependencies. Verified `gpt-trial --help`, protocol test collection (9 tests), and ruyipage test collection (176 tests). No services or automation flows were started.
- 2026-06-01: Cloned `https://github.com/huverse/GPT-FULL-REGIST-AND-PAYMENT-FLOW.git` source-only to `/opt/source-archives/GPT-FULL-REGIST-AND-PAYMENT-FLOW` at commit `6f5f11d5b5ba`. No install scripts, web UI, queue workers, browser/payment automation, or services were started.
- 2026-06-01: Added domestic/HK-optimized CTF GPT Plus edge `https://ctf-gpt-cn.zhangxuemin.work/ctf-gpt-plus` via `hk-relay`, reverse-proxying to origin `http://140.245.61.236:8000/ctf-gpt-plus`. Verified edge HTTP 200.
- 2026-06-01: Added domestic/HK-optimized SSH edge `reverse-cn.zhangxuemin.work:22061` via `hk-relay`, forwarding to this host SSH `140.245.61.236:22`. Verified SSH login with the oracle-reverse-dev key and added local convenience aliases `oracle-reverse-dev-cn` / `reverse-cn`. Direct/global `oracle-reverse-dev` SSH remains available as fallback.
- 2026-05-10: Oracle-scoped nightly read-only check found the host still reachable and resource-comfortable, with no Docker containers and no return of the temporary public upload listener on `:18080`. Snapshot at `2026-05-09 19:04 UTC`: uptime ~6.9 days, load idle (`0.00 0.00 0.00`), root disk `96G total / 10G used / 86G free` (`11%` used), memory comfortable (`11Gi` total, ~`11Gi` available), swap unused, and snap CUPS services were active with `cupsd` again listening publicly on TCP `:631` (`0.0.0.0` / `[::]`). Treat this as recurring CUPS/snap service exposure drift, not reverse-dev application surface.
- 2026-05-07: Oracle-scoped nightly read-only check found the host reachable, resource-comfortable, and back to minimal public exposure. Snapshot at `2026-05-06 19:03 UTC`: uptime ~3.9 days, load low (`0.08 0.02 0.01`), root disk `96G total / 10G used / 86G free` (`11%` used), memory comfortable (`11Gi` total, ~`11Gi` available), no swap use, no running Docker containers, and the focused listener check showed only SSH on public TCP `22`; the previously documented temporary public upload listener on `:18080` was not present. Snap CUPS services remain visible as service-level residue, but no public `:631` listener appeared in this focused check.
- 2026-04-29: Oracle-scoped nightly read-only check found the host reachable and resource-comfortable but no longer minimal-public-surface only. Snapshot at `2026-04-28 19:02 UTC`: uptime ~34.4 days, load low (`0.07 0.02 0.00`), root disk `96G total / 11G used / 86G free` (`11%` used), memory comfortable (`11Gi` total, ~`10Gi` available), no swap use, and no Docker containers running. Runtime drift to track: a temporary upload service is publicly listening on `0.0.0.0:18080` via `python3 /home/ubuntu/.tmp-upload-service/server.py`; Chromium remote-debug sessions remain loopback-only on `127.0.0.1:9222/9223/9224`; CUPS/snap listener on `:631` is present while `cups.service` reports inactive.
- 2026-03-27: Validation confirmed the host can run `js-reverse-mcp` in a minimal headless shape using local Node.js 22 + Chromium snap with `--executablePath=/snap/bin/chromium`.
- 2026-03-26: Read-only health check reconfirmed `oracle-reverse-dev` remained reachable and intentionally mostly idle. Snapshot at `2026-03-26 00:33 UTC`: uptime ~14 hours, load idle (`0.00 0.00 0.00`), root disk `96G total / 5.8G used / 91G free` (`6%` used), memory comfortable (`11Gi` total, `11Gi` available), no swap use, no running Docker containers, and listener surface remained minimal with only SSH publicly exposed.

- 2026-06-21: Nightly read-only check reconfirmed `oracle-newapi-standby` healthy in standby New API role. Snapshot at `2026-06-21 03:01 GMT+8`: uptime 48d, load idle, root disk `96G total / 24G used / 73G free` (`25%` used), memory `11Gi` total with `10Gi` available, swap `68Ki / 2Gi`. `new-api` container up 12d (healthy), loopback `127.0.0.1:13000` listening. Caddy on `80`. `gpt-pp.service` on `127.0.0.1:8888` still present (documented 2026-06-12). No drift.

- 2026-06-25: Nightly read-only check reconfirmed `oracle-newapi-standby` healthy in standby New API role. Snapshot at `2026-06-25 03:00 GMT+8`: uptime 52d, load idle, root disk `96G total / 25G used / 72G free` (`26%` used), memory `11Gi` total with `10Gi` available, swap `72Ki / 2Gi`. `new-api` container up 2 weeks (healthy), loopback `127.0.0.1:13000` listening. Caddy on `80`. `gpt-pp.service` on `127.0.0.1:8888` still present. **Tailscale not responding** (was documented as tailnet member since 2026-03-25 with IP `100.105.248.53` — may need restart).

- 2026-06-24: Nightly read-only check reconfirmed `oracle-newapi-standby` healthy in standby New API role. Snapshot at `2026-06-24 03:00 GMT+8`: uptime 51d, load idle, root disk `96G total / 24G used / 72G free` (`26%` used), memory `11Gi` total with `10Gi` available, swap `72Ki / 2Gi`. `new-api` container up 2 weeks (healthy), loopback `127.0.0.1:13000` listening. Caddy on `80`. `gpt-pp.service` on `127.0.0.1:8888` still present. No drift.

- 2026-06-26: Nightly read-only check confirmed `oracle-newapi-standby` healthy in standby New API role. Snapshot at `2026-06-26 03:03 GMT+8`: uptime 53d, load idle, root disk `96G total / 25G used / 72G free` (`26%` used), memory `11Gi` total with `10Gi` available, swap `72Ki / 2Gi`. `new-api` container up 2 weeks (healthy), loopback `127.0.0.1:13000` listening. Caddy on `80`. `gpt-pp.service` on `127.0.0.1:8888` still present. **Tailscale not responding** — may need restart (persistent issue since 2026-06-25). No other drift.

- 2026-06-30: Nightly read-only check confirmed `oracle-newapi-standby` healthy in standby New API role. Snapshot at `2026-06-30 03:01 GMT+8`: uptime 58d, load idle, root disk `96G total / 25G used / 72G free` (`26%` used), memory `11Gi` total with `10Gi` available, swap `48Ki / 2Gi`. `new-api` container up 3 weeks (healthy), loopback `127.0.0.1:13000` listening. Caddy on `80`. Legacy python listener on `127.0.0.1:8888` still present. **Tailscale not responding** (persistent). One new systemd failure: `snap-firefox-8519.mount` not-found/failed — likely stale snap mount, harmless.

- 2026-07-07: Nightly read-only check confirmed `oracle-newapi-standby` healthy. Snapshot at `2026-07-07 03:01 GMT+8`: uptime 64d, load idle (`0.00 0.00 0.00`), root disk `96G total / 25G used / 72G free` (`26%` used), memory `11Gi` total with `10Gi` available, swap negligible. `new-api` container up 4 weeks (healthy), loopback `127.0.0.1:13000` listening. Caddy on `80`. Legacy python listener on `127.0.0.1:8888` still present. Kernel restart required (pending). Tailscale status not checked. `snap-firefox-8519.mount` not-found/failed persists (harmless). No meaningful delta.
