# self-server / CHANGELOG

- 2026-04-04: Clarified likely current SSH entrypoint as `root@211.144.221.229:44001` based on user-provided access detail. Same-day verification showed this host still timed out from the current OpenClaw host on the direct path, but TCP connection to `211.144.221.229:44001` succeeded from `ali-cloud`. OpenClaw-side SSH config was updated so alias `self-server` now prefers `ProxyJump ali-cloud`, while `self-server-direct` remains available for direct-path diagnostics. After the user corrected the password, a full SSH login through the jump-host path succeeded. An OpenClaw-side public key was then installed for `root`, and passwordless login through `ssh self-server` was confirmed working. Same-day probing also checked `211.144.221.229:40005` as a second user-mentioned target on the same public IP; that port still timed out even from `ali-cloud`, so it remains pending for later. Password was intentionally not written into `infra/`.
