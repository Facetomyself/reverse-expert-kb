# oracle-mail / CHANGELOG

- 2026-04-08: Per user instruction, removed `rbot` from active infra and ops tracking. Current docs for `oracle-mail` now focus only on the live `Outlook Email Plus` deployment plus archived Mailu/moemail context.
- 2026-03-25: Deployed `rbot` / `Radiance OCI Bot` as a local systemd service on `oracle-mail` with local TLS listener `:9527`, then fronted it through the existing `outlook-email-plus-caddy` on shared `80/443` using `dreamhorse.eu.cc` as the public browser entrypoint. This avoided the hard browser failure caused by `mail.zhangxuemin.work` HSTS colliding with the app's self-signed `radiance` certificate on `:9527`.
- 2026-03-25: Updated `/root/rbot/client_config` to use `local_address=https://dreamhorse.eu.cc`, configured the Telegram-issued web credentials, and restored stable systemd ownership of the running `rbot.service` process.
- 2026-03-25: Installed OCI API material for `rbot` under `/root/rbot/oci_api_key.pem` and `client_config`. Verified successful client-side load via log line `oracle配置获取成功 -> 当前profile为:DEFAULT,地区为:ap-singapore-2`.
- 2026-03-25: Current `rbot` runtime also reports `非闪电用户只能使用1个，请去【客户端管理】删除其他客户端`, indicating the remaining blocker is service/account-side client-count policy rather than local deployment failure.
- 2026-03-25: Verified new external DNS on the host public IP `140.83.52.216` for `dreamhorse.eu.cc`, `www.dreamhorse.eu.cc`, `zhangxuemin.eu.cc`, `www.zhangxuemin.eu.cc`, `mengma.eu.cc`, and `www.mengma.eu.cc`. Authoritative `share-dns` servers returned A records for all six names pointing at this host; recursive/public resolution was still propagating during the first checks.
- 2026-03-24: Recurring read-only fleet check confirmed `oracle-mail` remains reachable and stable in its current web-app role. Snapshot at `2026-03-23 19:24 UTC`: uptime ~394 days, load idle (`0.00 0.00 0.00`), root disk `36G total / 21G used / 16G free` (`57%` used), memory comfortable (`5.5Gi` total, `4.0Gi` available), and both `outlook-email-plus-caddy` plus healthy `outlook-email-plus-app` remained up.
- 2026-03-24: Listener check again showed public `80/443` present with no classic mail protocol listener revival, which continues to match the documented repurposed role rather than dormant Mailu semantics.
- 2026-03-24: Refreshed `NETWORK.md` to remove now-stale pre-repurpose reachability notes and align it with the current `Outlook Email Plus` deployment.
- 2026-03-15: First-pass inspection completed. Confirmed `oracle-mail` (`140.83.52.216`) is an Oracle Linux 8.10 host with Docker running but no active containers or public mail/web listeners. Found a dormant Mailu deployment and a separate `moemail` repository. Public DNS currently points mail-related names here, but runtime does not match the expected exposed services.
- 2026-03-15: Per user instruction, retired the two inactive local email stacks instead of deleting them. Archived `/root/mailu` and `/root/moemail` to `/root/retired-services/2026-03-15/{mailu,moemail}` after confirming no containers were running. Active temporary mail is handled elsewhere via a Cloudflare-based deployment.
- 2026-03-20: Recurring read-only fleet check confirmed `oracle-mail` remains reachable and dormant. Snapshot at `2026-03-19 19:25 UTC`: uptime ~390 days, load idle (`0.00 0.00 0.00`), root disk still `36G total / 20G used / 16G free` (`56%` used), memory comfortable (`5.5Gi` total, `4.2Gi` available), no running Docker containers, and still no public `25/80/443/143/993` mail/web listeners. Only SSH plus local loopback listeners were present, which continues to match the documented retired role rather than an accidental service revival.
- 2026-03-16: Performed a read-only host health check over SSH. Current snapshot: uptime ~387 days, root disk 56% used, memory healthy, no active Docker containers, and no public mail/web listener resurfaced. This continues to match the intended retired/dormant role rather than an accidentally revived mail host.
- 2026-03-20: Deployed `Outlook Email Plus` on `oracle-mail` under `/opt/outlook-email-plus` using Docker Compose (`outlook-email-plus-app` + `outlook-email-plus-caddy`). `mail.zhangxuemin.work` now serves the web app over HTTPS with an automatically issued Let's Encrypt certificate.
- 2026-03-20: Follow-up recurring fleet check confirmed the new `Outlook Email Plus` deployment remained up several hours later: `outlook-email-plus-app` stayed healthy, `outlook-email-plus-caddy` still owned public `80/443`, and no legacy Mailu mail-protocol listeners resurfaced.
- 2026-03-21: Recurring read-only fleet check confirmed `oracle-mail` remains reachable and stable in its repurposed web-app role. Snapshot at `2026-03-20 19:24 UTC`: uptime ~391 days, load idle (`0.02 0.02 0.00`), root disk `36G total / 21G used / 16G free` (`57%` used), memory comfortable (`5.5Gi` total, `4.0Gi` available), `outlook-email-plus-caddy` and healthy `outlook-email-plus-app` both remained up, and public `80/443` listeners were still present without any classic mail-protocol listener revival.
- 2026-03-20: Evaluated integrating the existing Cloudflare temporary mailbox stack (`dreamhunter2333/cloudflare_temp_email`) into the Outlook Email Plus "临时邮箱" view. Conclusion: not directly compatible because the Cloudflare stack uses `x-custom-auth`, optional address passwords, and JWT-scoped mailbox access, while Outlook Email Plus expects a GPTMail-style backend contract. Integration is possible later via a compatibility adapter, but was explicitly deferred.
- 2026-04-04: Recurring read-only fleet check reconfirmed `oracle-mail` remained reachable and healthy in its current web-app role. Snapshot at `2026-04-04 07:56 UTC`: uptime ~406 days, load low (`0.00 0.04 0.01`), root disk `36G total / 21G used / 16G free` (`57%` used), memory comfortable (`5.5Gi` total, `4.0Gi` available), swap lightly used (`133Mi / 4.0Gi`), `outlook-email-plus-caddy` and healthy `outlook-email-plus-app` were both up, public `80/443` remained present, and classic mail listeners still stayed absent.
- 2026-04-04: One factual runtime delta only: the current `outlook-email-plus-*` containers were observed as recently refreshed (~28 hours old) while the host itself remained long-lived, which is consistent with an app/container restart or redeploy rather than host instability.
