# oracle-reverse-dev / PROJECTS

## Summary
`oracle-reverse-dev` is a lightweight ARM reverse-development / utility box.

## Active project groups
### 1. Reverse / MCP workflows
- `js-reverse-mcp`
- Chromium remote-debug workflows
- related MCP launcher tooling

## Quick checks
```bash
ssh oracle-reverse-dev
uname -a
node -v
/snap/bin/chromium --version
```
