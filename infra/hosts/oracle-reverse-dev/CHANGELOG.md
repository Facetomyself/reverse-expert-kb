# oracle-reverse-dev / CHANGELOG

- 2026-03-27: Validation confirmed the host can run `js-reverse-mcp` in a minimal headless shape using local Node.js 22 + Chromium snap with `--executablePath=/snap/bin/chromium`.
- 2026-03-26: Read-only health check reconfirmed `oracle-reverse-dev` remained reachable and intentionally mostly idle. Snapshot at `2026-03-26 00:33 UTC`: uptime ~14 hours, load idle (`0.00 0.00 0.00`), root disk `96G total / 5.8G used / 91G free` (`6%` used), memory comfortable (`11Gi` total, `11Gi` available), no swap use, no running Docker containers, and listener surface remained minimal with only SSH publicly exposed.
