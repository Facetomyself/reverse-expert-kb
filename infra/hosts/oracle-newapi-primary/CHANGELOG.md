# oracle-newapi-primary / CHANGELOG

- 2026-07-06: Nightly read-only check confirmed `oracle-newapi-primary` healthy in primary New API role. Snapshot at `2026-07-06 03:00 GMT+8`: uptime 63d, load idle (`0.04 0.01 0.00`), root disk `96G total / 7.9G used / 88G free` (`9%` used), memory `11Gi` total with `10Gi` available, swap `0Mi / 2Gi`. `new-api` container up 3 weeks (healthy), loopback `127.0.0.1:13000` listening. Caddy on `80/443`. **Kernel restart required** (pending updates). Tailscale status not checked (persistent issue since 2026-06-25). No meaningful delta from last check.

- 2026-07-03: Nightly read-only check confirmed `oracle-newapi-primary` healthy in primary New API role. Snapshot at `2026-07-03 03:02 GMT+8`: uptime 60d, load `0.08 0.06 0.02`, root disk `96G total / 7.9G used / 88G free` (`9%` used), memory `11Gi` total with `10Gi` available, swap `0Mi / 2Gi`. `new-api` container up 3 weeks (healthy), loopback `127.0.0.1:13000` listening. Caddy on `80/443`. **Kernel restart required** (pending updates). Tailscale status not checked (persistent issue since 2026-06-25).

- 2026-06-19 16:03 GMT+8: Added new channel `codexcli` (type=1/OpenAI-compatible) pointing to `https://codexcli.club/v1` with API key `sk-f93...b759`. Models exposed: `agnes,agnes-2.0-flash,gpt-5.5,gpt-5.4,gpt-image-2,gpt-5.4-mini,gpt-4o,gpt-4o-mini`. Endpoint responded with `INSUFFICIENT_BALANCE` — the key is valid but the upstream account has zero balance. Same channel added to `oracle-newapi-standby` for HA consistency.
- 2026-06-09 18:42 GMT+8: Added public New API source hostname `ai.zhangxuemin.work`. Cloudflare DNS-only A record points to `140.245.33.114`; Caddy now serves HTTPS for `ai.zhangxuemin.work` with root console proxied to `127.0.0.1:13000` and `/docs/` served from `/opt/new-api-docs`. Verification: `https://ai.zhangxuemin.work/` returned HTTP 200 with valid TLS.
- 2026-06-08 22:16 GMT+8: Deployed a custom static New API documentation site under `/opt/new-api-docs`, served at `/docs/` via Caddy while preserving the New API console on `/`. The site adapts the quick-start/token/model-group/CLI structure from an external reference into this private relay deployment, covering primary/standby endpoints, semantic model families, OpenAI-compatible usage, Anthropic `/v1/messages` usage, and FAQ. Verified `/docs` redirect, `/docs/` HTML, `/docs/style.css`, and root console all return expected responses.
- 2026-06-08 21:46 GMT+8: Corrected channel protocol types after user review: `claude` now uses New API channel type `14` / Anthropic because Kiro-Go supports native `/v1/messages`; `gpt`, `deepseek`, `minimax`, `glm`, and `qwen` remain type `1` / OpenAI-compatible. Verified `gpt-5.5` via `/v1/chat/completions`, `claude-sonnet-4.6` via both `/v1/chat/completions` conversion and native `/v1/messages`, all OK on this host.
- 2026-06-08 21:33 GMT+8: Applied user correction to New API routing: renamed channels to semantic family names `gpt`, `claude`, `deepseek`, `minimax`, `glm`, and `qwen`; removed `codex-auto-review` because it is internal automatic-review use only; removed aggregate `auto` / `auto-thinking`; normalized GPT aliases to lower-case canonical names only; kept Kiro-Go `gpt-4/gpt-4o` out of New API; purged stale excluded model rows; verified real chat completions for one representative model per family and verified `auto` now returns expected `model_not_found`.
- 2026-06-08 21:25 GMT+8: Repartitioned New API upstream routing by source/model family: CPA/CLIProxy now serves only OpenAI-family models via `oracle-proxy-cpa-openai`; Kiro-Go is split into `oracle-proxy-kiro-go-anthropic` for Claude/Anthropic models and `oracle-proxy-kiro-go-other` for non-Anthropic/non-OpenAI models. Rebuilt `abilities` from each channel model list, excluded Kiro-Go `gpt-4/gpt-4o` to avoid OpenAI-route ambiguity, and verified `gpt-5.5`, `claude-sonnet-4.6`, `auto`, and `deepseek-3.2` real chat completions returned `OK`.
- 2026-06-08 20:35 GMT+8: Integrated existing `oracle-proxy` CPA/CLIProxy and Kiro-Go services into the primary New API site by direct SQLite provisioning instead of manual UI setup. Added channels `oracle-proxy-cliproxy-cpa` (`http://proxy.zhangxuemin.work:8317`, representative model `gpt-5.5`) and `oracle-proxy-kiro-go` (`https://kiro.zhangxuemin.work`, representative model `auto`), populated `abilities`/`models`, created an operator smoke-test token stored only at `/opt/new-api/newapi-access-token.txt`, enabled `SelfUseModeEnabled=true`, and verified real `/v1/chat/completions` calls through both upstreams returned `OK`. Pre-change DB backup stored under `/opt/new-api/backups/one-api.before-channel-provision-*.db`.
- 2026-06-08 19:17 GMT+8: User re-positioned the former `oracle-registry` host as `oracle-newapi-primary`, the primary New API / AI API relay host. System hostname changed to `oracle-newapi-primary`; local SSH alias `oracle-newapi-primary` should be preferred while `oracle-registry` remains as a compatibility alias. Deployed New API under `/opt/new-api` using image `calciumion/new-api:latest`, compose project `newapi`, container `new-api`, SQLite data under `/opt/new-api/data`, app listener `127.0.0.1:13000`, and Caddy HTTP front door on `:80` with role header `X-NewAPI-Role: primary`. Verification: container healthy, `curl -I http://127.0.0.1:13000/` and Caddy `curl -I http://127.0.0.1/` returned `HTTP/1.1 200 OK` and `X-New-Api-Version: v1.0.0-rc.10`.
- 2026-06-08: User-requested cleanup retired all no-longer-needed deployed projects on this host. Removed Docker compose workloads `registry-proxy` (`reg-docker-hub`, `reg-ghcr`, `reg-k8s`, `reg-mcr`) and `nexusvault-monitor` (`nexusvault-monitor`, `nexusvault-dashboard`), removed project images `dqzboy/registry:latest` and `nexusvault-monitor:latest`, removed `/opt/registry-proxy` and `/opt/nexusvault-monitor`, and disabled the now-unused Caddy front door. Verification after cleanup showed no Docker containers, no matching project images/volumes, project directories gone, and public application listeners `80/443/51000/52000/55000/57000/58080/18080` absent; root disk was `96G total / 7.4G used / 89G free` (`8%` used).
- 2026-03-25: Fresh Oracle ARM host (`140.245.33.114`) reached over SSH after OCI-side IPv4 ingress and internet-path fixes.
- 2026-03-25: Base environment installed: common ops tools, Python runtime/build packages, Docker Engine + Compose v2, and persistent 2G swap.
- 2026-03-25: Migrated live registry-proxy front door from `oracle-gateway` to this host. New live public domains are `hub.zhangxuemin.work`, `ghcr.zhangxuemin.work`, `k8s.zhangxuemin.work`, and `mcr.zhangxuemin.work`.
- 2026-03-25: During cutover, local iptables initially allowed only SSH; adding and persisting `80/tcp` and `443/tcp` fixed ACME issuance and public service reachability.
- 2026-03-25: Installed local health-check helper `/usr/local/bin/check-registry-proxies` to validate both local backend ports and public HTTPS `/v2/` responses for the four registry domains.
- 2026-03-26: Read-only health check reconfirmed `oracle-registry` remained reachable and healthy after cutover. Snapshot at `2026-03-26 00:30 UTC`: uptime ~14 hours, load low (`0.09 0.02 0.01`), root disk `96G total / 5.9G used / 90G free` (`7%` used), memory comfortable (`11Gi` total, `10Gi` available), no swap use, public `80/443` remained present, and the expected four registry containers (`reg-docker-hub`, `reg-ghcr`, `reg-k8s`, `reg-mcr`) were all up.
- 2026-03-26: External validation from outside the host reconfirmed the four migrated registry front doors were serving correctly via Caddy. `https://hub.zhangxuemin.work/v2/`, `https://ghcr.zhangxuemin.work/v2/`, `https://k8s.zhangxuemin.work/v2/`, and `https://mcr.zhangxuemin.work/v2/` all returned `HTTP/2 200` with `docker-distribution-api-version: registry/2.0`.
- 2026-03-28: Recurring read-only fleet check reconfirmed `oracle-registry` remains reachable and healthy as the current registry front-door host. Snapshot at `2026-03-27 19:29 UTC`: uptime ~2 days, load still low (`0.03 0.02 0.00`), root disk `96G total / 6.1G used / 90G free` (`7%` used), memory comfortable (`11Gi` total, `10Gi` available), no swap use, public `80/443` remained present, backend listeners `51000/52000/55000/57000` were still up, and the expected four registry containers (`reg-docker-hub`, `reg-ghcr`, `reg-k8s`, `reg-mcr`) all remained running.
- 2026-03-31: Recurring read-only fleet check reconfirmed `oracle-registry` remained reachable and healthy. Snapshot at `2026-03-31 19:29 UTC`: uptime ~6.4 days, load idle (`0.00 0.00 0.00`), root disk `96G total / 6.2G used / 90G free` (`7%` used), memory comfortable (`11Gi` total, `10Gi` available), no swap use, public `80/443` remained present, backend listeners `51000/52000/55000/57000` were still up, and the expected four registry containers (`reg-docker-hub`, `reg-ghcr`, `reg-k8s`, `reg-mcr`) all remained running.
- 2026-03-31: One concise documentation delta only: UDP `443` was also observed open on the host during this pass, which matches the current Caddy/HTTP3-style HTTPS front door rather than indicating a separate newly introduced public service.
- 2026-04-06: Recurring read-only fleet check reconfirmed `oracle-registry` remained reachable and healthy. Snapshot at `2026-04-06 07:56 UTC`: uptime ~11.9 days, load idle, root disk `96G total / 6.4G used / 90G free` (`7%` used), memory comfortable (`11Gi` total, `10Gi` available), no swap use, public `80/443` remained present, backend listeners `51000/52000/55000/57000` were still up, and all four expected registry containers remained running.
- 2026-04-06: One concise runtime delta only: `reg-docker-hub` was observed much newer (`Up 47 hours`) than `reg-ghcr`, `reg-k8s`, and `reg-mcr` (`Up 11 days`) while the host itself stayed stable, which is most consistent with a targeted container refresh/recreate rather than broader host churn.
- 2026-04-08: Recurring read-only fleet check reconfirmed `oracle-registry` remained reachable and healthy. Snapshot at `2026-04-08 08:09 UTC`: uptime ~13.9 days, load low (`0.03 0.02 0.00`), root disk `96G total / 6.5G used / 90G free` (`7%` used), memory comfortable (`11Gi` total, `10Gi` available), no swap use, public `80/443` remained present, backend listeners `51000/52000/55000/57000` were still up, and the expected four registry containers remained running.
- 2026-06-06: Fixed NexusVault monitor dashboard staleness. Symptom: `https://drop.hk.zhangxuemin.work/nvmon/` looked like data was not updating. Root cause: `nexusvault-monitor` continued updating `/opt/nexusvault-monitor/data/state.json`, but `nexusvault-dashboard` kept an in-memory startup snapshot. Patched dashboard requests to reload state from disk before rendering `/` and `/api/state`, ran Go tests in a Go 1.26 container, then rebuilt the Docker image with `docker compose build --no-cache` and force-recreated `nexusvault-monitor` + `nexusvault-dashboard`. Verification: `state.json` and local dashboard `/api/state` both reported `updated_at=2026-06-06T02:33:48Z` after the next polling round.
- 2026-06-06: Tuned NexusVault monitor account-pool and polling behavior to reduce account pressure. Live config now keeps a 5-account pool, uses `120s + random 0..120s` monitor delay, enforces a `10m` per-account reuse gap, and lengthens failure cooldowns (`401=45m`, `403=90m`, `429=2h`, network=`10m`). Registration code now supports natural randomized usernames/display names; two new random-style accounts were automatically registered during deployment, bringing the pool to 5 accounts. Verification: `go test ./...` passed in a Go 1.26 container; containers were rebuilt no-cache and recreated; `data/state.json` showed 5 accounts and fresh `updated_at=2026-06-06T02:44:46Z` after the first post-change poll.

- 2026-06-21: Nightly read-only check reconfirmed `oracle-newapi-primary` healthy in primary New API role. Snapshot at `2026-06-21 03:01 GMT+8`: uptime 48d, load idle, root disk `96G total / 7.7G used / 89G free` (`9%` used), memory `11Gi` total with `10Gi` available, no swap use. `new-api` container up 12d (healthy), loopback `127.0.0.1:13000` listening. Caddy on `80/443`. Health endpoint `/api/health` returns 404 (expected for New API v1.0.0-rc.10; Docker healthcheck passes). No drift.

- 2026-06-25: Nightly read-only check reconfirmed `oracle-newapi-primary` healthy in primary New API role. Snapshot at `2026-06-25 03:00 GMT+8`: uptime 52d, load idle, root disk `96G total / 7.8G used / 89G free` (`9%` used), memory `11Gi` total with `10Gi` available, no swap use. `new-api` container up 2 weeks (healthy), loopback `127.0.0.1:13000` listening. Caddy on `80/443`. **Tailscale not responding** (was documented as tailnet member since 2026-03-25 with IP `100.96.23.110` — may need restart).

- 2026-06-24: Nightly read-only check reconfirmed `oracle-newapi-primary` healthy in primary New API role. Snapshot at `2026-06-24 03:00 GMT+8`: uptime 51d, load idle, root disk `96G total / 7.8G used / 89G free` (`9%` used), memory `11Gi` total with `10Gi` available, no swap use. `new-api` container up 2 weeks (healthy), loopback `127.0.0.1:13000` listening. Caddy on `80/443`. No drift.

- 2026-06-26: Nightly read-only check confirmed `oracle-newapi-primary` healthy in primary New API role. Snapshot at `2026-06-26 03:03 GMT+8`: uptime 53d, load idle, root disk `96G total / 7.8G used / 89G free` (`9%` used), memory `11Gi` total with `10Gi` available, no swap use. `new-api` container up 2 weeks (healthy), loopback `127.0.0.1:13000` listening. Caddy on `80/443`. **Tailscale not responding** — may need restart (persistent issue since 2026-06-25). No other drift.

- 2026-07-07: Nightly read-only check confirmed `oracle-newapi-primary` healthy. Snapshot at `2026-07-07 03:01 GMT+8`: uptime 64d, load idle (`0.03 0.01 0.00`), root disk `96G total / 7.9G used / 88G free` (`9%` used), memory `11Gi` total with `10Gi` available, no swap. `new-api` container up 4 weeks (healthy), loopback `127.0.0.1:13000` listening. Caddy on `80/443`. Kernel restart required (pending). Tailscale status not checked. No meaningful delta.
