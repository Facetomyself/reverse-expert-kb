# Reverse KB Run Report — 2026-04-08 10:56 Asia/Shanghai

Mode: external-research-driven
Date: 2026-04-08 10:56 Asia/Shanghai / 2026-04-08 02:56 UTC

## Summary
This run deliberately continued the bounded macOS persistence seam rather than broadening into a new startup-taxonomy page.

Chosen seam:
- **LaunchAgent/LaunchDaemon load or Login Items/BTM registration/approval visibility vs actual startup-owned launch truth when startup metadata is already visible but the natural persistence-owned launch path is still weaker than analysts assume**

Result:
- materially extended both macOS persistence workflow notes:
  - `topics/malware-launchd-launchagent-launchdaemon-consumer-proof-workflow-note.md`
  - `topics/malware-login-items-and-background-task-management-consumer-proof-workflow-note.md`
- added supporting source notes and saved the search-layer artifact

The practical value added this run is a sharper macOS persistence ladder:
- artifact or UI/record truth
- registration/load truth
- approval/eligibility or trigger/start truth
- startup-owned launch truth
- later consumer/effect truth

That keeps analysts from overreading Login Items UI, BTM records, `launchd` load/bootstrap state, or trigger/approval visibility as already-good proof of the startup-owned launch path they actually care about.

## Direction / branch-balance review
Recent cadence before this run:
- 07:51: external protected-runtime topology choice refinement
- 08:52: external WebSocket framed-consumer refinement
- 09:52: external IRQ/deferred consequence refinement

This hour kept the malware branch active, but on a bounded macOS persistence continuation rather than another Windows-heavy or broad malware survey pass.

Branch-choice reasoning:
- anti-stagnation still favored a real external-research pass
- the macOS persistence notes already existed and were the right place for a practical source-backed refinement
- this improved branch balance while keeping the work practical and case-driven

## What changed
### Added
- `sources/malware/2026-04-08-1052-macos-persist-search-layer.txt`
- `sources/malware/2026-04-08-macos-persistence-registration-vs-startup-launch-notes.md`
- `runs/2026-04-08-1056-run-report.md`

### Updated
- `topics/malware-launchd-launchagent-launchdaemon-consumer-proof-workflow-note.md`
- `topics/malware-login-items-and-background-task-management-consumer-proof-workflow-note.md`

## New findings
Primary durable split reinforced this run:

```text
artifact or UI/record truth
  != registration/load truth
  != approval/eligibility or trigger/start truth
  != startup-owned launch truth
  != later consumer/effect truth
```

Concrete practical reinforcements preserved in the notes:
- Login Items UI / BTM records are weaker than actual session-owned helper launch
- approval/eligibility truth is weaker than startup-owned launch truth
- `launchd` plist presence or load/bootstrap truth is weaker than actual startup-owned launch under the relevant domain/session/boot path
- even after startup-owned launch is proved, later consumer/effect truth still needs separate proof

## Sources consulted
Primary source anchors:
- Apple documentation surfaces for `SMAppService`, Login Items, and Background Task Management
- Apple documentation surfaces for `launchd`, LaunchAgent, LaunchDaemon, and launch conditions

Search-backed support:
- explicit `search-layer` attempt with `--source exa,tavily,grok`
- Exa and Tavily returned usable Apple documentation surfaces
- Grok was explicitly invoked but returned 502 errors through the configured proxy path

## Reflections / synthesis
This was the right shape of malware/macOS external run.
It did **not** create another broad startup-mechanisms page.
It tightened the existing macOS persistence workflow notes around registration/approval/load truth vs actual startup-owned launch truth.

The durable branch value is:
- keeping startup metadata separate from actual launch truth
- keeping approval/eligibility separate from natural startup-owned launch
- keeping launch truth separate from later consumer/effect truth
- making the macOS persistence side of the malware branch more useful for real operator reduction

## Candidate topic pages to create or improve
Good later macOS-malware candidates if branch balance points back here:
- a narrower continuation when startup-owned launch truth is already good enough but the remaining liar is one specific later helper-owned consumer/effect
- a parent/subtree sync only if the sharper macOS persistence ladder still feels too leaf-local

## Next-step research directions
Near-term preference:
- rotate again after this run rather than clustering several external slots inside macOS persistence
- continue preferring thin practical continuations over broad family restatements
- avoid internal-only wording/index maintenance unless it blocks a practical source-backed addition

## Concrete scenario notes or actionable tactics added this run
- ask explicitly whether the current proof is only startup metadata, only registration/load, only approval/eligibility or trigger/start truth, actual startup-owned launch truth, or one later consumer/effect
- do not stop at Login Items UI, BTM records, or `launchd` load/bootstrap state by default
- prove actual startup-owned launch separately before narrating later behavior

## Search audit
Search sources requested:
- `exa`
- `tavily`
- `grok`

Search sources succeeded:
- `exa`
- `tavily`

Search sources failed:
- `grok`

Exa endpoint:
- via `python3 /root/.openclaw/workspace/skills/search-layer/scripts/search.py --source exa,tavily,grok --mode deep --intent exploratory --num 5 --queries ...`

Tavily endpoint:
- via `python3 /root/.openclaw/workspace/skills/search-layer/scripts/search.py --source exa,tavily,grok --mode deep --intent exploratory --num 5 --queries ...`

Grok endpoint:
- via configured completions proxy surfaced in runtime errors: `http://proxy.zhangxuemin.work:8000/v1/chat/completions`

Searches attempted this run:
1. `Apple SMAppService login items background task management docs macOS`
2. `Apple launchd LaunchAgent LaunchDaemon plist loading loginwindow docs`
3. `Apple Background Task Management login items helper approval launch docs`

Degraded-source-set note:
- this run still counts as a real external-research pass because all three sources were explicitly requested and Grok was actually invoked
- Grok returned repeated 502 errors through the proxy path
- Exa, Tavily, and the retained Apple documentation surfaces were sufficient for a bounded extension of the existing macOS persistence notes

## Commit / archival sync
If diffs remain after report write:
- commit only KB paths from `/root/.openclaw/workspace`
- then run `/root/.openclaw/workspace/scripts/sync-reverse-expert-kb.sh`
- if archival sync fails, preserve the local commit and record the failure conservatively rather than treating it as KB-content failure

## Best-effort notes
- `.learnings/ERRORS.md` remains non-blocking / best-effort only
