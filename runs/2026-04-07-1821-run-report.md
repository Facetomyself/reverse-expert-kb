# Reverse KB Run Report — 2026-04-07 18:21 Asia/Shanghai

Mode: external-research-driven
Date: 2026-04-07 18:21 Asia/Shanghai / 2026-04-07 10:21 UTC

## Summary
This run deliberately used the external-research slot on a thinner native GUI seam instead of returning to browser or malware work.

Chosen seam:
- **Qt signal-slot delivery realism: connection truth != delivery truth != first behavior-changing consumer**

Result:
- materially extended `topics/native-gui-message-pump-and-signal-slot-first-consumer-workflow-note.md`
- added supporting source notes and saved the search-layer artifact

The practical value added this run is a sharper operator ladder for Qt-heavy GUI work:
- signal/connection exists
- connection type / receiver-thread delivery rule is what you think
- queued or direct delivery actually happened in the run that matters
- receiving slot was the first behavior-changing consumer
- later GUI/state consequence truth

That keeps analysts from overreading connect-site truth or signal emission truth as already-good first-consumer proof.

## Direction / branch-balance review
Recent cadence:
- 16:21: external protected-runtime topology-selection refinement
- 17:21: KB-internal notable-pages / index sync

That meant this hour should return to a real external-research pass rather than becoming another internal-maintenance slot.

Branch-choice reasoning:
- browser and malware had already received substantial recent attention
- native practical work remains a good home for thinner operator refinements that are easy to under-document
- the Qt delivery seam was already present in branch memory, but source-backed tightening improved real operator value without taxonomy drift

This run therefore improved branch balance in the intended way:
- real multi-source search attempted
- no browser/malware clustering by inertia
- one thinner native GUI seam materially improved

## What changed
### Added
- `sources/native-and-desktop/2026-04-07-1821-qt-signal-slot-search-layer.txt`
- `sources/native-and-desktop/2026-04-07-qt-signal-slot-delivery-notes.md`
- `runs/2026-04-07-1821-run-report.md`

### Updated
- `topics/native-gui-message-pump-and-signal-slot-first-consumer-workflow-note.md`

## New findings
Primary durable split reinforced this run:

```text
signal/connection exists
  != connection type / thread-affinity delivery rule is what you think
  != queued or direct delivery actually happened in the run that matters
  != receiving slot was the first behavior-changing consumer
  != later GUI/state consequence truth
```

Concrete practical reinforcements preserved in the note:
- connect-site truth is weaker than delivery truth
- emitted signal truth is weaker than actual queued/direct delivery truth
- `AutoConnection` needs receiver-thread reality, not folklore
- delivery truth is still weaker than one first behavior-changing consumer and later GUI/state consequence

## Sources consulted
Primary source anchors:
- Qt signals and slots docs
  - https://doc.qt.io/qt-6/signalsandslots.html
- Qt `ConnectionType` docs
  - https://doc.qt.io/qt-6/qt.html#ConnectionType-enum

Search-backed support:
- explicit `search-layer` attempt with `--source exa,tavily,grok`
- Exa and Tavily returned usable surfaced material
- Grok was invoked but again failed or returned no usable results through the configured proxy path

## Reflections / synthesis
This was the right shape of native external run.
It did **not** create a broad Qt GUI taxonomy page.
It tightened one practical delivery/consumer proof boundary inside the existing GUI note.

The durable branch value is:
- keeping Qt-heavy GUI work honest about delivery semantics
- preventing overclaim from connect sites, emissions, or assumed `AutoConnection` behavior
- improving the handoff from GUI signal discovery into first-consumer and later state-consequence work

## Candidate topic pages to create or improve
Good later native GUI candidates if branch balance points back here:
- a narrower event-filter vs signal-slot handoff continuation when both are visible and the first behavior-changing consumer is still ambiguous
- a parent-page sync only if the new Qt delivery memory still feels too leaf-local

## Next-step research directions
Near-term preference:
- keep alternating across underfed branches rather than spending several consecutive external runs inside one family
- if native returns soon, prefer similarly bounded proof-discipline refinements over broad family expansion

## Concrete scenario notes or actionable tactics added this run
- do not stop at connect-site visibility alone
- do not stop at emitted signal visibility alone
- decide explicitly whether the missing proof is connection-type resolution, queued/direct delivery, first behavior-changing consumer, or later GUI/state consequence
- treat `AutoConnection` as a delivery-rule question tied to receiver thread reality, not as a stable assumption

## Search audit
Search sources requested:
- `exa`
- `tavily`
- `grok`

Search sources succeeded:
- `exa`
- `tavily`

Search sources failed:
- `grok`

Exa endpoint:
- via `python3 /root/.openclaw/workspace/skills/search-layer/scripts/search.py --source exa,tavily,grok`

Tavily endpoint:
- via `python3 /root/.openclaw/workspace/skills/search-layer/scripts/search.py --source exa,tavily,grok`

Grok endpoint:
- via configured completions proxy surfaced in runtime errors: `http://proxy.zhangxuemin.work:8000/v1/chat/completions`

Searches attempted this run:
1. `Qt signals slots queued connection direct connection thread affinity official docs`
2. `Qt connection types queued direct blockingqueued official documentation`
3. `Qt AutoConnection receiver thread affinity queued delivery official docs`

Degraded-source-set note:
- this run still counts as a real external-research pass because all three sources were explicitly requested and Grok was actually invoked
- Grok returned no usable results because of proxy-side failures / weak results in this run
- Exa, Tavily, and primary Qt docs were sufficient for a conservative practical extension of the existing native GUI note

## Commit / archival sync
If diffs remain after report write:
- commit only KB paths from `/root/.openclaw/workspace`
- then run `/root/.openclaw/workspace/scripts/sync-reverse-expert-kb.sh`
- if archival sync fails, preserve the local commit and record the failure conservatively rather than treating it as KB-content failure

## Best-effort notes
- `.learnings/ERRORS.md` remains non-blocking / best-effort only
