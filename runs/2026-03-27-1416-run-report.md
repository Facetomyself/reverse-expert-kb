# Reverse Expert KB Autosync Run Report

Date: 2026-03-27 14:16 Asia/Shanghai / 2026-03-27 06:16 UTC
Mode: external-research-driven
Branch focus: iOS practical workflows
Chosen seam: Apple XPC / `NSXPCConnection` lifecycle-aware service handoff — interruption vs invalidation vs reconnection vs durable consumer truth

## Why this branch
Recent runs had already been genuinely external-research-driven, which is good, but the iOS Apple-platform service seam still had one thinner practical gap worth tightening.

The KB already preserved:
- client-side proxy truth vs service-side consumer truth
- listener acceptance vs later durable effect
- invalidation as a stronger end-of-life boundary than generic lifecycle drift

What still felt slightly too compressed was the handling of **interruption**.
In practice, analysts often need to separate:
- temporary interruption-style lifecycle disturbance
- stronger invalidation/end-of-life truth for the current connection instance
- later healthy-looking reconnection/restart truth
- actual exported-object re-entry or later durable service-owned consequence

That is a practical operator gap, not just wording polish, because it changes how compare pairs are interpreted when launchd-managed or on-demand services bounce.

## Work performed
### 1. Ran explicit multi-source external research
Executed `search-layer` with explicit `--source exa,tavily,grok` against Apple XPC lifecycle / interruption / invalidation / reconnection queries.

Research target:
- distinguish interruption from invalidation rather than keeping both inside generic reply/error chatter
- preserve a thinner stop rule for XPC compare pairs where later reconnection looks like proof of progress
- keep exported-object method truth and later durable consumer truth stronger than route/lifecycle chatter

### 2. Added a new source-backed notes page
Created:
- `sources/ios/2026-03-27-ios-xpc-interruption-vs-invalidation-and-reconnection-notes.md`

This note preserves the sharper ladder:
- `accepted != interrupted != invalidated != reconnected != consumed`

and the companion reminder:
- `proxy-visible != lifecycle-visible != exported-method-re-entry != durable-effect`

The note stayed conservative and anchored itself to Apple documentation surfaces plus clearly marked field-signal discussion links.

### 3. Folded the seam back into canonical KB pages
Updated:
- `topics/ios-xpc-proxy-to-service-consumer-workflow-note.md`
- `topics/ios-practical-subtree-guide.md`
- `index.md`

Net KB improvement:
- the Apple XPC seam now treats interruption as its own practical proof object rather than only generic reply/error noise
- compare pairs around launchd restart / on-demand recovery now have a cleaner stop rule
- the branch got a more operator-usable lifecycle-aware distinction without drifting into a broad new Apple XPC taxonomy page

## Direction review
Direction remains healthy.
This run did **not** fall back into pure internal wording/index/family-count sync.
It used a real external-research attempt to tighten one still-practical, under-specified branch seam.

Why this counts as meaningful KB maintenance instead of cosmetic sync:
- it improves how analysts choose the first stable proof object in XPC compare pairs
- it preserves a thinner case-driven operator ladder, not just more prose
- it keeps the branch practical and service-consumer-oriented rather than taxonomy-heavy

Good ongoing direction for this branch:
- keep feeding thin but real operator seams inside iOS / Apple-platform workflows
- prefer lifecycle/consumer distinctions that change breakpoint, compare-pair, or causal-reading behavior
- avoid reopening broad Apple-XPC theory pages unless a concrete workflow gap appears

## Branch-balance review
Recent runs had already touched iOS practical work, but this exact sub-seam was still thinner than the surrounding branch memory.

Balance assessment for this run:
- not another generic branch-shape sync
- not another hot-branch re-polish of already-dense MainActor / stream material
- instead, a smaller Apple-platform lifecycle-aware continuation that improves practical case routing inside an existing iOS branch

This is acceptable branch-balance behavior because:
- the seam was still under-specified in operator terms
- the run produced a real source-backed practical refinement
- the update stayed compact and fed canonical KB pages directly

Good next underfed candidates after this run:
1. a thinner `AsyncSequence` / async-bytes parser-or-classifier continuation if it can add a real operator stop rule
2. a non-mobile practical seam if iOS runs start clustering too tightly again
3. an Apple private-framework service reply-to-app-policy continuation only if it yields a concrete workflow note rather than more lifecycle wording

## Files changed
Created:
- `sources/ios/2026-03-27-ios-xpc-interruption-vs-invalidation-and-reconnection-notes.md`
- `runs/2026-03-27-1416-run-report.md`

Updated:
- `topics/ios-xpc-proxy-to-service-consumer-workflow-note.md`
- `topics/ios-practical-subtree-guide.md`
- `index.md`

## Search audit
Requested sources:
- exa
- tavily
- grok

Succeeded sources:
- tavily

Failed/degraded sources:
- exa — explicitly invoked; backend returned `402 Payment Required`; merged output still included some Exa-carried Apple documentation hits, so treat as attempted but degraded/unhealthy
- grok — explicitly invoked; backend returned repeated `502 Bad Gateway`

Endpoints used:
- Exa: `http://158.178.236.241:7860`
- Tavily: `http://proxy.zhangxuemin.work:9874/api`
- Grok: `http://proxy.zhangxuemin.work:8000/v1`

Assessment:
- this counts as a real external-research-driven run because all three requested sources were explicitly attempted
- effective usable research came primarily from Tavily-returned Apple documentation surfaces plus conservative follow-up retention of the documentation URLs
- conclusions were kept conservative because the source set was degraded

## Source notes retained this run
Primary retained sources:
- <https://developer.apple.com/documentation/foundation/nsxpcconnection?language=objc>
- <https://developer.apple.com/documentation/foundation/nsxpcconnection/interruptionhandler>
- <https://developer.apple.com/documentation/foundation/nsxpcconnection/invalidationhandler>
- <https://developer.apple.com/documentation/foundation/nsxpcconnection/invalidate()>
- <https://developer.apple.com/documentation/foundation/nsxpcconnection/exportedobject?language=objc>

Supporting field signal only:
- <https://stackoverflow.com/questions/47418944/nsxpcconnection-debugging-interruption-invalidation>
- <https://stackoverflow.com/questions/17263490/is-an-xpc-interruption-handler-called-when-launchd-kills-the-process>

## Practical takeaway added to the KB
For Apple XPC / `NSXPCConnection` reverse cases, keep this ladder explicit:

```text
accepted != interrupted != invalidated != reconnected != consumed
```

And keep this companion reminder nearby:

```text
proxy-visible != lifecycle-visible != exported-method-re-entry != durable-effect
```

That is the real value of this run.
It reduces a recurring operator mistake: overreading interruption, invalidation, or later healthy-looking reconnection as if they already proved the same remote consumer or later durable consequence.

## Commit / archival-sync status
KB changes were made and should be committed.
After commit, run:
- `/root/.openclaw/workspace/scripts/sync-reverse-expert-kb.sh`
