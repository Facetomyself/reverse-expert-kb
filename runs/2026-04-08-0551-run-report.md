# Reverse KB Run Report — 2026-04-08 05:51 Asia/Shanghai

Mode: external-research-driven
Date: 2026-04-08 05:51 Asia/Shanghai / 2026-04-07 21:51 UTC

## Summary
This run deliberately used the external-research slot on a thinner deobfuscation seam rather than continuing recent native / protocol / iOS micro-refinements.

Chosen seam:
- **encoded-branch / obfuscated-next-state recovery when control-flow flattening is already recognized but the next-state relation is still ambiguous**

Result:
- materially extended `topics/opaque-predicate-and-computed-next-state-recovery-workflow-note.md`
- added supporting source notes and saved the search-layer artifact

The practical value added this run is a sharper operator ladder for flattened-control-flow cases:
- dispatcher recognized
- next-carrier normalized
- trustworthy successor relation
- full deobfuscation required

That keeps analysts from overreading visible flattening or a decompiler-emphasized branch instruction as already-good successor proof.

## Direction / branch-balance review
Recent cadence before this run:
- 20:21: external protocol timeout/cancel owner realism refinement
- 03:51: external native Qt delivery realism refinement
- 04:51: external iOS XPC lifecycle realism refinement

That already covered three practical branches in sequence.
To avoid over-concentrating in those same families, this hour shifted to a thinner practical continuation in the deobfuscation branch.

Branch-choice reasoning:
- anti-stagnation still favored a real external-research pass
- recent practical work had already touched protocol, native, and iOS
- deobfuscation remains valuable and comparatively easier to underfeed than browser/mobile-protected-runtime convenience branches
- the existing opaque-predicate / computed-next-state note already had a natural place to absorb a bounded source-backed refinement

This improved branch balance by using an external slot on a still-practical deobfuscation seam rather than defaulting back into denser mobile/browser maintenance.

## What changed
### Added
- `sources/deobfuscation/2026-04-08-0551-deobf-search-layer.txt`
- `sources/deobfuscation/2026-04-08-encoded-branch-and-obfuscated-next-recovery-notes.md`
- `runs/2026-04-08-0551-run-report.md`

### Updated
- `topics/opaque-predicate-and-computed-next-state-recovery-workflow-note.md`

## New findings
Primary durable split reinforced this run:

```text
dispatcher recognized
  != next-carrier normalized
  != trustworthy successor relation
  != full deobfuscation required
```

Concrete practical reinforcements preserved in the note:
- Tigress flattening documentation explicitly treats obfuscated `next` handling and encoded conditional branches as flattening-shape variants, not a separate unrelated problem
- in encoded-branch cases, the visible branch instruction is often weaker than the last comparatively stable next-carrier before dispatcher re-entry
- practical recovery should often normalize one next-carrier family first, then extract one trustworthy successor edge before paying for broader cleanup
- one trustworthy edge or one reduced successor family is usually enough to restart static work, patching, or narrower dynamic confirmation

## Sources consulted
Primary source anchors:
- Tigress flattening docs
  - https://tigress.cs.arizona.edu/transformPage/docs/flatten/index.html
- Quarkslab deobfuscation write-up
  - https://blog.quarkslab.com/deobfuscation-recovering-an-ollvm-protected-program.html

Search-backed support:
- explicit `search-layer` attempt with `--source exa,tavily,grok`
- Exa and Tavily returned usable Tigress, Quarkslab, and practical tool/workflow surfaces
- Grok was explicitly invoked but returned 502 errors through the configured proxy path
- tooling/blog surfaces were retained conservatively as workflow support, not as sole truth

## Reflections / synthesis
This was the right shape of deobfuscation external run.
It did **not** create a broad obfuscation taxonomy page.
It tightened one practical continuation inside the existing opaque-predicate / computed-next-state workflow note.

The durable branch value is:
- keeping dispatcher recognition separate from actual successor proof
- keeping encoded branch noise separate from the normalized next-carrier that really matters
- preventing the workflow from stalling on full CFG prettification before one trustworthy edge exists
- making the deobfuscation branch more operator-useful rather than merely descriptive

## Candidate topic pages to create or improve
Good later deobfuscation candidates if branch balance points back here:
- a narrower compare-heavy continuation when one normalized next-carrier already exists but the remaining liar is copied-code branch inflation around two near-identical successor families
- a parent-page sync only if the sharpened encoded-branch / obfuscated-next rule still feels too leaf-local

## Next-step research directions
Near-term preference:
- rotate again after this run rather than staying in deobfuscation for several consecutive external slots
- if the branch returns soon, prefer another bounded proof-discipline refinement over broad obfuscation-family expansion
- keep avoiding internal-only wording/index maintenance unless it blocks a practical source-backed addition

## Concrete scenario notes or actionable tactics added this run
- when flattening is already known, ask whether the current blocker is really the branch instruction or the still-obscured next-carrier
- in encoded-branch / obfuscated-next cases, prefer the last comparatively stable carrier before dispatcher re-entry over upstream arithmetic noise
- stop once one OBB/state or state/successor edge is trustworthy enough to guide the next move; do not require full CFG beautification first

## Search audit
Search sources requested:
- `exa`
- `tavily`
- `grok`

Search sources succeeded:
- `exa`
- `tavily`

Search sources failed:
- `grok`

Exa endpoint:
- via `python3 /root/.openclaw/workspace/skills/search-layer/scripts/search.py --source exa,tavily,grok --mode deep --intent exploratory --num 5 --queries ...`

Tavily endpoint:
- via `python3 /root/.openclaw/workspace/skills/search-layer/scripts/search.py --source exa,tavily,grok --mode deep --intent exploratory --num 5 --queries ...`

Grok endpoint:
- via configured completions proxy surfaced in runtime errors: `http://proxy.zhangxuemin.work:8000/v1/chat/completions`

Searches attempted this run:
1. `Tigress FlattenObfuscateNext encoded branch next variable control flow flattening official docs`
2. `OLLVM flattening opaque predicate next state recovery symbolic execution Binary Ninja Miasm`
3. `control flow flattening computed next state opaque predicates encoded branch deobfuscation workflow`

Degraded-source-set note:
- this run still counts as a real external-research pass because all three sources were explicitly requested and Grok was actually invoked
- Grok returned repeated 502 errors through the proxy path
- Exa, Tavily, Tigress docs, and conservative practical workflow material were sufficient for a bounded extension of the existing note

## Commit / archival sync
If diffs remain after report write:
- commit only KB paths from `/root/.openclaw/workspace`
- then run `/root/.openclaw/workspace/scripts/sync-reverse-expert-kb.sh`
- if archival sync fails, preserve the local commit and record the failure conservatively rather than treating it as KB-content failure

## Best-effort notes
- `.learnings/ERRORS.md` remains non-blocking / best-effort only
