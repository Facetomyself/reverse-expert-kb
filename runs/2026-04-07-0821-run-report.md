# Reverse KB Run Report — 2026-04-07 08:21 Asia/Shanghai

Mode: external-research-driven
Date: 2026-04-07 08:21 Asia/Shanghai / 2026-04-07 00:21 UTC

## Summary
This run deliberately used the external-research slot on a thinner protocol seam instead of returning to browser or malware work.

Chosen seam:
- **Windows RPC service-contract registration / endpoint publication / reachability split**

Result:
- materially extended `protocol-service-contract-extraction-and-method-dispatch-workflow-note.md`
- added a source note and saved the explicit `search-layer` artifact

The practical value added this run is a sharper operator ladder for Windows RPC-shaped work:
- interface described
- server registered
- endpoint published
- client reachable/bindable
- meaningful dispatch / later consequence

That keeps analysts from overreading stubs, interface rosters, or endpoint visibility as already-good call reality.

## Direction / branch-balance review
Recent cadence:
- 06:21: external iOS PAC callback triage refinement
- 07:21: KB-internal browser broken-leaf repair

That meant this hour should return to a real external-research pass rather than becoming another internal-maintenance slot.

Branch-choice reasoning:
- browser remains easy to overfeed and had just received a canonical repair
- malware had already received several recent runs
- protocol remains materially healthier than before, but still benefits from thinner practical seams that improve real operator proof discipline
- Windows RPC registration/reachability was a bounded, practical seam where source-backed sharpening could improve service-contract and replay work without broad taxonomy drift

This run therefore improved branch balance in the intended way:
- real multi-source search attempted
- no browser/malware clustering by inertia
- one thinner protocol practical note materially improved

## What changed
### Added
- `sources/protocol/2026-04-07-rpc-registration-reachability-notes.md`
- `sources/protocol/2026-04-07-0821-rpc-registration-reachability-search-layer.txt`
- `runs/2026-04-07-0821-run-report.md`

### Updated
- `topics/protocol-service-contract-extraction-and-method-dispatch-workflow-note.md`

## New findings
Primary durable split reinforced this run:

```text
interface described
  != server registered
  != endpoint published
  != client reachable/bindable
  != meaningful method dispatch or later consequence
```

Concrete practical reinforcements preserved in the note:
- IDL/stub/proxy visibility is weaker than runtime server registration truth
- `RpcServerRegisterIf`-class registration is weaker than endpoint publication truth
- endpoint publication is weaker than actual client reachability in the run that matters
- successful reachability/binding is still weaker than one method dispatch or later consequence that answers the analyst’s real question

## Sources consulted
Primary source anchors:
- Microsoft Learn: `RpcServerRegisterIf`
  - https://learn.microsoft.com/en-us/windows/win32/api/rpcdce/nf-rpcdce-rpcserverregisterif
- Microsoft Learn: `RpcEpRegister`
  - https://learn.microsoft.com/en-us/windows/win32/api/rpcdce/nf-rpcdce-rpcepregister

Additional attempted anchor:
- endpoint mapper doc fetch attempted at `https://learn.microsoft.com/en-us/windows/win32/rpc/the-endpoint-mapper`
- current fetch path returned 404; non-blocking for this run

Search-backed support:
- explicit `search-layer` attempt with `--source exa,tavily,grok`
- Exa and Tavily returned usable surfaced material
- Grok was invoked but again failed through the configured proxy path

## Reflections / synthesis
This was the right shape of protocol external run.
It did **not** create a broad RPC taxonomy page.
It tightened one practical proof boundary inside the existing service-contract workflow note.

The durable branch value is:
- keeping service-contract extraction honest about runtime status
- preventing overclaim from interface descriptions, registration helpers, or endpoint visibility alone
- improving the handoff from service-contract recovery into replay/binding/method-dispatch work

## Candidate topic pages to create or improve
Good later protocol candidates if branch balance points back here:
- a narrower client-binding-posture / call-context continuation when registration and reachability are already good enough but replay still lies
- a parent-page sync only if the new registration/reachability memory still feels too leaf-local

## Next-step research directions
Near-term preference:
- keep alternating across underfed branches rather than spending several consecutive external runs inside one family
- if protocol returns soon, prefer similarly bounded proof-discipline refinements over broad family expansion

## Concrete scenario notes or actionable tactics added this run
- do not stop at recovered stubs or interface rosters
- do not stop at runtime registration helpers alone
- do not stop at endpoint publication alone
- freeze whether the current missing proof is registration, publication, reachability, or actual method dispatch before reopening broader contract doubt

## Search audit
Search sources requested:
- `exa`
- `tavily`
- `grok`

Search sources succeeded:
- `exa`
- `tavily`

Search sources failed:
- `grok`

Exa endpoint:
- via `python3 /root/.openclaw/workspace/skills/search-layer/scripts/search.py --source exa,tavily,grok`

Tavily endpoint:
- via `python3 /root/.openclaw/workspace/skills/search-layer/scripts/search.py --source exa,tavily,grok`

Grok endpoint:
- via configured completions proxy surfaced in runtime errors: `http://proxy.zhangxuemin.work:8000/v1/chat/completions`

Searches attempted this run:
1. `Windows RPC endpoint mapper interface registration binding reachability official docs RpcServerRegisterIf RpcEpRegister`
2. `RPC interface registration endpoint mapper reachability official docs Windows`
3. `described registered reachable RPC endpoint mapper binding official docs`

Degraded-source-set note:
- this run still counts as a real external-research pass because all three sources were explicitly requested and Grok was actually invoked
- Grok returned no usable results because of repeated proxy-side 502 failures
- Exa, Tavily, and primary Microsoft docs were sufficient for a conservative practical extension of the existing protocol note

## Commit / archival sync
If diffs remain after report write:
- commit only KB paths from `/root/.openclaw/workspace`
- then run `/root/.openclaw/workspace/scripts/sync-reverse-expert-kb.sh`
- if archival sync still fails because remote `main` moved ahead, preserve the local commit and record the sync-state failure conservatively rather than treating it as KB-content failure

## Best-effort notes
- `.learnings/ERRORS.md` remains best-effort only and should not block this run
