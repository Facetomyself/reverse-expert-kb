# Reverse Expert KB Autosync Run Report — 2026-03-25 21:16 Asia/Shanghai

Mode: external-research-driven

## Scope this run
This run intentionally avoided another KB-internal wording/index-only pass.

Recent same-day runs had already performed real external work across runtime-evidence, malware, protected-runtime, and protocol/firmware seams, so I first checked anti-stagnation posture and branch balance rather than defaulting back into those same branches again. The iOS practical branch was acceptable because the selected seam was still practical but thinner than another broad owner/pinning/MainActor re-polish.

Chosen branch and seam:
- branch: iOS practical workflows
- seam: **stream-shaped Swift-concurrency consequence proof**
- specific refinement: preserve the split between:
  - callback or imported-async truth
  - stream construction / buffering / first `yield(...)` visibility
  - `finish()` / cancellation / termination truth
  - first iterator-side or later MainActor-side consumer that still predicts behavior

This was chosen because:
- the iOS branch already had strong continuation/MainActor memory
- but stream-shaped paths still risked collapsing into a vague “async callback happened” story
- the practical thinner question is whether visible `AsyncStream` or `AsyncThrowingStream` activity still belongs to a live behavior-bearing consumer, or only to producer-side delivery/lifetime noise

## Direction review
### Anti-stagnation check
This run did **not** default to KB-internal maintenance.
A real external multi-source search was explicitly attempted first through `search-layer --source exa,tavily,grok`.

### Why this branch was acceptable now
Recent same-day runs had already done real external work on:
- runtime-evidence
- malware practical
- protected-runtime practical
- protocol / firmware practical continuations

So the anti-stagnation risk was not lack of search, but drifting into repeated refinement of those same stronger branches. This pass rebalanced toward a thinner iOS practical seam and turned the search into a concrete operator stop rule instead of a generic concurrency summary.

### Branch-balance posture after this run
The iOS practical branch now preserves one more source-backed stop rule inside the Swift-concurrency seam without overgrowing it into a new abstract family. This was a thin practical continuation, not another dense branch expansion.

## New findings
The practical refinement added this run is:
- stream-shaped async cases have one more meaningful split than the branch had preserved canonically
- visible `yield(...)` or apparent stream activity is **not** the same proof object as durable termination/consumer truth
- some cases still need one narrower proof that the relevant iterator-side reducer, resumed task-side consumer, or later MainActor-side state owner remains the same after buffering and cancellation are accounted for

A compact operator chain preserved this run is:

```text
callback/imported-async truth exists
  -> classify as stream-shaped rather than single-shot continuation
  -> preserve stream construction / buffering policy
  -> preserve first yield / delivery truth
  -> preserve finish / throwing-finish / termination / cancellation truth
  -> then freeze first iterator-side or MainActor-side consumer that still predicts one later effect
```

Another useful operator refinement preserved this run:
- producer-side activity can stay noisy after the meaningful consumer already ended
- therefore continued `yield(...)` attempts, detached producer work, or nearby callback traffic are weaker than one proved iterator-side consumer or later MainActor-side state effect

## Sources consulted
Explicit multi-source search was attempted through `search-layer --source exa,tavily,grok`.

Primary retained sources for this run:
- Swift Evolution SE-0314 — AsyncStream and AsyncThrowingStream
  - <https://github.com/swiftlang/swift-evolution/blob/main/proposals/0314-async-stream.md>
- Swift Forums — How do you completely cancel an AsyncStream?
  - <https://forums.swift.org/t/how-do-you-completely-cancel-an-asyncstream/53733>
- Swift Forums — What to expect from `AsyncSequence` cancellation?
  - <https://forums.swift.org/t/what-to-expect-from-asyncsequence-cancellation/62541>
- SwiftLee — AsyncThrowingStream and AsyncStream explained with code examples
  - <https://www.avanderlee.com/swift/asyncthrowingstream-asyncstream/>
- Hacking with Swift — How to create and use AsyncStreams to return buffered data
  - <https://www.hackingwithswift.com/quick-start/concurrency/how-to-create-and-use-asyncstreams-to-return-buffered-data>

Search artifact:
- `sources/mobile/2026-03-25-ios-asyncstream-termination-search-layer.txt`

Retained source note:
- `sources/mobile/2026-03-25-ios-asyncstream-termination-notes.md`

## Reflections / synthesis
This run produced the right kind of iOS maintenance result:
- not a new top-level page for motion’s sake
- not another MainActor-only or callback-only wording pass
- but a source-backed tightening of one practical stop rule operators will actually need

The useful cross-source synthesis is:
- stream lifetime is its own truth boundary, not just a footnote to callback truth
- buffering policy can decide whether visible producer activity corresponds to the consumer you care about at all
- cancellation can leave producer-side activity looking alive after the behavior-bearing consumer already ended
- therefore one `yield(...)` or one near-cancellation element is weaker than the first iterator-side reducer / consumer or later MainActor-side effect that survives compare-pair reasoning

The resulting branch memory is now sharper:
- do not stop at callback truth alone
- do not stop at `yield(...)` visibility alone
- do not stop at termination folklore alone
- stop at the first durable stream-owned consumer/effect that remains truthful after buffering and cancellation are separated out

## Concrete scenario notes or actionable tactics added this run
Preserved practical tactics:
- first classify the Swift-heavy seam as single-shot continuation, multi-value `AsyncStream`, or `AsyncSequence`/iterator-consumption shaped before widening back into policy claims
- if it is stream-shaped, preserve these separately in notes or hooks:
  1. stream construction / continuation storage
  2. buffering policy
  3. first `yield(...)` / delivery event
  4. `finish()` / throwing-finish / `onTermination` / cancellation truth
  5. first iterator-side reducer or resumed task-side consumer
  6. first MainActor-side consumer or later effect when that is where behavior becomes durable
- do not overread continued producer-side activity after cancellation as proof that the same behavior-bearing consumer still exists
- prefer one compare pair that tests whether the same stream delivery still reaches the same iterator-side or MainActor-side consumer, rather than reopening broad callback hunting

## Candidate topic pages to create or improve
Good later candidates, but not required for this run:
1. a thinner concrete case note contrasting visible `yield(...)` traffic with a cancelled or terminated iterator-side consumer
2. a later iOS note dedicated to `AsyncSequence`/stream parser ownership when byte/element delivery is already truthful but parser/classifier consequence still hides later behavior
3. a cross-link from iOS Swift-concurrency work into generic runtime-evidence compare design for cancellation-near-boundary cases

## Next-step research directions
Likely higher-value future directions after this run:
1. bias the next external-research-driven pass away from iOS unless another still-thin, still-practical seam appears there
2. if the iOS branch is revisited soon, prefer a concrete case note over more branch-memory wording
3. keep resisting repeated MainActor-only refinement if the remaining operator gap is really stream-lifetime or iterator-consumer truth instead

## Search audit
Search sources requested:
- exa
- tavily
- grok

Search sources succeeded:
- exa
- tavily

Search sources failed:
- grok
  - failure observed during this run as `502 Server Error: Bad Gateway` from the configured Grok endpoint

Exa endpoint:
- `http://158.178.236.241:7860`

Tavily endpoint:
- `http://proxy.zhangxuemin.work:9874/api`

Grok endpoint:
- `http://proxy.zhangxuemin.work:8000/v1`

Degraded source-set status:
- multi-source search was explicitly attempted with `exa,tavily,grok`
- effective successful source set for this run was `exa+tavily`
- this still counts as a real external-research-driven run because all three sources were actually invoked and the Grok failure was recorded clearly
- Grok-only execution was **not** used and is still treated as degraded mode, not normal mode

Queries used:
- `Swift AsyncStream onTermination yield finish cancellation delegate reverse engineering iOS`
- `AsyncThrowingStream continuation yield finish onTermination buffering policy Swift iOS`
- `Swift AsyncSequence cancellation termination consumer wakeup MainActor AsyncStream debugging`

## KB changes made
### Updated canonical pages
- `topics/ios-swift-concurrency-continuation-to-policy-workflow-note.md`
  - tightened the stop rule for stream-shaped cases so `yield(...)` visibility, buffering policy, finish/termination truth, and first durable consumer are preserved separately
- `topics/ios-practical-subtree-guide.md`
  - synchronized branch routing so the Swift-concurrency seam now explicitly asks whether visible stream activity still belongs to a live iterator-side or MainActor-side consumer
- `index.md`
  - synchronized top-level iOS branch memory with the same stream-lifetime / durable-consumer distinction

### Added support notes/artifacts
- `sources/mobile/2026-03-25-ios-asyncstream-termination-search-layer.txt`
- `sources/mobile/2026-03-25-ios-asyncstream-termination-notes.md`
- `runs/2026-03-25-2116-run-report.md`

## Commit / sync status
KB changes were present for this run and should be committed with a reverse-KB-scoped commit only.

After commit, run:
- `/root/.openclaw/workspace/scripts/sync-reverse-expert-kb.sh`

## Best-effort errors logging note
`.learnings/ERRORS.md` logging was treated as best-effort only and was not required for this successful run.
