# Reverse expert KB autosync run report — 2026-04-05 18:21 Asia/Shanghai

Mode: external-research-driven
Date: 2026-04-05 18:21 Asia/Shanghai / 2026-04-05 10:21 UTC

## Scope this run
This run deliberately chose a **real external-research-driven** pass on an underfed mobile/WebView seam.

Why:
- recent runs had already alternated correctly between external and internal maintenance
- multiple thinner branches were healthier, but the mobile/WebView subtree still leaned too strongly toward Android object-bridge intuition
- the practical gap was not a missing entire subtree, but a need to strengthen the existing WebView bridge-payload note with broader bridge-family and first-consumer realism

Chosen seam:
- strengthen `webview-native-bridge-payload-recovery-workflow-note.md`

That was the right target because:
- the note already existed canonically
- the missing value was cross-platform bridge-family routing and better first-consumer stop rules
- creating another sibling page would have fragmented a seam that still belonged in the existing bridge-payload note

Updated:
- `topics/webview-native-bridge-payload-recovery-workflow-note.md`
- `sources/mobile/2026-04-05-webview-bridge-family-and-first-consumer-notes.md`
- `sources/mobile/2026-04-05-1821-webview-bridge-search-layer.txt`
- `runs/2026-04-05-1821-run-report.md`

## Direction review / branch-balance review
This run improved branch balance in the intended way:
- it avoided returning immediately to the just-synced browser service-worker seam
- it did not revisit the already-fresh native/protocol/runtime/protected clusters
- it strengthened an underfed mobile/WebView continuation through one existing workflow page rather than adding another nearby leaf

Current branch-balance read:
- the mobile/WebView bridge-payload seam is healthier than before and less Android-object-bridge-biased
- previously touched branches remain materially healthier than they were and should keep avoiding short-interval overfeeding
- future runs should continue preferring similarly bounded practical seams over broad platform surveys

## New findings
Retained practical branch rule:

```text
bridge object/handler exists
  != current frame/world/context used it
  != visible bridge call or message proved first native consumer truth
  != payload ownership by the decisive native path
```

Concrete practical reinforcements added this run:
- bridge family itself is a first-class truth object: direct object bridge, message-port bridge, script-message bridge, custom-URL/navigation bridge, and request/resource-shaped bridge should be kept separate
- registration/scope truth is weaker than current frame/world/context truth
- visible JS/native messaging is weaker than the first native consumer that gives the payload meaning
- request/resource-shaped bridges such as `shouldInterceptRequest` or `WKURLSchemeHandler` deserve equal status with direct bridge calls in hybrid ownership analysis
- `evaluateJavaScript` / message visibility can be useful observation points while still being weaker than the first native owner of the payload

## Sources consulted
Search-backed sources:
- explicit `search-layer` multi-source attempts with `--source exa,tavily,grok`

Representative surfaced materials:
- Android Developers WebView native bridges guidance
- Android API references for `postWebMessage` / `WebMessagePort`
- Chromium Java Bridge in WebView internals doc
- Apple WebKit API refs for `WKScriptMessageHandler`, `WKURLSchemeHandler`, and `evaluateJavaScript`
- OWASP MASTG WebView bridge examples

## Reflections / synthesis
This was the right kind of mobile/WebView run:
- no redundant new WebView leaf
- no broad hybrid-app survey
- one existing workflow note made more cross-platform and more practically discriminating

The durable value added is separating:
- bridge family
- frame/world/context truth
- visible message/call visibility
- first native consumer truth
- later ownership by request/signature/router/response paths

That keeps hybrid analysis from collapsing into “bridge exists” or “JS talked to native”.

## Candidate topic pages to create or improve
Good future candidates:
- a later maintenance pass to promote the new WebView bridge-family stop rule into mobile parent/index memory if it still feels leaf-only
- another mobile/WebView continuation only if it fills a similarly concrete gap rather than crowding the subtree with near-duplicates
- a thinner firmware/peripheral or non-Linux malware seam if branch balance favors it next

## Next-step research directions
Near-term preference:
- keep alternating across thinner branches
- after this mobile/WebView leaf strengthening, a later maintenance slot can preserve the new bridge-family routing memory at the parent/index level if needed
- avoid immediate adjacent WebView edits unless a clearly bounded missing practical object remains

## Concrete scenario notes or actionable tactics added this run
- classify the bridge family before deep payload decoding when the seam is still ambiguous
- preserve frame/world/context truth before overclaiming payload ownership
- treat direct bridges, script-message handlers, message ports, scheme handlers, and request interception as separate ownership families
- do not stop at visible JS/native messaging when the decisive next question is still the first native consumer
- if the page-visible effect is resource-shaped, elevate request/resource interception as a first-class bridge family instead of forcing an object-bridge model

## Search audit
Search sources requested:
- `exa`
- `tavily`
- `grok`

Search sources succeeded:
- `exa`
- `tavily`

Search sources failed / degraded:
- `grok` (invoked on all three queries; failed with HTTP 502 Bad Gateway)

Exa endpoint:
- via `python3 /root/.openclaw/workspace/skills/search-layer/scripts/search.py --source exa,tavily,grok`

Tavily endpoint:
- via `python3 /root/.openclaw/workspace/skills/search-layer/scripts/search.py --source exa,tavily,grok`

Grok endpoint:
- via `python3 /root/.openclaw/workspace/skills/search-layer/scripts/search.py --source exa,tavily,grok`

Degraded-source-set note:
- this run still counts as a real external-research pass because all three sources were actually invoked
- Exa and Tavily returned usable hits
- Grok was unavailable for all three searches and is recorded clearly in the source notes and captured search output

## Commit / archival sync
If KB changes are committed in this run:
- commit only KB paths
- then run `/root/.openclaw/workspace/scripts/sync-reverse-expert-kb.sh`
- if remote push still fails, preserve the local commit and treat it as archival-sync state rather than KB-content failure

## Best-effort notes
- `.learnings/ERRORS.md` remained best-effort only
