# Reverse KB Run Report — 2026-04-07 16:21 Asia/Shanghai

Mode: external-research-driven
Date: 2026-04-07 16:21 Asia/Shanghai / 2026-04-07 08:21 UTC

## Summary
This run deliberately used the external-research slot on a thinner protected-runtime observation seam instead of returning to browser or malware work.

Chosen seam:
- **protected-runtime observation-topology selection: quieter lower surface exists != it answers the current question better**

Result:
- materially extended `topics/protected-runtime-observation-topology-selection-workflow-note.md`
- added a supporting source note and saved the explicit `search-layer` artifact

The practical value added this run is a sharper operator ladder for topology selection:
- user-space surface is weak/noisy/detected
- a quieter lower surface exists
- that lower surface actually answers the current question better
- lower-surface visibility is the decisive boundary
- broader mechanism/owner/consequence is solved

That keeps analysts from overreading the existence of a quieter lower surface as proof that it is already the right or sufficient next move.

## Direction / branch-balance review
Recent cadence:
- 14:21: external protocol pending-request owner-realism refinement
- 15:21: KB-internal top-level branch-memory sync

That meant this hour should return to a real external-research pass rather than becoming another internal-maintenance slot.

Branch-choice reasoning:
- browser and malware had already received substantial recent attention
- protected-runtime work is strong, but this was a bounded proof-discipline refinement rather than another dense anti-instrumentation expansion
- the topology-selection seam was the right parent-level place to sharpen the handoff between noisy user-space surfaces and lower-surface tracing choices

This run therefore improved branch balance in the intended way:
- real multi-source search attempted
- no browser/malware clustering by inertia
- one thinner protected-runtime practical seam materially improved

## What changed
### Added
- `sources/mobile/2026-04-07-1621-topology-selection-search-layer.txt`
- `sources/mobile/2026-04-07-topology-selection-lower-surface-notes.md`
- `runs/2026-04-07-1621-run-report.md`

### Updated
- `topics/protected-runtime-observation-topology-selection-workflow-note.md`

## New findings
Primary durable split reinforced this run:

```text
quieter or lower surface exists
  != that surface answers the current question better
  != lower-surface visibility is the decisive boundary
  != broader mechanism or owner path is solved
```

Concrete practical reinforcements preserved in the note:
- noisy/detectable user-space hooks are weaker than a justified lower-surface move
- lower-surface availability is weaker than proving that the current behavior-bearing boundary is visible there
- lower-surface event visibility is weaker than proving decisive gate/owner/consequence truth
- the goal is usually to recover one trustworthy lower boundary and then route back upward, not to stay at the lowest surface forever

## Sources consulted
Primary source anchors actually used:
- Linux seccomp filter docs
  - https://www.kernel.org/doc/html/latest/userspace-api/seccomp_filter.html

Search-backed support:
- explicit `search-layer` attempt with `--source exa,tavily,grok`
- Exa and Tavily returned usable surfaced material
- Grok was invoked but again failed through the configured proxy path

Attempted but degraded source paths:
- `https://docs.ebpf.io/linux/concepts/overview/` -> fetch route returned 404 in this run
- `https://source.android.com/docs/core/architecture/kernel/bpf` -> fetch route hit redirect issues in this run

Artifact-capture housekeeping note:
- the first search-artifact save attempt used a non-existent subtree path; this was corrected and the artifact was then saved under `sources/mobile/`
- this was also logged best-effort in `.learnings/ERRORS.md`

## Reflections / synthesis
This was the right shape of protected-runtime external run.
It did **not** create another broad anti-instrumentation taxonomy page.
It tightened one practical topology-selection boundary inside the existing parent note.

The durable branch value is:
- keeping lower-surface escalation honest
- preventing overclaim from “quieter lower surface exists” reasoning
- improving the handoff between noisy user-space evidence and lower-surface tracing choices

## Candidate topic pages to create or improve
Good later protected-runtime candidates if branch balance points back here:
- a narrower compare-heavy note around proving that a lower surface reduced uncertainty more than one improved user-space surface would have
- a parent-page sync only if the new topology-selection memory still feels too leaf-local

## Next-step research directions
Near-term preference:
- keep alternating across underfed branches rather than spending several consecutive external runs inside one family
- if protected-runtime returns soon, prefer similarly bounded proof-discipline refinements over broad family expansion

## Concrete scenario notes or actionable tactics added this run
- do not escalate to lower surfaces just because user-space hooks are noisy
- freeze the exact question before choosing eBPF/seccomp/SVC tracing
- prove that the lower surface exposes the behavior-bearing boundary you need
- route back upward once one lower boundary is truthful enough

## Search audit
Search sources requested:
- `exa`
- `tavily`
- `grok`

Search sources succeeded:
- `exa`
- `tavily`

Search sources failed:
- `grok`

Exa endpoint:
- via `python3 /root/.openclaw/workspace/skills/search-layer/scripts/search.py --source exa,tavily,grok`

Tavily endpoint:
- via `python3 /root/.openclaw/workspace/skills/search-layer/scripts/search.py --source exa,tavily,grok`

Grok endpoint:
- via configured completions proxy surfaced in runtime errors: `http://proxy.zhangxuemin.work:8000/v1/chat/completions`

Searches attempted this run:
1. `eBPF tracing overhead observability anti-debug instrumentation lower surface official docs`
2. `seccomp ptrace anti debug Android syscall tracing docs eBPF official`
3. `user space hooks vs syscall tracing observation distortion anti instrumentation docs`

Degraded-source-set note:
- this run still counts as a real external-research pass because all three sources were explicitly requested and Grok was actually invoked
- Grok returned no usable results because of repeated proxy-side 502 failures
- two fetch paths degraded (404 / redirect), but the usable source set was sufficient for a conservative practical refinement of the existing topology note

## Commit / archival sync
If diffs remain after report write:
- commit only KB paths from `/root/.openclaw/workspace`
- then run `/root/.openclaw/workspace/scripts/sync-reverse-expert-kb.sh`
- if archival sync fails, preserve the local commit and record the failure conservatively rather than treating it as KB-content failure

## Best-effort notes
- `.learnings/ERRORS.md` remains best-effort only and did not block this run
