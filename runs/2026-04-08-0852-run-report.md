# Reverse KB Run Report — 2026-04-08 08:52 Asia/Shanghai

Mode: external-research-driven
Date: 2026-04-08 08:52 Asia/Shanghai / 2026-04-08 00:52 UTC

## Summary
This run deliberately used the external-research slot on the protocol branch, rotating away again after recent work in protected-runtime, malware, deobfuscation, and iOS.

Chosen seam:
- **WebSocket upgrade truth vs first framed-consumer truth when handshake visibility already exists but the first behavior-changing frame consumer is still ambiguous**

Result:
- materially extended `topics/protocol-websocket-upgrade-to-first-framed-consumer-workflow-note.md`
- added supporting source notes and saved the search-layer artifact

The practical value added this run is a sharper separation ladder for WebSocket-shaped cases:
- Upgrade accepted
- first framed traffic
- receive callback visibility
- first behavior-changing framed consumer

That keeps analysts from overreading `101 Switching Protocols`, `onopen`, or one library receive callback as already-good proof of application-owned framed behavior.

## Direction / branch-balance review
Recent cadence before this run:
- 05:51: external deobfuscation encoded-next recovery refinement
- 06:51: external malware send-boundary realism refinement
- 07:51: external protected-runtime topology choice refinement

That left protocol as a good practical target for this hour.

Branch-choice reasoning:
- anti-stagnation still favored a real external-research pass
- protocol remains a branch where narrow practical continuations beat broad survey growth
- the existing WebSocket workflow note had a natural place to absorb a bounded source-backed refinement
- this therefore improved branch balance without widening into a generic protocol taxonomy pass

## What changed
### Added
- `sources/protocol/2026-04-08-0852-ws-search-layer.txt`
- `sources/protocol/2026-04-08-websocket-upgrade-vs-framed-consumer-notes.md`
- `runs/2026-04-08-0852-run-report.md`

### Updated
- `topics/protocol-websocket-upgrade-to-first-framed-consumer-workflow-note.md`

## New findings
Primary durable split reinforced this run:

```text
Upgrade accepted
  != first framed traffic
  != receive callback visibility
  != first behavior-changing framed consumer
```

Concrete practical reinforcements preserved in the note:
- RFC 6455 already separates handshake success from later framed exchange
- browser/client `open` surfaces are weaker than later message-bearing surfaces
- library receive callbacks can be the right reduction boundary, but are still weaker than the first parser/dispatcher/reducer that actually predicts later behavior
- the workflow should stop only when one framed consumer meaningfully reduces the next protocol question

## Sources consulted
Primary source anchors:
- RFC 6455 — The WebSocket Protocol
- MDN Web Docs — WebSocket `open` / `message` event surfaces
- libwebsockets callback/receive surfaces

Search-backed support:
- explicit `search-layer` attempt with `--source exa,tavily,grok`
- Exa and Tavily returned usable RFC/MDN/library surfaces
- Grok was explicitly invoked but returned 502 errors through the configured proxy path

## Reflections / synthesis
This was the right shape of protocol external run.
It did **not** create a broad WebSocket protocol page.
It tightened one practical continuation inside the existing WebSocket workflow note.

The durable branch value is:
- keeping handshake success separate from framed traffic
- keeping framed traffic separate from framework/library receive visibility
- keeping callback visibility separate from the first behavior-changing consumer
- making the protocol branch more useful for real operator reduction rather than surface-level handshake inventory

## Candidate topic pages to create or improve
Good later protocol candidates if branch balance points back here:
- a narrower continuation when first framed consumer truth is already good enough but the next liar is message fragmentation / continuation-frame realism rather than basic handshake-vs-frame confusion
- a parent/subtree sync only if the sharpened WebSocket ladder still feels too leaf-local

## Next-step research directions
Near-term preference:
- rotate again after this run rather than clustering several external slots inside protocol
- continue preferring thin practical continuations over broad protocol-family restatements
- avoid internal-only wording/index maintenance unless it blocks a practical source-backed addition

## Concrete scenario notes or actionable tactics added this run
- ask explicitly whether the run only proved Upgrade success, actual framed traffic, receive-callback visibility, or the first behavior-changing framed consumer
- do not stop at `101 Switching Protocols` or `onopen` by default
- if receive callbacks are visible, still decide whether the first meaningful parser/dispatcher/reducer remains unproven

## Search audit
Search sources requested:
- `exa`
- `tavily`
- `grok`

Search sources succeeded:
- `exa`
- `tavily`

Search sources failed:
- `grok`

Exa endpoint:
- via `python3 /root/.openclaw/workspace/skills/search-layer/scripts/search.py --source exa,tavily,grok --mode deep --intent exploratory --num 5 --queries ...`

Tavily endpoint:
- via `python3 /root/.openclaw/workspace/skills/search-layer/scripts/search.py --source exa,tavily,grok --mode deep --intent exploratory --num 5 --queries ...`

Grok endpoint:
- via configured completions proxy surfaced in runtime errors: `http://proxy.zhangxuemin.work:8000/v1/chat/completions`

Searches attempted this run:
1. `RFC 6455 opening handshake frames masking first application consumer WebSocket`
2. `WebSocket client onopen message event framing MDN official docs`
3. `libwebsockets callback receive data first framed consumer official docs`

Degraded-source-set note:
- this run still counts as a real external-research pass because all three sources were explicitly requested and Grok was actually invoked
- Grok returned repeated 502 errors through the proxy path
- Exa, Tavily, and the retained RFC/MDN/library surfaces were sufficient for a bounded extension of the existing WebSocket note

## Commit / archival sync
If diffs remain after report write:
- commit only KB paths from `/root/.openclaw/workspace`
- then run `/root/.openclaw/workspace/scripts/sync-reverse-expert-kb.sh`
- if archival sync fails, preserve the local commit and record the failure conservatively rather than treating it as KB-content failure

## Best-effort notes
- `.learnings/ERRORS.md` remains non-blocking / best-effort only
